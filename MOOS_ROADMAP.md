# MoOS — خارطة التنفيذ الحالية

آخر تحديث تنفيذي: 2026-08-01 (`PROJECT_STATE.md` هو الخريطة الحية الأحدث دائماً)

> **وصولية الدخول والجلسة والطاقة (`4073365e`, `e5c18d9b`, `2c298613`, `d51a1252`):** أزرار Logout/Power في جميع
> الثيمات الـ16 تعلن الآن صراحةً دور Button وحالة الضغط لقارئ الشاشة، مع تثبيت
> الاسم والوصف وStrongFocus والتنقل بالأسهم في regression gate. نجح `just check`
> كما صار حقل Password في شاشة القفل ذا اسم دلالي ثابت، وزر Unlock يعلن دوره
> وحالة الضغط مع الإبقاء على مسار PAM الحقيقي. نجح `just check` كاملاً. هذا
> إثبات مصدر واختبارات فقط؛ فحص قارئ شاشة حي ومصفوفة Light/Dark ×
> RTL/LTR × 4K ما زالا مطلوبين على صورة MoOS مركبة.
> أُغلق أيضاً overflow فعلي في Power Dock: الصف الأحادي كان يستطيع حمل 8 مفاتيح
> بعرض يتجاوز جزيرة 50 وحدة قبل حساب المسافات. صار Grid responsive بحد خمسة
> أعمدة وصفوف متوازنة 3+3/4+3/4+4، والتنقل العمودي يقفز بعدد الأعمدة الفعلي
> ثم يصل إلى Cancel بدل الالتفاف إلى زر علوي غير مرتبط، بينما ينعكس الأفقي فقط
> في RTL. بوابة جديدة تختبر حالات 1–8، ونجحت اختبارات
> UI2 الـ28 والفحص الكامل؛ يبقى render حي مع fractional scaling مطلوباً.
> كذلك لم يعد قارئ الشاشة يستقبل mnemonic خاماً مثل `Slee&p` و`&Hibernate` من
> ActionButton الحقيقي المشترك بين Login/Lock؛ الاسم الآن يأتي من
> `MnemonicData.plainTextLabel` فيما تبقى underline واختصارات Alt كما هي. بوابة
> UI2 تمنع الرجوع إلى `root.text`. هذا دليل مصدر؛ قارئ شاشة حي ما زال مطلوباً.
> وأُغلق مسار تفعيل مفقود: Portal keys في Logout/Power وزر Unlock كانوا يعلنون
> الاسم والدور بلا `Accessible.onPressAction`. المفاتيح تستدعي الآن
> `animateClick()` فتصل إلى `clicked` و`armOrFire` نفسه (لا تجاوز لتأكيد الطاقة)،
> وUnlock يصل إلى `clicked` نفسه. النسخ الـ16 متطابقة والفحص الكامل ناجح؛ يبقى
> اختبار assistive activation حي مطلوباً.
> أُصلح أيضاً UserDelegate الفعلي في Plasma Login Manager: الصيغة الميتة
> `function accessiblePressAction()` استُبدلت بـ`Accessible.onPressAction`، وصار
> اختيار المستخدم يعمل من قارئ الشاشة ومن Enter/Return/Space عبر نفس إشارة
> النقر. زر إجراءات greeter يمرر ضغط أدوات الوصول عبر `animateClick()` الحقيقي.
> كذلك أصبحت انتقالات الأزرار الأربعة وانتقالات اختيار المستخدم الثلاثة ذات
> مدة صفر حقيقية عند Reduced Motion/Cloud، وأُضيف الملفان لاختبار بوابة الحركة
> المبني على Qt. فرع runtime ينتظر صورة تحتوي Qt/KDE و`kwriteconfig6`.
> **Reduced Motion للقفل (`ebcebc57`):** أُغلقت أيضاً انتقالات بطاقة المصادقة
> الثمانية وانتقالات المشهد الستة إلى مدة صفر فعلية، ولا يبدأ تنبيه PAM المكرر
> حركة bounce عندما تكون الحركة معطلة. لم تتغير bindings كلمة المرور أو PAM أو
> مؤقتات الحماية أو إشارات Unlock. الملفان داخل بوابة Qt، و`just check` ناجح.

> **تدقيق Mo PC Remote للطاقة والجلسة (غير منشور بعد):** أُصلحت أوامر Linux
> الوهمية: Lock وSign out أصبحا يستهدفان جلسة Plasma نفسها، وSleep/Restart/
> Shutdown تستخدم تنفيذًا non-blocking مع فحص exit code وtimeout بدل إعلان
> النجاح بعد `Process.Start`. وفي `moos-cloud` يمنع الـAPI نفسه عمليات طاقة
> المضيف المشترك ويرجع 403. كشف التدقيق اللاحق أن حساب Cloud بلا كلمة مرور،
> لذلك Lock غير قابل للفك، وأن Sign out يوقف سطحه الخاص خروجاً نظيفاً؛ صارت
> الأوامر الخمسة كلها بيد console الخادم وتشرح الواجهة ذلك. MoOS المكتبي يحتفظ
> بكل الوظائف. نجح 38 اختبار .NET، نشر Linux، بناء Windows،
> TypeScript typecheck، PWA v16 و`just check` كامل. لم يُنفذ أمر طاقة فعلي؛
> compose للصورة واختبار الهاتف/البوابة الحقيقيان ما زالا مطلوبين.

> **تدقيق نقل الملفات والصوت في Mo PC Remote (`67e58e6a`):** أُزيل session
> bearer القابل لإعادة الاستخدام من روابط download وaudio واستُبدل بتذاكر
> 256-bit مرتبطة بالغرض، أحادية الاستخدام وتنتهي خلال 45 ثانية. أثبت HTTP حي
> معزول: download `200` ثم replay `401`، وaudio upstream `502` ثم replay `401`.
> الرفع صار ذرياً عبر ملف مؤقت يُحذف عند الانقطاع، محدوداً بـ1 GiB، ويحجز آخر
> 512 MiB للنظام مع فحص مساحة paced كل 64 MiB. مخزن التذاكر محدود بـ1024
> وإخلاء FIFO بتكلفة O(1) بدل مسح كامل تربيعي تحت الضغط. نجح 48 اختبار .NET، Linux/
> Windows builds، TypeScript، PWA v17 و`just check`. اختبار نقل حقيقي من هاتف
> عبر tailnet وcompose الصورة ما زالا مطلوبين قبل الشحن.

> **دورة حياة Mo PC Remote (`44ab6880`):** أُغلق سباقان كانا يعيدان العمل بعد
> خروج المستخدم: مؤقت Refresh غير المملوك كان يستطيع فتح socket بعد Sign out،
> وطلب audio ticket المتأخر كان يستطيع بدء الصوت بعد Stop. صارت الاتصالات
> والمؤقتات مملوكة وقابلة للإلغاء، والصوت generation-bound مع إزالة media src
> وإغلاق encoder عند Stop/unmount. أضيف gate إلى CI، وبُنيت PWA v18 ونجح
> `just check` كامل.

> **إبطال جلسة الهاتف (`dcdb9c0c`):** زر Sign out كان يمسح التوكن محلياً فقط
> ويترك bearer صالحاً على الخادم حتى 60 دقيقة. صار يستدعي `/api/logout` وينتظر
> الإبطال server-side مع بقاء الخروج المحلي آمناً عند انقطاع الشبكة. أضيف gate
> إلى CI، وبُنيت PWA v19 ونجح `just check` كامل.

> **وصولية واجهة الهاتف (`aad4a25c`):** لوحات Display/Settings/Files/Clipboard
> أصبحت حوارات modal حقيقية: التركيز يدخلها ويظل داخلها، Tab/Shift+Tab يلتفان،
> Escape وزر إغلاق مسمّى يغلقانها، ثم يعود التركيز إلى الزر الذي فتحها. شريط
> الاتصال صار disclosure button بدلالة expanded صحيحة، والتنبيهات وحالة الاتصال
> تُعلن عبر live region. نجحت اختبارات المصدر وTypeScript والبناء الإنتاجي
> وإعادة بناء متطابقة وnpm audit بلا ثغرات وبوابة الأصول و`just check`. شُحنت
> PWA v20؛ اختبار VoiceOver/TalkBack ولمس فعلي على هاتف ما يزال دليلاً مطلوباً.

> **إدخال PIN الكامل (`32de7625`):** شاشة الاتصال لم تكن تقبل لوحة مفاتيح
> فعلية رغم أنها تعرض أزرار HTML. أصبحت أرقام العتاد وBackspace/Delete وEnter
> تمر عبر نفس keypad، ويُعلن عدد الخانات فقط بلا كشف السر. يتعطل كامل keypad
> أثناء الطلب أو lockout بدل أزرار تتحرك ثم تُتجاهل. نجحت TypeScript والبوابة
> وnpm audit والبناء المتطابق و`just check`، وشُحنت PWA v21.

> **Reduced Motion كامل للهاتف (`d78ac6cc`):** كان التفضيل يوقف حركتين فقط
> ويترك 18 حركة أخرى، بينها spinner دائم وsheets وtoast وtoolbar وkeypad. سياسة
> واحدة توقف الآن كل animation وtransition وعناصر pseudo بلا حيلة 0.01ms، مع
> بقاء تغييرات الحالة فورية. نجحت البوابة وTypeScript وnpm audit والبناء
> المتطابق و`just check`، وشُحنت PWA v22. يلزم إثبات مرئي من هاتف حقيقي.

> **تأكيد الطاقة الموحد (`34a1f8b0`):** أُزيل `window.confirm()` الرخيص من
> Sign out/Restart/Shutdown واستُبدل بـMoOS `alertdialog` داخل عقد focus نفسه،
> يبدأ على Cancel ويشرح فقدان العمل. التنفيذ single-flight ذري يمنع النقر
> المزدوج، وبعد الإرسال يصبح الحوار Working وغير قابل للإغلاق لأن الإغلاق لا
> يلغي أمراً وصل للخادم. Cloud يبقى بلا هذه العمليات. نجحت البوابة وTypeScript
> وnpm audit والبناء المتطابق و`just check`، وشُحنت PWA v23.

> **استرداد انتقال المصادقة (`b8ff5480`):** كان انقطاع الشبكة أثناء Login أو
> Setup يترك `busy=true` ويجمّد keypad، وفشل status بعد إصدار التوكن ينتج Promise
> غير معالج. صار المساران catch/finally، ويحفظ App التوكن ويعرض Loading/Error
> قابلاً لإعادة المحاولة بلا إعادة PIN. تُرفض استجابة status غير 2xx فعلياً؛
> أثبت اختبار Node سلوك 503. نجحت TypeScript وnpm audit والبناء المتطابق و`just
> check`، وشُحنت PWA v24. يبقى قطع شبكة حي أثناء handoff دليلاً مطلوباً.

> **حدود خدمات Mo AI (`1cf194b3`, `017df8a6`):** أُغلق restart loop غير محدود
> في OpenClaw (~386MB + preflight ثقيل)، وعقل Ollama وSpeaches (~1.5GB). صار
> OpenClaw يسمح 8 محاولات/5 دقائق، والـQuadlets خمساً/5 دقائق، ثم يظهر failed
> بدل استهلاك CPU/RAM للأبد. حُدّد teardown لـOpenClaw/Ollama بـ30 ثانية كي لا
> يعلقا logout/reboot 90 ثانية. بقي reload وwake-on-demand وAutoUpdate كما هي،
> وأضيف gate إلى CI ونجح `just check`. القياس الحي بعد compose ما يزال مطلوباً.
> تدقيق لاحق وسّع الحد إلى الخدمات الصغيرة: control ‏6/120s، gateway ‏12/120s،
> wake وMo PC Remote ‏5/300s، مع TimeoutStopSec بين 10 و15 ثانية لكل من control/
> gateway/wake/agent-api/Remote/Cloud-audio. كما أُصلح محلل gate كان يلتقط اسم
> `[Service]` من تعليق بدل القسم النافذ. اختبارات Mo AI والفحص الكامل نجحا؛ مضيف
> Cloud خارج الصورة لا يملك systemd-analyze، لذلك قياس الإيقاف الحي ليس مُدّعى.

> **حالة الإصدار الحي:** اندمج Mo AI الجديد على `main` في `77707fd1`، ونجح
> تشغيل GitHub Actions `30704582346` للنسخ العامة والسحابية وNVIDIA: build +
> push + cosign + تحقق بالمفتاح المفروض في MoOS. الجهاز أقلع فعلياً من NVIDIA
> digest `c73d9002…` مع kernel `7.1.5-201`. ربط المالك WhatsApp الحقيقي، وأثبت
> `openclaw status --deep` أن Telegram `OK` وWhatsApp `LINKED` على نفس Gateway.
> كشف الربط أن API الواجهة كان يقرأ `probe.ok` الخاص بTelegram فقط، فكان يعرض
> WhatsApp disconnected رغم اتصاله؛ أُصلح محلل OpenClaw 2026.7 وأثبت backend
> المصدر الحي القناتين `connected:true`. ثُبتت أيضاً allowlist صريحة لإضافات
> OpenClaw الموثوقة عند وجود WhatsApp. `just check` أخضر. يلزم نشر هذا التصحيح
> الصغير الموقّع، ثم إثبات رسالة WhatsApp inbound وفحص post-update نظيف بعد
> بدء جلسة جديدة؛ الفحص الأول كان 46/3 بسبب ملف keyboard ونسختي MoPlayer قديمة
> في `$HOME`، وقد نُقلت نسختا MoPlayer لنسخة احتياطية وصار الإعداد المكتوب
> `de,us,ara` مطابقاً للصورة.
> كشف تدقيق الاستمرارية أيضاً أن OpenClaw يخرج برمز نجاح عند full reload، لذلك
> `Restart=on-failure` كان يترك القناتين offline، وأن وحدة قديمة تحت `$HOME`
> كانت تحجب وحدة الصورة الموقعة، وأن sleeper كان يستطيع قطع WebSocket الوحيد
> لWhatsApp. أُصلحت الثلاثة: `Restart=always`، ترحيل محافظ للوحدة القديمة إلى
> backup خاص، وإبقاء gateway القنوات حياً عندما WhatsApp مفعّل مع استمرار
> تفريغ أوزان النموذج/VRAM بعد الخمول. Telegram-only يبقى wake-on-demand خفيفاً.

> **إعادة بناء Mo AI Workspace — شريحة الأساس قيد التنفيذ على فرع المنتج.** ثُبّت
> المخطط الدائم في `MO_AI_ARCHITECTURE.md`. المنفّذ والمختبر حتى الآن: إدارة
> محادثات OpenClaw (بحث/تثبيت/تسمية/أرشفة)، سجل مشاريع canonical داخل home،
> مهام مستمرة، Terminal PTY حقيقي داخل التطبيق، استيراد خاص للصور والملفات
> النصية مع drag/drop، واستخراج حقيقي محدود إلى 512 KiB لملفات PDF/DOCX/ODT
> (مع بقاء الصيغ الثنائية الأخرى metadata-only بوضوح)، وpush-to-talk عبر
> PipeWire، وتوجيه Hybrid يحمي المرفقات محلياً ويشرح قراره، وأربع درجات صلاحية
> فعلية. صار إرسال الصور يعتمد capability
> `input=image` المعلنة فعلياً من Ollama/المزوّد بدل تخمين الاسم؛ وأثبت طلب PNG
> حي عبر Mo AI وOpenClaw و`qwen3-vl:4b` رداً حقيقياً خلال 17.6 ثانية.
> كما أثبت صوت إنجليزي منطوق صناعياً مسار Speaches الحقيقي وأعاد جملة مفهومة؛
> صار الافتراضي auto-detect ثنائي اللغة، وأُصلحت حالة failed الكاذبة عند سكون الخدمة.
> أُزيل أيضاً انقسام إعداد Cloud: تقرأ البوابة الآن مزوّد/مفتاح OpenClaw نفسه
> الذي تكتبه Settings، ويبقى المخزن القديم fallback للترقية فقط. أثبتت طلبات
> المصدر Cloud وLocal بعلامتين دقيقتين، وHybrid الخاص اختار `local/privacy`
> والمعقد اختار `cloud/complex-task`، وكلها HTTP 200 عبر OpenClaw.
> فحص المصدر الحي على 4K RTL أثبت خرج الطرفية الحقيقي. أضيف أيضاً تشغيل مهام OpenClaw حقيقي مع إيقاف/
> استكمال/إلغاء، وسجل أدوات ونتائج، وProject Workbench يعرض الملفات وحالة Git
> والفروقات من أوامر ثابتة داخل جذر المشروع مع سجل تدقيق دائم. لم يُنشر هذا
> بعد. ترتبط بطاقات المهام الآن بطابور موافقات التنفيذ الحقيقي في OpenClaw عبر
> Session المهمة المحمي، وتعرض الأمر والمسار والقرارات المسموحة فعلياً فقط.
> أثبت اختبار حي من API المصدر إدراج طلب حقيقي ثم رفضه واختفاءه من الطابور
> وتسجيل القرار مع بصمة الأمر في سجل التدقيق؛ لا يصل توكن Gateway إلى QML.
> يسجّل ledger الآن أيضاً النتيجة النهائية المستقلة لعملية الوكيل (نجاح/فشل/
> إلغاء/timeout) مع exit code وأسماء الأدوات فقط، بلا prompt أو stdout/stderr.
> وأصبح يسجّل كل `toolResult` منفرداً success/error أو `missing-result`، ضمن آخر
> 8MiB و200 حدث فقط، مع اسم الأداة وبصمة call id بلا arguments أو output.
> أصبحت المحادثة الرئيسية تمر فعلياً عبر OpenClaw نفسه مع streaming وصور
> وSession ثابتة؛ أثبت اختبار محلي من جولتين أن ذاكرة سطح المكتب هي ذاكرة
> OpenClaw، وأضاف `moai/hybrid` نفس التوجيه الذكي لقنوات الهاتف. أضيف أيضاً
> سجل محادثات قابل للبحث داخل لوحة الشات نفسها؛ فتح جلسة OpenClaw
> الحقيقية وعرض رسائلها الأربع ثم تابع الذاكرة نفسها عبر المفتاح المحمي وأعاد
> الرمز `MOAI-LOCAL-UNIFIED-READY` من مسار `X-MoAI-Agent: openclaw`. أصبحت ردود
> Markdown قابلة للتحديد والنسخ بزر واحد، وتظهر استدعاءات ونتائج أدوات OpenClaw
> كبطاقات حالة حقيقية محدودة الحجم؛ أثبت فحص 4K RTL أمر `exec` ونتيجته الفعلية.
> أضيف إيقاف streaming الحقيقي وإعادة توليد آخر رد مع إعادة payload الأصلي نفسه
> (بما فيه الصور والوثائق) من دون مضاعفة الرسالة في الذاكرة.
> أُعيد تنظيم الإعدادات إلى 12 صفحة فعلية منفصلة: Models وProviders وOpenClaw
> وTelegram وWhatsApp وVoice وPermissions وMemory وProjects وTerminal وPrivacy
> وAppearance. صار Hybrid خيار خصوصية مباشر، وبقيت الأسرار write-only، وحُذفت
> صفحة Health القديمة المخفية والمكررة. أثبت الفحص الحي شبكة RTL وحالة OpenClaw.
> صار شريط Workspace متجاوباً فعلياً: يبقى 76px عند 720×540، ويتوسع إلى 188px
> بتسميات أفقية مقروءة من 1120px؛ فُحصت لقطتا RTL حقيقيتان للحالتين بلا قص.
> أُغلق أيضاً فحص الوصول حياً عبر AT-SPI: أصلحت ستة حقول/مفاتيح كانت تُعلن بلا
> اسم، وأصبح العدد صفراً مع `labelledBy` للحقول السرية؛ واجتاز Tab فعلياً
> إجراءات الشات والمحرر والإعدادات بأسماء صحيحة، وتبقى الحركة خاضعة لبوابة
> reduced-motion الحقيقية.
> أضيف مفتاح مراجعة مصدر آمن `--layout-direction ltr|rtl`. أُغلقت المصفوفة
> البصرية فعلياً على جلسة Wayland الحية: Conversations وProjects وTasks
> وTerminal، في Light/Dark × LTR/RTL، عند `720×540` و`1120×760` ونافذة 4K؛
> 48 لقطة حقيقية عُرضت في ثلاث contact sheets. استُخدم backend المصدر على منفذ
> معزول وظهرت بيانات المشروع والجلسات والمهمة الحقيقية بلا Mock، ثم أُعيد
> `org.moos.ui2.study.light` و`MoOSUI2ScholarLight` حرفياً.
> تعرض صفحتا القنوات الآن probe حقيقياً محدوداً بلا أسرار بدل حالة افتراضية:
> أثبت backend المصدر اتصال Telegram الفعلي بـ `@Moalfarras_bot` عبر polling،
> وأظهر WhatsApp كغير مربوط مع شرح أن زر الربط سيفتح QR. لا يزال مسح QR من
> حساب المالك مطلوباً، لذلك WhatsApp غير محسوب كمنجز. نجح `just check` كاملاً
> بعد هذا التغيير.
> أُثبت Telegram الآن end-to-end: allowlist محصور بالمالك `1142563280`، والجلسة
> الحقيقية تحمل رسائل inbound منه ومرآتي تسليم `telegram-final` إلى نفس chat،
> ونجح probe المصدر من Gateway متوقف ثم أعاد `@Moalfarras_bot connected:true`.
> أصلح الاختبار سباق startup بإنتظار منفذ loopback الفعلي حتى 12 ثانية.
> أُثبت أيضاً مسار الصوت العربي: WAV مدته 11.96 ثانية مرّ عبر
> `moai-transcribe` وSpeaches الحقيقي وأعاد نصاً عربياً مفهوماً برمز خروج صفر،
> ثم عادت الخدمة إلى السكون النظيف. المتبقي خارجياً هو ربط حساب WhatsApp عبر QR.

> تحديث 2026-08-01 — فشل نشر الصور العامة وNVIDIA والسحابية بعد اكتمال البناء
> والرفع، لأن خطوة Syft/SBOM ثقيلة أُعيدت بالخطأ إلى jobs النشر وقتلت runners
> الثلاثة قبل اكتمال التوقيع. أُزيلت الخطوة من workflow الحرج مع إبقاء توقيع
> digest والتحقق بالمفتاح العام الإلزامي، وأضيف
> `tests/test_release_workflow_safety.py` لمنع رجوعها. يبقى إغلاق بوابات CI ثم
> staging وإعادة الإقلاع وفحص ما بعد التحديث مطلوباً قبل وصف الإصدار بأنه مشحون.

> تحديث 2026-07-30 مساءً (**THEME_REV=27**) — الأيقونات المعاد تصميمها كانت
> على `main` لكن الجهاز الحي ما زال يحلّها من `~/.local/share/icons` (بقايا
> معاينة بلون Breeze `#3daee9`). الجولة تُفرّغ تلك التجاوزات مرة واحدة عند
> تطبيق الثيم حتى تفوز علامات `/usr` المخبوزة لكل palette. الجهاز الحالي
> `44.20260730.486`؛ بعد CI + تحديث + إعادة إقلاع تصل THEME_REV=27.
>
> تحديث 2026-07-30 (جولة **أيقونات التطبيقات**) — أُعيد تصميم أيقونات تطبيقات
> MoOS التسعة من الصفر في `artwork/generate_moos_app_icons.py`: مربّع دائري
> واحد بمقاس 880 من لوحة 1024، والرمز داخل منطقة آمنة 640، وكل حدّ حامل ≥ 76
> وحدة ليصمد في خلية 16px. **كل لون فيها دور KDE (`ColorScheme-*`) لا قيمة
> ثابتة.** ولأن MoOS يثبّت `FollowsColorScheme=false` (بعد إثبات أن `true`
> يقرأ QPalette التطبيق فيرسم رموزاً داكنة على الداكن)، فتَتبُّع الثيم يحدث
> **بالخَبْز لا بوقت التشغيل**: نسخة مُعاد تلوينها من الأيقونات التسعة تُكتب
> داخل كل واحد من ثيمات الأيقونات الـ14 (`moos/apps/scalable` مُعلَن الآن في
> `Directories=` لكل طبقة)، و`recolor_moos_app_dir` في `build.sh` تفعل الشيء
> نفسه للقاعدتين العريضتين. أدنى تباين مقيس على الـ16 palette هو **4.4:1**
> و`tests/test_moos_app_icons.py` يعيد حسابه ويفشل تحت 4:1. الإثبات الحقيقي
> عبر `kiconfinder6` في `tests/test_moos_symbolic_runtime.py`: لكل palette
> يجيب KDE بملف تلك الطبقة نفسها ولونه المميّز = `Colors:Selection` لها.
> وأُصلحت ثلاثة أعطال وجدتها الجولة قائمة أصلاً: أيقونة Mo AI على `main` كانت
> قد **فقدت الصورة الأصلية المضمّنة** وبقيت لوحة بألوان Breeze، وأصول
> الأيقونات كانت منقولة إلى `scalable/places` (وPlasma يقرأ `apps`)، وصور
> `moos-logo.png` الأربع كانت محذوفة. الدليل المرئي:
> `artwork/moos-ui2/previews/moos-app-icons-palettes.png`.

> تحديث 2026-07-30 (جلسة لاحقة) — **Tidal Horizon / THEME_REV=25 شُحنت
> وأقلع الجهاز عليها**: النسخة الموقعة `moos-nvidia 44.20260730.478`
> (بصمة `924414e04dfb…`) هي المُقلَع منها الآن مع بقاء `.477` كـrollback —
> تحقق حي عبر `rpm-ostree status`. نسخة `post-update-check.sh` المشحونة
> أعادت **46/2** والفشلان هما بقايا معاينة شجرة العمل (تجاوز أيقونات
> وplasmoid منزليان) لا عيوب صورة. الجولة التالية — **جسر الأيقونات /
> THEME_REV=26** — منفذة في شجرة العمل: طبقة أيقونات رمزية لكل palette
> (14 ثيماً، `FollowsColorScheme=false` بعد إثبات أن `true` يرسم رموزاً
> داكنة على الداكن عبر QPalette التطبيق)، وCommand Canvas أهدأ بأربعة
> أعمدة على سلم أنواع 11px+، وحالات أزرار دلالية 44px. كل بوابات المستودع
> خضراء والأدلة الحية مفهرسة في `artwork/moos-ui2/live-tests/README.md`.
> بوابات النشر (build محلي، commit/push، CI موقّع، staging، reboot،
> post-update-check الأصرم) ما زالت مفتوحة لهذه الجولة.
>
> النص التالي سجل الجولة السابقة كما كان قبل شحنها:
>
> **Tidal Horizon / THEME_REV=25 قيد بوابة الإصدار** على
> `product/tidal-horizon-2026-07-30`: لم تبدأ دورة Audit جديدة؛ تحولت النتائج
> المقبولة مباشرةً إلى لغة منتج واحدة تشمل الخلفية، Command Canvas، Hero Clock،
> Splash/Login/Lock/Logout، Store، Mo AI ومركز تحكم MoOS الأصلي. هندسة الـHorizon
> واحدة عبر كل الأسطح، والحركة قصيرة ومحدودة وتحترم animations-off، وهوية شعاري
> MoOS وMo AI لم تتغير. أدلة before/after من المصدر الحي مفهرسة في
> `artwork/moos-ui2/live-tests/README.md`. هذه اللقطات ليست دليلاً على الشحن:
> بوابات المستودع و`just build` المحلي اكتملتا، وخرجت الصورة العامة
> `13ce8105e9fd…` / OSTree `a568b31c85c4…`. ما زالت بوابات commit/push،
> CI الموقّع، staging، reboot وpost-update-check مفتوحة.
>
> خط الأساس التجاري السابق صار الآن **مشحوناً ومُثبتاً بعد الإقلاع** على
> `moos-nvidia 44.20260730.477` بالبصمة الموقعة `87ed100fd228…`؛ أعاد
> `tests/post-update-check.sh` في 2026-07-30 **48 ناجحة / 0 فاشلة** ولا توجد
> وحدات system أو user فاشلة. لا يُنسب هذا الدليل إلى Tidal Horizon قبل
> نشره وإعادة الإقلاع عليه.

> تحديث 2026-07-30 — **سجلّ نقطة قبول المصدر للنظام البصري التجاري** على الفرع
> `audit/commercial-visual-polish-2026-07-30`: أُنجز تدقيق Visual & UX تجاري
> بقياسات التباين والحركة وRTL ولوحة المفاتيح والوصول والأداء، وتفاصيله في
> `artwork/MOOS_VISUAL_POLISH_AUDIT_2026-07-30.md`. أُغلقت موانع المصدر:
> طبقة QML مشتركة؛ Launcher بثلاثة أعمدة؛ خط نظام وأهداف وصول للـDock؛ حركة
> Splash وMo AI مبسطة؛ لغة نشطة واحدة؛ عائلة **Tidal Cut** أصلية من 69 رمزاً
> مع اختبارات KDE/GTK/librsvg؛ وMoPlayer بكروم MoOS أصلي، analyze نظيف و
> **176/176** اختباراً. MoPlayer منشور في مستودعه عند `23799ad` ومنسوخ إلى
> `moplayer/` من تلك المراجعة النظيفة. `just check` النهائي لمستودع الصورة
> أخضر، وكذلك `just build` العام الكامل: اجتازت الصورة اختبارات QML وLauncher
> ومشهد سطح المكتب والهوية والمتجر وinitramfs/Plymouth وbootc، وخرجت
> `localhost/moos:latest` عند `5e64dbf3373a…`. بوابات النشر التي كانت مفتوحة
> عند كتابة هذا السجل أُغلقت لاحقاً على الإصدار الموقّع `.477` كما يثبت التحديث
> أعلاه؛ وهو الآن خط الأساس الذي يبني فوقه Tidal Horizon.

> تحديث 2026-07-25: جرى تدقيق المستودع والجلسة الحية 4K/225% وعائلة MoOS
> كاملة (16 Global Themes). فرع `moos-ui-unify` يطوّر الشكل الفعلي لا الأسماء:
> مجموعة Plasma SVG كاملة للأسطح عالية الظهور، Aurorae كامل الحالات، خلفيات
> Graphite/Tidal جديدة بلا تمديد، Blur موحد آمن، RTL وGSettings/GTK/Portal،
> وتسعة رموز تطبيقات أصلية. التفاصيل والأدلة والبوابات المتبقية في
> `artwork/MOOS_VISUAL_AUDIT_2026-07-25.md`. نجح البناء المحلي للصورتين العامة
> وNVIDIA على الكيرنل نفسه، وبقي النشر الموقّع وإعادة الإقلاع بنسخة CI بوابتين
> مفتوحتين، لذلك لا يوصف التحديث بأنه مشحون بعد.

> تحديث 2026-07-16: عائلة الثيمات UI2 (٦ ثيمات دارك/لايت) منشورة ومفعّلة؛ إصلاح
> "متجر واحد فقط" (`moos-one-store`) مع بواباته؛ `fwupd-refresh` يكتمل بنجاح؛
> الإقلاع 4.8 ث userspace مع سبلاش MoOS مُثبَت بالصور. المتبقي الحرج قبل وصف
> الإصدار بالكامل: تجربة المثبّت end-to-end على ISO حقيقية (خطوة المنطقة الزمنية).

> تحديث 2026-07-24: شُحنت ترقية Mo AI الليلية بعد مراجعتها والتحقق منها (`just check`
> أخضر، رندر حي لكل سطح مُعدّل، بناء `podman` محلي للنسخة العامة): محرك نطق عربي محلي
> (speaches) بديلاً عن `moai-speech`، بوابة OpenClaw دائمة، وواجهتا HTTP على loopback
> محصّنتان ضد CSRF (رفض `Origin` الأجنبي قبل أي كتابة إعداد أو تنفيذ وكيل، كتابة إعداد
> ذرّية، قائمة سماح تيليجرام). دليل الأجهزة في Welcome صار يفتح وحدات KCM حقيقية
> (بلوتوث/USB/لوحة مفاتيح/فأرة) ببوابة تتحقق من تثبيتها. NumLock مفعّل عند تسجيل الدخول
> كتفضيل بدء لمرة واحدة مُرتّب قبل KWin. نوافذ منبثقة زجاجية "Tidal Glass" وإعادة توليد
> عائلة UI2 كاملة. التحقق الحي بعد إعادة الإقلاع يبقى الخطوة الأخيرة.

هذه الوثيقة هي مصدر الحقيقة لحالة التنفيذ. `README.md` يشرح البناء والتثبيت،
أما هذه الخارطة فتحدد ما اكتمل وما يمنع وصف MoOS بأنه إصدار كامل.

> تحديث شجرة العمل 2026-08-01: أُزيل انحدار غير منشور كان قد استبدل Tidal Cut
> في شاشات Splash/Login/Lock/Logout بظلّ كامل الشاشة عالي الكلفة، وأُعيدت بوابات
> الانحدار لتفشل عند رجوعه بدلاً من طلبه. نجحت وصفة `just check` كاملة بتنفيذ
> أوامرها مباشرة في بيئة Cloud المصغرة، وأُصلح استخراج DOCX/ODT في Mo AI عندما
> لا توجد قاعدة MIME للنظام. نجحت بوابات Mo PC Remote المتاحة، لكن بناء React/
> TypeScript و.NET، بناء صورة Cloud، الفحص الحي واتصال هاتف حقيقي ما زالت مفتوحة
> لأن هذه الجلسة لا تحتوي Node/.NET/systemd/Qt/KDE ولا شاشة؛ لا تُنسب إليها أدلة
> حية أو شحن موقّع.

> تكملة الجولة: بُني Controller الهاتف فعلياً على Node LTS موثّق SHA256، ورُقّي
> إلى React 19/Vite 8/TypeScript 7/PWA 1.3. نجحت الاختبارات وtypecheck وبناءان
> متطابقان و`npm audit` بلا ثغرات، وصار CI يفرضها. أُصلح WebSocket شبح كان يعيد
> الاتصال بعد الخروج، وحُدّ إنهاء encoder الصوت بعد TERM/KILL، وجُعل ختم نشاط
> Mo AI ذرياً تحت التزامن. أزيل تقرير تشخيصي مكرر سرّب رقم هاتف وسياسة allow-all
> وأضيفت بوابة خصوصية. وصفة بوابات المستودع الكاملة خضراء؛ بناء صورة Cloud
> والفحص المثبت واتصال هاتف حقيقي ما زالت بوابات إصدار مفتوحة.

> دليل خادم Remote أُضيف فعلياً: SDK ‏.NET 10.0.302 نفّذ 21 اختباراً ونشر
> Linux agent ذاتي الاحتواء دون warnings، ثم شُغّل binary على منفذ loopback
> معزول ونفّذ setup/logout/رفض PIN خاطئ/login وتقديم PWA، وكتب config بصلاحية
> 0600. أُضيف `MOREMOTE_DATA_DIR` مطلق فقط مع mutex مستقل للخدمات المعزولة.
> فحص NuGet بلا ثغرات؛ ImageSharp 4 اختُبر ورفض البناء دون ترخيص تجاري، لذلك
> ثُبّت خط 3.1.11 القابل للشحن مع gate ترخيص. بقيت تجربة KDE Portal والفيديو
> والإدخال من هاتف حقيقي، وبناء الصورة والخدمة المثبتة، خارج هذا الدليل.

## الرؤية

MoOS نظام bootc ذري مبني تقنياً على Fedora Atomic وPlasma، لكن كل سطح مرئي
للمستخدم يجب أن يحمل هوية MoOS فقط، بلغة التصميم الرسمية
**MoOS UI — Liquid Glass Design System**. تبقى أسماء التقنيات في المعلومات
الترخيصية والتشخيصية والأسماء الوظيفية غير المرئية عندما يتطلبها التوافق.

Mo AI مساعد محلي أولاً. لا ينفذ نصاً عشوائياً بصلاحيات مرتفعة؛ إجراءات النظام
تمر عبر قائمة أوامر واضحة في `moai-do`، ومطالبات Polkit محدودة عند الحاجة.

## الحالة المنفذة

- صورة bootc رئيسية وصورة NVIDIA مبنيتان يومياً وموقعتان في GitHub Actions.
- هوية `os-release` كاملة: `ID=moos` و`VARIANT=MoOS`/`VARIANT_ID=moos` وشعار `moos-logo`.
- مسار تثبيت Live ISO ومسار qcow2 واختبارات initramfs/ostree.
- Global Themes بألوان داكنة وفاتحة وPlasma Style وتخطيط لوحة وخلفيات 4K
  (بدأت بجيل Nova القديم؛ الشحنة الحالية هي عائلة MoOS UI على محرك UI2).
- **MoOS UI2 Graphite/Tidal** في شجرة الإصدار: زوج دارك/لايت كامل ومستقل، خلفيتان
  4K، باران متطابقان هندسياً ومختلفان لونياً، وDashboard زجاجي جديد. اختُبر الزوج
  فعلياً على جلسة Plasma المثبتة وصوره في `artwork/moos-ui2/live-tests/`. يبقى UI1
  مثبتاً مع رجوع صريح. نشر الصورة الموقعة وإعادة الإقلاع هما بوابة الإصدار المتبقية.
- SDDM وPlymouth وGRUB وAnaconda وCockpit بعلامة MoOS.
- استبدال أسماء وأيقونات Fedora/Anaconda التوافقية بمحتوى MoOS.
- تطبيقات Welcome وHardware وCompatibility وUpdate وRecovery وMo AI.
- Mo AI عبر RamaLama، مع خدمة مستخدم اختيارية وموافقة تنزيل لمرة واحدة.
- Flathub وBazaar ومجموعة تطبيقات أولية، وWaydroid وأدوات الألعاب والحاويات.
- خطة أجهزة موحدة `moos-device-plan` تكشف GPU والتعريف وKVM والذاكرة وSecure
  Boot، وتقترح فقط التطبيقات والإجراءات المناسبة للجهاز الحقيقي.
- كشف فجوات التعريفات والبرامج الثابتة: أجهزة PCI بلا تعريف نواة (مُصفّاة على
  الفئات التي يعني فيها ذلك عطلاً فعلياً — جسر المضيف بلا تعريف بالتصميم ولا
  يجوز أن يرفع إنذاراً كاذباً)، وبرامج ثابتة فشلت النواة في تحميلها، وتحديثات
  fwupd المعلّقة، موصولة بإجراء `moai-do update-firmware` عبر Polkit.
- إعداد ذكي بكبسة واحدة يثبت تطبيقات Flathub الناقصة فقط، ومركزا Hardware و
  Compatibility يمرران الإجراءات عبر قائمة آمنة وموافقة Polkit بدل نسخ أوامر.
- Mo PC Remote مدمج في الصورة بواجهة أصلية وخدمة مستخدم اختيارية ومدخل uinput
  خاص بالمستخدم النشط، مع اختبارات للـ backend والواجهة والإدخال Unicode.
- التقاط الشاشة عبر PipeWire من جلسة بورتال واحدة (RemoteDesktop + ScreenCast)
  تحمل الفيديو والإدخال معاً. القياس الفعلي: ‏20–30 إطاراً/ثانية مقابل ~1 سابقاً
  (كان يُشغّل `spectacle` لكل إطار بكلفة 630 مللي ثانية)، وزمن ذهاب وإياب للإدخال
  ‏1.2 مللي ثانية. المؤشر مُخفى من البث ويرسمه الهاتف — رسمه داخل الفيديو كان يُجبر
  إعادة ترميز إطار كامل مع كل حركة (‏+8.5 ميجابت/ث لمجرد تحريك الماوس).
  التموضع مطلق فتقع النقرة تحت الإصبع تماماً، واللمس فوري بلا تأخير 280 مللي ثانية،
  والكتابة بالـ keysym مستقلة عن تخطيط لوحة المفاتيح وتدعم العربية.
- حارس بناء لهوية MoOS واختبار تحميل فعلي لكل تطبيق QML.

## بوابات الإصدار الكامل

لا يعتبر الإصدار كاملاً قبل نجاح جميع البوابات التالية على جهاز حقيقي وعلى VM:

1. **البناء والأمان**
   - [x] نجاح صورتي `moos` و`moos-nvidia` وISO وqcow2.
   - [x] **تحقق cosign مفروض فعلياً** عند التثبيت وعند كل تحديث. السياسة صارت
     `sigstoreSigned` بمفتاح `/etc/pki/containers/moos.pub`، والمثبّت لم يعد يمرر
     `--no-signature-verification` — فالمرجع المنشور موقّع، والجهاز يبقى يتحقق مدى الحياة.
     المحاولة السابقة (v20) كسرت التثبيت لأن `use-sigstore-attachments` كان ناقصاً؛ cosign
     يحفظ التوقيع كـ sigstore attachment فبدونه يقرأ المتحقق كل صورة كأنها غير موقّعة.
     مُختبَر مقابل السجل الحقيقي: مفتاح خاطئ يُرفض، والصحيح يُقبل.
   - [x] **الانتقال بين الإصدارات يبقى موقّعاً** (2026-07-16). ادّعاء «يتحقق مدى الحياة»
     أعلاه كان مكسوراً عملياً: `bootc upgrade` يحافظ على الأصل، لكن `bootc switch`
     **يستبدله**، وبدون `--enforce-container-sigpolicy` يكتب
     `ostree-unverified-registry:` — فـ `moai-do install-nvidia` (الطريق الوحيد بين
     `moos` و`moos-nvidia`) كان يحوّل جهازاً يفرض التوقيع إلى جهاز لا يتحقق من شيء،
     **ولبقية عمر التثبيت** لأن الأصل يبقى. حدث فعلاً على جهاز المالك (السجل،
     08:05:11). البوابات كانت خضراء لأنها تفحص الـ kickstart وقت التثبيت، ولا شيء كان
     يفحص الـ switch الذي ينفّذه النظام العامل. أُصلح في `moai-do` ومحروس في
     `tests/verify_user_experience.py`. التفاصيل في `docs/FIXES_2026-07-16b.md`.
   - اختبار rollback بعد تحديث ناجح وتحديث فاشل محاكى.
   - [x] **مُثبَت حياً 2026-07-21** (تحديث لبند "مفتوح 2026-07-16"): عاد الفشل على
     إقلاع 2026-07-21، وهذه المرة أظهر journal السبب الصريح `Failed to obtain auth` — وهو توقيع
     polkit لا لبس فيه. الجذر: الخدمة تعمل بـ DynamicUser بلا جلسة، فيُقيَّم أكشن
     `org.freedesktop.fwupd.refresh-remote` (الافتراضي `allow_any=auth_admin`) بلا وكيل يوثّق.
     الإصلاح: قاعدة polkit محصورة `system_files/usr/share/polkit-1/rules.d/60-moos-fwupd-refresh.rules`
     (commit 448f926) تمنح `refresh-remote`/`get-remotes` لمستخدم `fwupd-refresh` فقط، دون أي مساس
     بأكشنات فلاش/تخفيض/تعديل الـ firmware (تبقى `auth_admin`). **التحقق الحي على الصورة الموقّعة
     be91759a (الإصدار 44.20260721.281):** تشغيل يدوي لـ `fwupd-refresh.service` خرج
     `0/SUCCESS`، نزّل ميتاداتا LVFS بالكامل ("نُزّلت معلومات وصفيّة جديدة بنجاح") **بلا أي
     `Failed to obtain auth`**، و`systemctl --failed` فارغة قبله وبعده، وmoos-selfcheck 39/‏لا فشل.
     السجل التاريخي أدناه:
     `fwupd-refresh.service` كان يفشل في كل
     تشغيل يحتاج تنزيلاً فعلياً (يخرج 1 بعد `Updating lvfs` مباشرة). فرضيتا polkit
     وصندوق العزل **مرفوضتان بتجربة** (نسخة طبق الأصل session-less من الوحدة نجحت)،
     والسيناريو الفاشل نفسه صار يمر: تنزيل stale نجح من الوحدة ذاتها (12:03) ومن العميل
     قسرياً (19:40)، وكل التشغيلات التسعة بعدها خضراء. السبب الأصلي غير مفسَّر — يبقى
     البند مفتوحاً حتى يتكرر ويُقرأ (drop-in الـ journal مشحون لهذا الغرض حصراً؛ فشل
     الوحدة يظهر في `moos-selfcheck` كوحدة فاشلة). انظر `docs/FIXES_2026-07-16b.md`.
   - [x] **`drkonqi-coredump-pickup.service` — كُتِمت (2026-07-16)**. كانت تفشل بعد 30
     دقيقة من **كل جلسة**. تتبُّع بـ gdb أثبت أنها **لا يمكن أن تنجح أصلاً**:
     `--settle-first` مجرد `nanosleep` لـ 60 ثانية (وليس 30 دقيقة)، وبعده يدخل `main()`
     في `QCoreApplication::exec()` ولا يعود أبداً — حتى مع **صفر** coredumps (مُختبَر
     بـ `--boot-id` لإقلاع بلا انهيارات). حلقة أحداث صمّمها الأصل كأنها مهمة محدودة
     بالوقت: يقتلها systemd عند `RuntimeMaxSec` فتُسجَّل `timeout` = فشل، بلا أي مسار
     خروج ناجح. و`SuccessExitStatus=` لا ينفع (يطابق أكواد الخروج والإشارات، لا نتيجة
     `timeout`). المفقود بالكتم: التقاط الانهيارات التي حدثت *قبل* تسجيل الدخول فقط.
     **الانهيارات الحية سليمة**: تمر عبر `drkonqi-coredump-launcher.socket` الذي يسحبه
     `sockets.target` لا الـ pickup — تُحقّق من ذلك بشجرة الاعتماديات العكسية قبل
     الكتم، لأن `Requires=` في الوحدة يوحي بالعكس. انظر `docs/FIXES_2026-07-16b.md`.

2. **الهوية المرئية**
   - [x] **جولة QEMU على ISO 179 + الهدف المثبَّت (2026-07-16)**: صُوِّر الإقلاع (نص +
     شعار MoOS)، وWelcome بالعربية **و**الإنجليزية، و100%/150%/200%، وAbout (MoOS 44،
     لا Fedora)، والمثبّت كامل (٨ صفحات مع المنطقة الزمنية وhold-to-commit)، وصفحة النجاح،
     وأول-إقلاع Welcome، وشاشة القفل. الأدلة في `~/Desktop/MoOS-ISO/proof/gate2-2026-07-16/`.
   - [x] **عطلان حقيقيان كشفتهما الجولة وأُصلحا** (لم يرهما أي gate من قبل): ساعة شاشة
     القفل كانت ترسم الوقت الطويل `…European Summer Time:10:48 PM Central…` بعرض الشاشة
     (`Qt.formatTime` بوسيط locale لا يوجد له overload — أُصلح في `55fef8b` مع بوابة)،
     وتوست أحمر `Launching plasma-welcome (Failed)` على أول إقلاع (الحذف نفسه كان السبب —
     بديل صامت في `919711e` مع بوابة). كلاهما محقَّق في الصورة المجهّزة `183` على القرص.
   - لا يظهر اسم أو شعار توزيعة أخرى في أي لقطة مستخدم — محروس بثلاث بوابات هوية.
   - [x] **جولة QEMU على ISO 190 (2026-07-17)**: نفس مسار جولة 179 وكلها خضراء على الـ ISO
     الجديد — السبلاش بالعلامة وحلقة المذنّب، الكيبورد الألماني في الجلسة الحية (z,z,y,y ← "yyzz")،
     المثبّت ٩ صفحات مع بحث المنطقة الزمنية وhold-to-commit، شريط التقدم **يتحرك** (9%←86%←نجاح)،
     والهدف أقلع من القرص بأول إقلاع: autologin، ترحيب أول إقلاع، الساعة على فيينا (+2)،
     وشاشة القفل الجديدة (حلقة + HH:mm نظيفة). `moos-selfcheck` على الهدف: **38/39**.
     الأدلة في `~/Desktop/MoOS-ISO/proof/gate2-2026-07-17/`.
   - [x] **أُصلح ومُثبَتت آليته حياً 2026-07-21** (عطل اكتشفته جولة 190، أول إقلاع لتثبيت جديد، QEMU):
     كانت `systemd-zram-setup@zram0.service` و`dev-zram0.swap` تنتهيان فاشلتين وswap مطفأ. السلسلة:
     فشل عابر واحد وقت الإقلاع، وبعده **كل** إعادة محاولة لـ zram-setup تموت بـ EBUSY على كتابة
     `/sys/block/zram0/comp_algorithm` لأن الجهاز مهيأ أصلاً (disksize مضبوط وmkswap تمت) —
     ففشل عابر واحد يبدو دائماً. لا يتكرر على جهاز المالك (zram يعمل، صفر وحدات فاشلة).
     **الإصلاح (commit 2e7b7d0، مشحون في be91759a):** drop-in
     `systemd-zram-setup@.service.d/10-moos-reset-on-retry.conf` بـ `ExecStartPre=-` يعيد ضبط
     `/sys/block/%i/reset` قبل كل (re)start، محروس ثلاثياً (يتخطّى إن كان `/dev/%i` swap نشطاً،
     يلمس عقدة reset فقط إن كانت قابلة للكتابة، والـ`-` يجعل systemd يتجاهل أي فشل عابر ويعيد
     المحاولة)؛ + تسلسل `moos-hardware-adapt` الآمن (stop→daemon-reload→reset-failed→start واحد،
     بلا `systemctl restart`) يمنع start-limit-hit. الاثنان محروسان في `tests/verify_user_experience.py`.
     **التحقق الحي 2026-07-21** (على النظام الحي، عبر حاوية podman privileged تشارك `/sys` مع النواة،
     على جهاز zram1 خدشيّ دون لمس swap الحي zram0): (1) أُعيد إنتاج العطل — جهاز مُهيّأ يرفض
     `comp_algorithm` بـ EBUSY؛ (2) أمر `ExecStartPre` الحرفي أعاد ضبط الجهاز المُعطَّل فصارت
     `comp_algorithm` قابلة للكتابة؛ (3) الحارس آمن — الأمر ضد swap **نشط** لم يُغيّر disksize ولم
     يُخرجه من `/proc/swaps`؛ (4) `systemctl show` يؤكّد أن systemd يحمّل الـ`ExecStartPre` (بـ
     `%i`→`zram0`, `ignore_errors=yes`) في التعريف الفعّال. **المتبقٍّ (الطبقة الذهبية):** إعادة
     إنتاج توقيت سباق أول‑إقلاع كاملاً في VM تثبيت جديد — غير متاح في بيئة التطوير الحالية
     (لا `qemu-system-x86_64` ولا `bootc-image-builder`، و`/usr` للقراءة فقط)؛ يُنجَز في جولة ISO
     القادمة على العتاد. العطل التجميلي المرافق (قفل الشاشة أثناء التثبيت بعد ٥ دقائق) أُصلح أيضاً
     في `moos-live-polish` ومحروس.
   - [ ] **متبقٍ لجهاز حقيقي**: تصوير GRUB وSDDM وشاشة الخروج، ونسب 16:10 و21:9 —
     تحتاج إقلاعاً فعلياً (الإدخال الاصطناعي لا يعمل على جهاز المالك؛ الهدف المثبَّت في
     QEMU يقفل خلف كلمة مرور). خطوة المالك مع ISO على USB.

3. **العتاد**
   - [x] تعريف NVIDIA في صورة NVIDIA، وAMD/Intel في الصورة الرئيسية. الصورتان تُبنيان الآن
     من **نفس القاعدة** (`kinoite-main`) والتعريف يُركَّب فوقها من حاوية akmods الخاصة بـ
     ublue. كانت `moos-nvidia` مبنية على `kinoite-nvidia:44` التي هجرها upstream في أيار،
     فصارت عملياً نظاماً عمره ستة أسابيع: `589 downgraded, 6 removed, 92 added`. بعد الإصلاح:
     **`84 added` فقط، صفر downgraded**. الـ kmod مُثبَّت على نواة الصورة بالضبط، ووجوده داخل
     الـ initramfs محروس ببوابة تقرأ الـ initramfs الفعلي — لأن غيابه هو الشاشة السوداء.
   - الصوت والميكروفون والبلوتوث والواي فاي والنوم والاستيقاظ والشاشات المتعددة.
   - [x] تحديث firmware عبر fwupd وتقرير واضح عند غياب تعريف: `moos-device-plan` يكشف
     أجهزة PCI بلا تعريف نواة (مُصفّاة على الفئات التي يعني فيها ذلك عطلاً حقيقياً — جسر
     المضيف بلا تعريف بالتصميم ولا يجوز أن يرفع إنذاراً كاذباً)، وبرامج ثابتة فشلت النواة في
     تحميلها، وتحديثات fwupd المعلّقة — موصولة بإجراء `moai-do update-firmware`.
     Mo AI يقرأ هذا التقرير ويعرفه من أول رسالة بدل انتظار طلب الفحص.

4. **Mo AI — صار مركز الجهاز كله**
   - [x] **دمج المراكز داخل Mo AI**: مركز الأجهزة والتعريفات، ومركز التوافق، ومركز
     التطبيقات، وMo PC Remote، ووكلاء البرمجة — كلها لوحات داخل Mo AI الآن، لا نوافذ
     منفصلة. `moos-hardware` و`moos-compat` صارا أمرين يفتحان `moai --panel …`،
     وحُذفت `org.moos.hardware.desktop` و`org.moos.compathub.desktop`.
   - [x] **تثبيت أي تطبيق**: بحث Flathub حقيقي داخل Mo AI (`moai-control /search`،
     مع `flatpak search` كبديل بلا إنترنت). التثبيت يمرّ عبر
     `moai-do install <id>` — تأكيد + Polkit + تحقّق من المعرّف مرّتين.
   - [x] **التثبيت يفتح التطبيق (ينزّل ويشغّل)**: `do_install` بعد النجاح يفتح التطبيق
     عبر `launch_app` (Flatpak بالمعرّف أو `gtk-launch` للأصلي)، فطلب «ثبّت كاميرا»
     ينتهي بالكاميرا على الشاشة لا بأيقونة جديدة. ومسار `moos://apps/run/<id>` يفتح أي
     تطبيق مثبّت بلا صلاحيات (نظير `apps/install/<id>`)، مع تحقّق reverse-DNS ورفض غير
     المثبّت — أسوأ ما تفعله صفحة ويب أن تفتح تطبيقاً يملكه المستخدم أصلاً.
   - [x] **رموز البار: اثنان فقط (الصوت واللغة)، والباقي خلف السهم — فعلياً هالمرة**. الإعداد
     كان صحيحاً على القرص (`shownItems=keyboardlayout,volume`) بينما البار يرسم 8 رموز ويبقى هيك
     بعد إعادة التشغيل. السبب: في Plasma 6.7.2 `writeConfig`+`reloadConfig()` يكتب الملف لكن **لا
     يعيد بناء** نموذج الإظهار/الإخفاء في الغلاف الحي، والبوابة على نص `writeConfig` بقيت خضراء
     (فخّ الإعداد المُظلَّل). الحل الوحيد الفعّال: **إعادة تشغيل plasmashell** مرة واحدة لكل
     `THEME_REV` (محروسة بعلامة `moos-tray-collapsed.v${REV}`). رُفِع `THEME_REV` إلى 11. مُختبَر
     end-to-end: كسرت البار، شغّلت apply-theme، فرجع لاثنين. بوابة جديدة تفرض إعادة التشغيل.
   - [x] **الكاميرا تعمل فعلاً**: العطل كان أن أعلى نتيجة Flathub لكلمة «camera» هي
     `io.github.cosmic_utils.camera` — تطبيق مصمّم لسطح COSMIC يُثبَّت بنجاح ثم **ينهار**
     على KDE Plasma (يبحث عن `com.system76.Cosmic*` غير الموجودة ويموت في مُصيّر الفيديو
     wgpu). العتاد وlibcamera سليمان. الحل: Mo AI يوصي بكاميرا مُختبَرة حيّاً، ويُحذّر صراحةً
     من تطبيق COSMIC. بوابة تحرس قائمة التوصيات فلا يعود المعطوب يُعرَض.
     **تصحيح (2026-07-13، بعد إعادة التشغيل):** التوصية الأولى كانت `org.kde.kamoso` ووُسِمت
     «مُختبَرة» لأنها فُتِحت مرة. هي **تنهار (SIGSEGV) بعد 15–45 ثانية** داخل `libgstcamerabin`،
     صامتةً، وليست مشكلة ذاكرة رسومية (تموت مع 6.5 غيغا حرّة، بينما `gst-launch v4l2src` يلتقط
     إطاراً نظيفاً من الكاميرا نفسها). البديل المُثبَّت الآن: **`org.gnome.Snapshot`** — تصل
     للكاميرا عبر بوّابة XDG (التي تُنفّذها KDE)، مُختبَرة حيّة: عارض مباشر وزرّ تصوير. الدرس:
     المعيار ليس «أصلية لـ KDE» بل **«شُغّلت هنا وصمدت»**؛ و«فُتِحت مرة» ليست اختباراً.
   - [x] **Mo PC Remote**: تشغيل/إيقاف/إعادة اتصال من داخل Mo AI (خدمات مستخدم،
     بلا كلمة مرور)، والحالة مقروءة من الجهاز لا مفترضة.
   - [x] **Codex و Claude**: تثبيت في `~/.local` عبر npm (بلا صلاحيات مسؤول إطلاقاً —
     `/usr` للقراءة فقط على OSTree) وتشغيل في طرفية حقيقية.
   - [x] العقل المحلي والسحابي: كلاهما يعمل على 8080، والمفتاح في خزنة النظام.
   - [x] **تفريغ العقل من VRAM عند الخمول (إصلاح استقرار حرج)**: `moai.service` يحمّل ~6GB في
     كرت 8GB ولا يفرّغها أبداً وهو شغّال، فيبقى ~280MB فقط للكمبوزيتر — نافذة مكبّرة فوقه
     كفيلة بإنهاء kwin_wayland (`NVRM: VM: invalid mmap context`) وتعليق الشاشة. الحل:
     `moai-idle.timer` يوقف العقل بعد خموله (افتراضي 15 دقيقة عبر `MOAI_IDLE_MINUTES`)،
     والـ gateway يختم النشاط في `$XDG_RUNTIME_DIR/moai-activity` كل رسالة محلية، ويرجّع العقل
     تلقائياً عند أول رسالة (`ensure_local`). مُفعَّل لكل مستخدم افتراضياً. مُختبَر end-to-end
     على العتاد: التفريغ حرّر 5.8GB (من 280MB إلى 6094MB فاضي)، والرسالة التالية أعادت العقل
     وردّ. **يحلّ صنف التعليق الذي رآه المالك.**
   - **لم يكتمل**: تنزيل النموذج وبدء الخدمة بعد الدخول لم يُختبرا على تثبيت نظيف.
   - [x] **رسائل واضحة للمساحة والذاكرة والشبكة وفشل النموذج**: كانت رسالة الفشل **واحدة**
     («أول تشغيل يحمّل النموذج ~2.5GB ويكمل بالخلفية») تُطبع حتى لو امتلأ القرص، أو لا إنترنت،
     أو انهار `llama-server` لأن ذاكرة الكرت نفدت (حدث فعلاً على هذا الجهاز، SIGABRT) — فينتظر
     المستخدم تنزيلاً لا يجري. الآن: **فحص قبلي** يرفض تشغيلاً مستحيلاً (مساحة/شبكة) ويقول الرقم
     بالضبط، و**تشخيص** يقرأ سجلّ الخدمة نفسها عند سقوطها ويسمّي السبب (VRAM / القرص / الشبكة /
     نموذج غير موجود)، ولا يخلط بين خدمة **بطيئة** (`activating`) وخدمة **ميتة**. مُختبَر حياً:
     العقل مطفأ ← سؤال ← أيقظه وردّ خلال 55 ثانية؛ وكل مسار فشل مُختبَر بافتعاله.
   - [x] **اختبارات لكل أمر في `moai-do` ورفض أي إدخال خارج القائمة**: `tests/test_moai_do.py`
     **يشغّل** السكربت (لا يقرأه فقط): كل أمر في التوزيع له تنفيذ ومذكور في المساعدة، وكل مسار
     `do/*` يمرّره `moos-open` موجود فعلاً، **وكل `moai-do X` تذكره واجهة QML أمرٌ حقيقي** —
     وهذا صاد خطأً حياً: نصّ Mo AI كان يعلّم النموذج أن يجيب «كيف أشغّل الألعاب؟» بـ
     `moai-do setup-gaming`، وهو أمر **غير موجود** (كان `moos-setup --gaming` فقط). صار أمراً
     حقيقياً. والرفض مُختبَر بالتنفيذ: 7 مدخلات خبيثة (`rm -rf /`، `$(reboot)`، تجاوز مسار…)
     كلها تُرفض بـ exit 2 دون أن تصل flatpak أو pkexec.
   - [x] **البوابات صارت تمنع فعلاً**: `tests/*` لم تكن تعمل **لا في CI ولا في `just build`** —
     شرف فقط، على مستودع صورته تنزل على جهاز المالك اليومي. الآن `just check` وصلتها
     `just build`/`build-nvidia`، وخطوة **Repo gates** في `.github/workflows/build.yml` تشغّلها
     قبل بناء أي طبقة. بوابة لا تستطيع إسقاط البناء هي تعليق.
     (يوجد الآن فحص ثابت يربط كل زر بمساره وبمنفّذه، لكنه ليس اختبار تنفيذ.)
   - لاحقاً: واجهة أصلية مترجمة بدلاً من مشغل QML.

5. **التطبيقات وتجربة اليوم الأول**
   - [x] **متجر Mo صار مركز تطبيقات حقيقياً، لا جدار بطاقات**. `moos-store-index` يبني فهرساً
     **بقراءة ملفات فقط** (AppStream الخاص بـ Flatpak + الـ refs المنصوبة، بلا شبكة وبلا استدعاء
     `flatpak`): مقيس على الجهاز الحي **3324 تطبيقاً من 9308 مكوّنات في 11.5 ثانية باردة و0.24
     ثانية دافئة**، 3278 منها بأيقونة محلية. البحث والتصفح يعملان بلا إنترنت.
     و`moos-storectl` ينفّذ التثبيت/الإزالة/التشغيل/التحديث عبر **معاملات libflatpak**، فالتقدّم
     من `FlatpakTransactionProgress` و«غير معلوم» يُكتب `null` بدل مؤقّت مُختلَق. جُرّب حيّاً:
     تثبيت Flatseal ثم إزالته، كلاهما `state=success`.
   - [x] **صفحة التحديثات صارت تقول ماذا ينتظر التحديث**. كانت زراً بلا معلومة —
     «حدّث التطبيقات» قفزة في الظلام. الآن `moos-storectl check-updates` (قراءة فقط، بلا
     قفل عام وبلا لمس `job.json`) ينشر `updates.json`، والصفحة تسرد كل تطبيق باسمه
     وإصداره ومصدره مع شارة عدد في الشريط الجانبي. الحالة **ثلاثية**: «غير معروف» ليست
     «لا يوجد» — إظهار صفر قبل الفحص كذبة لا يستطيع المستخدم كشفها، و«تعذّر الفحص» عند
     انقطاع الشبكة لا يُعرَض أبداً كـ«كل شيء محدّث». والمكوّنات المشتركة (runtimes) تُعدّ
     منفصلة عن التطبيقات. مُثبَت بتحديث حقيقي: خُفِّض Flatseal لنسخة أقدم فظهر
     «تطبيق واحد ينتظر التحديث»، ثم رُفِّع فعاد العدّ إلى صفر.
   - [x] **مسار التثبيت خرج من نظام الروابط العام**. `moos:` مخطّط مُسجَّل — أي صفحة ويب تقدر
     تسلّم `moos-open` رابطاً — والمتجر القديم كان يطلق `moos://store/install/<id>`، أي أن تبويب
     متصفّح كان يقدر يبدأ تثبيتاً. المسار **حُذف**، وثلاث بوابات تحرس بقاءه محذوفاً، والبديل جسر
     `StoreBridge` خاص لا يُعرَض إلا لـ `org.moos.store` و`org.moos.welcome` ويشغّل الخلفية
     بـ argv ثابت. ولنفس السبب صار `remote/restart` هو `try-restart` (الـ restart العادي كان
     يشغّل التحكم عن بُعد وهو مطفأ).
   - [x] إكمال ربط تصنيفات Bazaar الموجودة في `moos-curated.yaml` بالواجهة الفعلية وعبر overrides للـ Flatpak.
     (تجاوزَته النقطتان أعلاه: المتجر صار يملك تنظيمه وفهرسته، وBazaar بقي محرّكاً اختيارياً
     يُنصَّب عبر الخلفية الموثوقة، وصلاحية `host-etc` القديمة تُزال عند الترقية.)
   - [x] استبدال خطوات النسخ إلى الطرفية بتدفقات واجهة آمنة (تم تحديث `moos-setup` لواجهة checklist رسومية مدمجة).
   - [x] تكامل مدمج لـ Mo Remote كأداة تحكم أساسية بالخلفية والواجهة.
   - [x] تثبيت ذكي حسب العتاد من شاشة الترحيب ومركز التوافق.
   - [x] زر آمن للانتقال الذري إلى صورة NVIDIA الصحيحة مع rollback.
   - فحص الوصولية ولوحة المفاتيح وRTL وقارئ الشاشة والتباين.

## Tidal Horizon — مرحلة Product Design (THEME_REV=25، working tree)

هذه ليست إعادة تدقيق. نقطة البداية هي الحكم التجاري المقبول أدناه؛ العمل هنا
يحوّل ذلك الحكم إلى شخصية MoOS المرئية الواحدة ويقفل الفرق بين «ثيم متناسق»
و«نظام يمكن تمييزه من أول إطار».

### لغة المنتج

- [x] **توقيع مكاني واحد:** غشاءان منخفضان من mineral glass يلتقيان عند
  **Tidal Cut** مقعّر قصير؛ المساحة العليا هادئة للعمل. العقد العادي:
  `0.11/0.89W` للحافتين، `0.82H` للأفق، `0.12H` للقمة،
  `0.22W` للكتف و`max(11px,0.013W)` لنصف القطع. العقد المضغوط:
  `0.04/0.96W`، `0.78H`، `0.19H` و`0.18W`. الشكل فيزيائي فلا ينعكس
  في RTL؛ المحتوى وحده يتبع start/end.
- [x] **مادتان لا ضجيج زجاجي:** Tidal Light بضوء alabaster/turquoise وGraphite
  Dark بزجاج غرافيتي مدخّن؛ نفس الهندسة والكاميرا والقص. الأسطح الدائمة
  صلبة لثبات القراءة، والزجاج للأسطح العابرة والـhero.
- [x] **إيقاع واحد:** radii ‏`8/12/16/24px`، حواف دقيقة وظل عريض واحد،
  ومساحة سلبية أكبر من الزخرفة. لا white slabs ولا glass-on-glass ولا
  traffic lights مستعارة.
- [x] **حركة مقصودة ومحدودة:** `120ms` للتفاعل، `240ms` للانتقال البنيوي،
  و`320ms` لكشف Horizon داخل التطبيقات. كل المدد تصبح صفراً عند
  `Kirigami.Units.longDuration <= 1`؛ لا Timer سريع أو loop زخرفية خفية.
- [x] **الهوية محفوظة:** هندسة شعاري MoOS وMo AI لم تتغير. عائلة الرموز
  الأصلية Tidal Cut ذات 69 رمزاً تبقى العقد الرمزي؛ لم تُستورد أو تُرقّع
  Breeze/Material/Lucide/Fluent/Papirus.

### التنفيذ حسب السطح

- [x] **الخلفية والعائلة:** ماستران PNG lossless بقياس 1672×941:
  `moos-ui-tidal-horizon-master-v1.png` (`b09a5a71e68d…`) و
  `moos-ui-graphite-horizon-master-v1.png` (`4402f755df0c…`)؛ مولّد العائلة
  يحتفظ بالشكل ويحوّل الضوء عبر 16 palette، وصورة proof جماعية موجودة.
- [x] **Launcher / Command Canvas:** ‏`828×630` logical px، هامش `24px`،
  command bar بارتفاع `68px`، أهداف لا تقل عن `40px`، وثلاث وجهات رئيسية
  فقط: Mo AI وStore وSettings. لا تكرار لنفس الوجهات في footer صاخب؛ Session
  Edge هادئ ومنفصل.
- [x] **Desktop / Hero Clock / Dock:** Hero Clock بالدقيقة فقط، بلا ثانية أو
  حركة دائمة؛ أرقام Latin داخل chrome ثنائي الاتجاه. Dock يحتفظ بمحرك Plasma
  الآمن لكنه يأخذ هوية Command Canvas وإيقاع النظام.
- [x] **Splash / Login / Lock / Logout:** مكوّن Tidal Horizon واحد بهندسة
  byte-identical عبر الأسطح الأساسية وأفراد العائلة؛ reveal واحد محدود،
  progress محدود، ولا loop لا نهائية. Splash ‏460ms + progress ‏260ms،
  Logout background ‏480ms + sheet ‏420ms، Login wallpaper ساكنة، وLock لا
  يحرّك إلا انتقالات محدودة ونبضة حدث الدقيقة. اللغة العربية تستخدم IBM Plex
  Sans Arabic، الوقت يبقى LTR، وتبقى هندسة العلامة كما هي. SHA256 المكوّن
  القانوني: `11a0ddbd40ae…`.
- [x] **Store:** اختفت الحلقة/الكتلة العامة من hero؛ الـHorizon يجلس خلف
  المحتوى ولا يمر فوق النص. عينة idle لمدة 20 ثانية: **0 ticks / 0.000% CPU**.
- [x] **Mo AI:** استبدال طبقات الشفق/التوهج غير المترابطة بالـHorizon المشترك،
  مع إبقاء orb Mo AI نفسه. العلامة المائية الثانوية صارت هادئة ولا تنافس
  المحادثة.
- [x] **Control Center / Settings / Recovery:** مركز MoOS أصلي موحد ثنائي
  اللغة وRTL، أهداف ≥`48px`، Overview حي وRecovery داخل القشرة نفسها،
  status helper للقراءة فقط، قياس التخزين على `/var`، و**34 route ثابتة**
  ومسموح بها بلا wildcard. القياس بعد دقائق: **55.8 MiB current /
  58.8 MiB peak، 0.427s CPU**.
- [x] **Launcher وHero Clock idle:** كل منهما **0 ticks / 0.000% CPU**
  خلال عينة 20 ثانية.
- [x] **MoPlayer:** لا إعادة تصميم اعتباطية في هذه الدفعة؛ يبقى كروم MoOS
  الأصلي وحالة canonical `23799ad` مع analyze و176/176 هما خط الأساس المقبول.

### الدليل وبوابة الإصدار

- [x] فهرس before/after لسطح المكتب والمشغّل وبوابة الخروج ومركز التحكم وStore
  وMo AI وRecovery:
  [`artwork/moos-ui2/live-tests/README.md`](artwork/moos-ui2/live-tests/README.md).
  سطح المكتب والمشغّل مثبتان كـ**live-session** على Plasma 3840×2160؛ بقية
  الملفات **source preview**. لا واحد منهما دليل على صورة موقّعة.
- [x] النسخة السابقة `.477` مثبتة بعد الإقلاع: post-update-check ‏48/0،
  توقيع مفروض، ولا وحدات فاشلة. هذا baseline فقط.
- [x] بوابات Portals المركزة حالياً: `test_tidal_portals.py` ‏6/6،
  `test_moos_ui2.py` ‏22/22، `verify_user_experience.py` ناجحة و`qmllint`
  خرج 0 (تحذيرات سياق host فقط).
- [x] أعيد توليد كل مخرجات UI2 والعائلة من الماسترين النهائيين؛
  `test_tidal_horizon.py` ‏5/5 و`test_tidal_portals.py` ‏6/6، وأضيف كلاهما
  إلى `just check` وCI.
- [x] Repo gates كاملة و`just build` المحلي ناجحان مع `THEME_REV=25`.
  الصورة: `13ce8105e9fd…`، manifest محلي `sha256:28e93f5b8546…`،
  OSTree `a568b31c85c4…`. فحص المحتوى داخل الصورة أثبت `NAME/ID=MoOS`،
  مركز القيادة، الخلفية، وعقد Portal ذي SHA `11a0ddbd…`.
- [x] commit منظم وpush، ثم نجاح مصفوفة CI ونشر صورة `moos-nvidia` موقعة —
  صدرت `44.20260730.478` بالبصمة `924414e04dfb…`.
- [x] التحديث والإقلاع تمّا: الجهاز يقلع من `.478` مع بقاء `.477` rollback
  (تحقق `rpm-ostree status` حي). نسخة post-update-check **المشحونة** أعادت
  ‏46/2 والفشلان بقايا معاينة منزلية لا عيوب صورة؛ لقطات ما بعد التحديث
  مفهرسة (`post-update-desktop-44.20260730.478.jpg` وأخواتها). Tidal
  Horizon الآن **shipped و boot-proven**، وجولة THEME_REV=26 (جسر
  الأيقونات) هي المفتوحة حالياً.

## حكم Visual & UX التجاري (working tree، 2026-07-30)

- [x] منظومة رموز Symbolic واحدة: رُفضت عائلة 67/68 monoline الأولى ولم تُرقع.
  استُبدلت بعائلة **Tidal Cut** أصلية من **69 SVG** بمسارات compound filled
  تحتفظ بالفراغات في GTK mask، ومولّد/manifest/catalogue حتمية واحدة. اختبارات
  KDE KIconLoader وGTK resolver وlibrsvg عند 16/20/24/64/128px تغطي القصّ،
  الفراغات، الحالات وتبدّل ألوان الثيم. شعاري MoOS وMo AI احتفظا بشكلهما؛
  التكيّف اللوني فقط.
- [x] قياس كل علاقات النص/الخلفية عبر **16 مخططاً**: أسوأ on-accent
  **4.516:1**، destructive **4.857:1**، وInstaller semantic tint
  **4.538:1**؛ الحدود غير النصية لا تقل عن 3:1.
- [x] شاشة القفل والخروج وInstaller وWelcome وMo AI: تحسين التركيز المرئي،
  لوحة المفاتيح، الأسماء وأدوار الوصول؛ Splash يملك frame نهائياً كاملاً
  للوضع الساكن.
- [x] إصلاح precedence في ternary الحركة داخل Welcome/Installer وإضافة gate
  يثبت duration=0 عند animations-off ويحافظ على stagger عند التشغيل.
- [x] توحيد QML فعلياً: Store/Mo AI/Welcome/Installer تستهلك Tokens وButton
  وFocusRing وSymbolIcon وSymbolCatalog من طبقة مشتركة صغيرة.
- [x] إعادة تكوين Launcher إلى 3 أعمدة وأهداف أكبر وخط النظام؛ ورفع label
  في Dock إلى الحد الوظيفي 11pt.
- [x] تبسيط Splash إلى reveal واحد + progress واحد، وإزالة loops المحيطة من
  Mo AI لصالح حقل ثابت بلون الثيم. قياس الإقلاع الهادئ باقٍ بعد التحديث.
- [x] جعل Logout وTheme Picker و133 مدخلاً ظاهراً في Mo AI تتبع اللغة النشطة
  وحدها (visible وAccessible.name)، وحصر العرض الثنائي في language chooser أو
  خطأ دعم مقصود قبل معرفة اللغة.
- [x] GTK يتبع ColorScheme الفعلي لكل أعضاء العائلة، مع تحديث حي debounce
  ‏75ms؛ Recovery وMo PC Remote لم يعودا يحجبان main loop.
- [x] RTL: إزالة المرآة المزدوجة من Launcher وساعة البار، مع إبقاء تحكم نافذة
  MoPlayer في اليسار الفيزيائي في المستودع القانوني.
- [x] الاختبارات المركزة و`just check` النهائي لمستودع الصورة خضراء؛ منهج
  الرندر والـnegative fixtures والقيود موثقة في تقرير التدقيق.
- [x] إعادة تشغيل قائمة Repo gates النهائية بعد استقرار كل التغييرات.
- [x] بناء الصورة العامة محلياً وتشغيل بوابات
  الهوية/QML/icon/initramfs/Plymouth/bootc داخلها؛ `just build` خرج 0 والصورة
  `5e64dbf3373a…`، وinitramfs حجمه 122MB ويحتوي OSTree وMoOS Plymouth.
- [ ] إقلاع VM وتصوير كل الأسطح بالإنجليزية والعربية RTL وعلى النسب والمقاييس
  المرجعية؛ ثم قياس جلسة هادئة.
- [x] MoPlayer: إصلاح حد `Duration.zero`، analyze نظيف و**176/176**، واستبدال
  chrome traffic-light بتحكم MoOS أصلي. دُفع canonical commit `23799ad` ثم
  نُسخ إلى شجرة الصورة بـ`just sync-moplayer`.
- [ ] لا توصف الشجرة بأنها shipped قبل
  CI الموقّع، staging، reboot و
  `tests/post-update-check.sh`.

## جاهزية الشحن: التدقيق العدائي وإصلاحاته (THEME_REV=24، 2026-07-30)

- [x] تدقيق 16-وكيلاً لكل أسطح سطح المكتب مع دحض عدائي مستقل: 8 نتائج مؤكدة،
  2 مدحوضتان، واستُرجعت النتائج المُسقطة بسقف التحقق من سجل الـworkflow.
- [x] إطار النافذة: الشريط المكبَّر كان تدرجاً مقروءاً خارج مداه (لطخة `#527F79`
  بتباين 3.12:1 في كل اللوحات الفاتحة) — الآن لون ختامي مسطّح، أسوأ تباين مقيس
  عبر الـ16 ثيماً **10.28:1** ووميض التركيز 1.06–1.16:1؛ الأزرار والشرطة توسّطت
  (مع مفتاح `ButtonMarginTopMaximized` الذي لا يَرِث)؛ قناع الـblur الميت أُزيل
  وقرار المادة موثّق في المولّد. (`2290dbb`)
- [x] شاشتا القفل والدخول: ستة مواقع `Inter` (بلا عربية) صارت IBM Plex Sans
  Arabic — وجه عربي واحد؛ رأس حلقة المذنّب حصل على غطاء 6° بدل القطع الحاد
  (21 نسخة أُعيد توليدها)؛ هالة القفل صارت مشتقة من `accentA/accentB` كمرآة
  الخروج تماماً. (`97f2b89`، `c9a9c25`، `17ecd65`)
- [x] القشرة: مؤشر التحكم عن بعد **لم يعد مخفياً** (المخفي لا يظهر عند التنشّط
  في Plasma 6 — قِيس أثناء جلسة حية؛ انقلب عقد البوابة مع اختبار سلبي)؛ حذف 16
  سطر `layoutDirection` كانت تعكس المشغّل عكسين فيرتد LTR — تحقق حي قبل/بعد؛
  توحيد أرقام التاريخ لاتينيةً في حبة الرصيف؛ زر الثيمات يلبس أيقونة تطبيقه؛
  المعرّف العربي لجسر الفيديو في قائمة الإخفاء. (`3cf2e4b`)
- [x] التطبيقات: فخ `onAccent` (معالج إشارة متنكّر — تسميات سوداء) صار
  `accentText` والاسم محظور ببوابة؛ المحدّث يعرض المُقلع لا المرحَّل؛ متجر
  RTL يفتح على بداية القراءة مع تمرير كيبورد كامل وensure-visible؛ منتقي
  الثيمات حصل على معاملة الكيبورد كاملة؛ زر التراجع في Recovery لم يعد
  مقصوصاً على 4K/225%؛ صفوف المناطق الزمنية في المثبّت بلا TypeError.
  (`c046c81`، `a151e61`، `181217e`، `0124a6d`)
- [x] أيقونة Mo AI المفوَّضة جلست على لوحة العائلة (85.9% كالأشقاء حرفياً)
  بمولّد حتمي جديد، والماستر بايت-ببايت كما فرضت البوابة. (`ea8c591`)
- [x] محقق الحركة البديل أنهى المسح: **بنية التبويب سليمة بالكامل** (309 حلقة لا
  نهائية، كلها خلف `longDuration > 1` + شرط رؤية/حالة). أربع نتائج أُصلحت في
  نفس اليوم: نبضة فهرس المتجر كانت بلا حدّ عند فشل البناء (~12% نواة إلى
  الأبد — صارت مقيّدة بـ`indexPoll.running` مع نص حالة فشل)؛ مشهد Mo AI
  المحيطي كان يكلف ~13% طوال حياة النافذة (صار `paused: !root.active`)؛
  شريط عدّ الخروج كان يتجاهل إيقاف الحركة (950ms صارت خلف البوابة، 16
  نسخة)؛ حلقة الجلسة البعيدة في Mo AI حصلت على شرط الرؤية الناقص.
  قياس plasmashell ~9% بقي **بلا إسناد** — سطح المكتب لم يكن هادئاً (جلسات
  تطوير نشطة)؛ يُعاد قياسه ضمن فحص ما بعد التحديث على جلسة هادئة.
- [ ] المتبقي المفتوح — الخطة الكاملة القابلة للتنفيذ (مهمة/سبب/أولوية/قبول/
  اختبار/مخاطر) في **`docs/SESSION_HANDOFF_2026-07-29.md` §6**، وأعلاها:
  تحقق ما بعد إعادة التشغيل (Critical)، إعادة قياس plasmashell على جلسة
  هادئة (High)، تمرير كيبورد Welcome/Installer وAppStream للمتجر (Medium)،
  تحقق تحديث VPS moos-cloud (Medium — يتحدث ذاتياً من ghcr؛ SSH خلف
  إعادة مصادقة Tailscale)؛ شعار البطل قرار مالك.

## MoOS UI unified polish (THEME_REV=22)

- [x] جرد 16 Global Themes وربط كل عضو بألوانه وPlasma Style وAurorae وKonsole
  والخلفية، مع بقاء Breeze محرّك fallback غير معدّل.
- [x] توليد 16 سطح Plasma عالي الظهور لكل Style، مع hover/pressed/focus وحواف
  ومجموعة blur-mask كاملة بدل الاعتماد البصري الواسع على Breeze.
- [x] توليد Aurorae كامل: حالات نافذة عادية/غير نشطة/معتمة/maximized وعشرة أزرار
  وظيفية بثماني حالات تفاعل؛ استخدام seam الحقيقي
  `org.kde.kwin.aurorae.v2`.
- [x] زوج خلفيات Graphite Frost/Tidal Frost جديد مربوط بالمشروع، وصادرات
  4K/ultrawide/16:10 بقصّ LANCZOS بلا تمديد، مع حركة محيطية منخفضة الكلفة.
- [x] عائلة أصلية لتسعة رموز تطبيقات MoOS، وطبقة أولوية داخل Dark/Light،
  ومؤشر افتراضي لا يسقط إلى Adwaita.
- [x] توحيد Blur على 15/3، وربط Qt/GTK/GSettings/Flatpak portal، وتحسين RTL
  والساعة المصغرة وهوية نافذة منتقي الثيمات.
- [x] بوابات بنيوية جديدة تعضّ لكل ما سبق، وترحيل
  `THEME_REV=22`/`MOOS_THEME_REV=10`.
- [x] بناء الحاوية المحلي النهائي للفرع: نجحت صورتا `moos` و`moos-nvidia`
  على `7.1.4-204.fc44.x86_64`، مع بوابات الهوية وbootc وinitramfs/Plymouth،
  وثمانية إدخالات NVIDIA في initramfs، و50 قاموس Qt WebEngine في كل صورة.
- [x] دمج الفرع في `main` (`1e7991b` ثم `5823f93`)، ونجاح CI ونشر الصورتين
  الموقّعتين: التشغيل `30152979451` انتهى أخضر في 17د37ث ووقّع
  `moos:latest` و`moos-nvidia:latest` بـ cosign.
- [x] **أُغلقت 2026-07-25**: الجهاز جُهِّز على الأصل الموقّع
  (`ostree-image-signed:docker://ghcr.io/moalfarras-sys/moos-nvidia:latest`،
  البصمة `sha256:12b44aba…` = المنشورة بالضبط، الإصدار `44.20260725.347`)، وأُعيد
  الإقلاع، و`tests/post-update-check.sh` رجع **48 ناجحة / 0 فاشلة** على سطح المكتب
  الحي — بما فيها السطر الذي كان أحمر: الصورة المُقلَع منها هي المنشورة. الكيرنل
  `7.1.4-204` مع تعريف NVIDIA `610.43.03` المطابق، و`BlurStrength=15`، و`code`
  المُطبَّقة بقيت. `moos-selfcheck`: 46 فحصاً ناجحاً.

## MoOS UI2 — Graphite / Tidal Glass (THEME_REV=16، سجل تاريخي)

- [x] **زوج كامل وليس Light فوق Dark**: لكل جهة حزمة Global Theme وPlasma Style
  وSVGs وAurorae وKDE/Konsole scheme وخلفيتها الخاصة. اللايت تركواز معدني بلا أبيض
  فاقع؛ الدارك غرافيت رمادي بلا بنفسجي UI1. التباين محروس باختبار WCAG.
- [x] **Dashboard جديد مستقل**: `org.moos.ui2.dashboard` بقياس 31×12، ساعة rolling،
  طقس حقيقي بتسعة أصول 3D محلية وحركة حسب الحالة، وCPU/RAM/GPU. بلا MouseArea أو
  ShaderEffect/QtQuick3D/Lottie، وكل الحركة مربوطة بالرؤية وإعداد سرعة الأنيميشن.
- [x] **إثبات على النظام الحقيقي**: `plasmawindowed` بقي حياً، الدارك واللايت قُرئا
  من KConfig والتقطا من containment الحقيقي، والبار صُوّر في الجهتين. لا خطأ QML أو
  missing module أو crash في journal. الموضع المثبت `80,64,560,224`.
- [x] **ترحيل ورجوع لا يتركان نسختين**: UI2 يُنشأ ويُثبت أولاً، ثم UI1 يُزال فقط
  بعد إثبات التحميل. اختُبر الاتجاهان فعلياً وبقي Widget واحد في كل مرة.
  **تصحيح (2026-07-27):** قيل هنا إن `moos-theme ui1-dark|ui1-light` يعكس
  العملية — **هذا الأمر غير موجود** (`grep -c ui1` على السكربت يعطي 0)، وUI1 نفسه
  أُزيل من الصورة المشحونة. مسار الرجوع الحقيقي: `moos-theme undo` ثم
  `plasma-apply-lookandfeel org.kde.breezedark.desktop` ثم `rpm-ostree rollback`.
- [x] **التطبيقات الأساسية تتبع الثيم**: Mo AI وWelcome يأخذان semantic palette من
  الثيم النشط، وMo PC Remote (الويب والنافذة الأصلية) يملك Graphite/Tidal متطابقين.
- [x] **بوابات جديدة تعضّ**: اكتمال الحزم، تباين الألوان، استقلال SVG، أبعاد واختلاف
  الخلفيات، أصول الطقس، passive/motion contract، ومSmoke حقيقي عبر `plasmawindowed`.
- [x] **البناء المحلي النهائي**: نجحت صور MoOS العامة وNVIDIA من المصدر نفسه؛ أثبتت
  نسخة NVIDIA تطابق الكيرنل ووجود ثماني وحدات NVIDIA داخل initramfs، ونجحت بوابات
  bootc وتجربة المستخدم والـDashboard في الصورتين.
- [x] **توقيع صورة CI ثم bootc upgrade وإعادة الإقلاع والتحقق — تم (2026-07-16)**:
  الجهاز مُقلَع على `44.20260716.179` بأصل `ostree-image-signed` وسياسة
  `containerPolicy`، والتحقق على النشر الجديد: `post-update-check.sh` ‏39/0
  و`moos-selfcheck` ‏39/39 (بعد إصلاح `else` الناقصة في فحص الكيبورد، `f46ba45`).

## ويجت سطح المكتب السابق (UI1، THEME_REV=12)

- [x] **الطقس والحرارة بأيقونات مرسومة متحركة**: الموقع من IP عبر `ipwho.is` والطقس من
  `Open-Meteo` — **بلا مفتاح وبلا حساب وبلا إعداد**. الأيقونات مرسومة متجهية لا من سمة
  الأيقونات: شمس يدور تاجها مرة كل 24 ثانية وقرصها يتنفّس، سحابة تنجرف، مطر يسقط بأطوار
  مختلفة (السقوط المتزامن يبدو باركوداً لا مطراً)، ثلج يتمايل، برق يضرب ضربتين كل أربع ثوانٍ،
  وضباب ينزلق. يعرض: الدرجة، الحالة بالعربية، المدينة، وأعلى/أدنى درجة اليوم.
  - **فخّ حقيقي كلّف وقتاً**: `ipapi.co` يجيب `curl` تماماً، لكنه يردّ على **User-Agent شبيه
    بالمتصفّح** (وهو ما يرسله Qt) بصفحة تحدّي Cloudflare — فالويجت يستقبل HTML بدل JSON،
    و`JSON.parse` يرمي، والطقس **لا يظهر أبداً بلا أي رسالة** (مخرجات plasmoid لا تُعرض).
    اختبار API بـ`curl` لا يثبت شيئاً عن الـAPI كما يراه الويجت. بوابة تمنع عودته.
  - إن فشلت الشبكة: الصف **لا يُرسم** أصلاً — لا صندوق مكسور ولا «—°».
- [x] **الساعة لم تعد تقفز**: كانت تُعيد رسم `HH:mm` كاملاً وترفع الكتلة كلها كل دقيقة، فترمي
  أربعة أرقام لم تتغيّر في الهواء مع الرقمين اللذين تغيّرا — يُقرأ كارتعاشة. الآن **كل رقم
  أسطوانة مستقلة**: الرقم القديم يصعد ويختفي (خلال ثلث المدة) والجديد يطلع من تحته ويستقر
  بارتداد خفيف؛ ورقم لم يتغيّر **لا يتحرك إطلاقاً**. مُثبَت بلقطة في منتصف الأنيميشن: عند
  15:26→15:27 الرقم الأخير وحده كان طائراً ومصغّراً، والبقية ساكنة.
  - والنقطتان لا تقفزان: **تتنفّسان** ببطء (2.6 ثانية) — الدليل الوحيد أن الساعة حيّة لا صورة.
- [x] **لا يغطّي شيئاً ولا يلتقط نقرة**: بلا خلفية (NoBackground) وبلا **أي** `MouseArea` —
  ويجت يقبل النقرات يبتلع قائمة سطح المكتب اليمنى وتحديد المستطيل داخل مساحته والمستخدم لا
  يعرف لماذا مات سطح مكتبه هناك. بوابة تمنع إضافة `MouseArea`.
- [x] **الكاش لا يجمّد الويجت**: OSTree يثبّت mtime، وqmlcache مفهرس على mtime — فبعد الترقية
  يظل plasmashell ينفّذ **الويجت القديم المُصرَّف** والملف الجديد موجود على القرص.
  `moos-apply-theme` صار يطهّر qmlcache عند كل `THEME_REV` (وليس مرة واحدة كما في
  `moos-ui-migrate`).

## سرعة الإقلاع والاستجابة (مقيسة على الجهاز، لا مفترضة)

- [x] **إخراج فهرس التطبيقات من مسار الإقلاع**: `systemd-analyze critical-chain` أظهر
  `fedora-atomic-desktop-appstream-cache-refresh.service` **داخل** السلسلة الحرجة بـ **‎+3.525s**
  — ثلث زمن userspace كله (‎11.5s) يُنفَق في بناء فهرس متجر تطبيقات لم يفتحه أحد بعد، والمستخدم
  ينتظر. المهمة تبقى، لكن عبر `moos-appstream-refresh.timer` بعد **3 دقائق** من جهوز سطح المكتب.
  بوابة في `build.sh` تفشل البناء لو أُعيدت تسمية الوحدة الأصلية (وإلا `disable` يصمت وترجع
  التأخيرة ببناء أخضر).
- [x] **تحديث Flatpak لا يُحسّ**: أغلى وحدة في إقلاع MoOS (`systemd-analyze blame`:
  **1min 3.885s**) كانت تنطلق بعد **دقيقتين** من الدخول بأولوية عادية (Nice=0، I/O best-effort)،
  فتزاحم سطح المكتب على المعالج والقرص في أكثر لحظة انشغالاً — وهذا حرفياً ما يصنع إحساس «النظام
  بطيء بعد الدخول». الآن: `Nice=19` و`CPUSchedulingPolicy=idle` و`IOSchedulingClass=idle`،
  والانطلاق بعد **10 دقائق**. لا يتغيّر ما تفعله الخدمة — يتغيّر متى وكيف تزاحم.
- [x] **والوحدة الأغلى كانت التوأم الذي فات**: البند السابق أصلح
  `flatpak-**system**-update`. أما `flatpak-**user**-update` — وهو في جلسة المستخدم، وأغلى:
  `systemd-analyze blame --user` يعطيه **1min 19.073s** مقابل 1min 3.885s للنظامي — فبقي على
  إعداد المُوزِّع `OnBootSec=2m` بلا `Nice` ولا جدولة I/O. أي أن الإصلاح المكتوب أعلاه كان
  نصفه فقط، والنصف الباقي هو الذي يقع **بعد دقيقتين من الدخول** بالضبط حين لم يستقرّ سطح
  المكتب بعد. الآن له نفس المعاملة: 25 دقيقة و`Nice=19` وجدولة خاملة.

  **الفخّ الذي كاد يجعل الإصلاح صامتاً:** قيم الوقت في مؤقّتات systemd **قائمة**، لا قيمة
  مفردة. `OnBootSec=25min` وحده في drop-in **يُضاف** إلى 2m ولا يستبدلها، فينطلق المؤقّت عند
  الاثنتين — إصلاح يبدو مطبَّقاً ولا يفعل شيئاً. لذلك يسبقه `OnBootSec=` فارغ. مُثبت حياً:
  `systemctl --user show -p TimersMonotonic` يعطي `{ OnBootUSec=25min }` وحدها.
- [x] **`AnimationDurationFactor` كان مكتوباً في المجموعة الخطأ منذ اليوم الأول**:
  `etc/xdg/kwinrc` كان يشحن `[Compositing] AnimationDurationFactor=0.75` — و**لا أحد يقرأ
  ذلك**. تحقّقٌ من المخططات المثبَّتة (2026-07-27): `kwin.kcfg:314` يُعلن المفتاح داخل
  `<group name="KDE">`، و`[Compositing]` في نفس الملف يُعلن حصراً `AllowTearing` و`Backend`
  و`Enabled` و`LastFailureTimestamp`، و`landingpage_kdeglobalssettings.kcfg` يربط نفس المفتاح
  بـ`kdeglobals`. فكل سطح مكتب MoOS كان يعمل على 1.0 بينما الملف يدّعي 0.75. والمستودع نفسه
  كان يعرف الصحّ في موضعين (`moos-fast-remote:85`، `build.sh:2909`) — الخطأ في `kwinrc` وحده.
  الافتراضي الآن **0.5** في `etc/xdg/kdeglobals` تحت `[KDE]`.

  ولا يوجد تثبيت مرافق في `moos-apply-theme`، وهذا قرار مفحوص لا سهو: الفخّ المعتاد
  («ثيم تشحنه الصورة ليس ثيماً يحصل عليه المستخدم») هو مفتاح جالس في `~/.config/kdedefaults`
  الذي يتفوّق على `/etc/xdg`. الفحص الحيّ: مجموعة `[KDE]` هناك تحمل `widgetStyle` فقط، ومن لم
  يمسّ الإعداد **لا يملك** قيمة خاصة به أصلاً (بلازما لا تبذر واحدة). فالافتراضي يصل، ومن
  اختار قيمته في الإعدادات يحتفظ بها.

  **وبصراحة عن المكسب:** إدراكيّ فقط. قياس هذا المستودع نفسه (`build.sh:2983`) وجد أن 0 مقابل
  1 أعطى 157% مقابل 139% لـplasmashell — داخل الضجيج. يجعل سطح المكتب **يُحسّ** فورياً، لا
  أرخص.
- [x] **`nvidia_peermem` كان يفشل في كل إقلاع**: مثبّت ublue يفرض تحميله ضمن `force_drivers`،
  وهو وسيط GPUDirect/RDMA لبطاقات مراكز البيانات بـNVLink+InfiniBand — **لا يمكن** أن يُحمَّل
  على بطاقة استهلاكية. السجل يحمل الفشل حرفياً في كل إقلاع:
  `modprobe: ERROR: could not insert 'nvidia_peermem': Invalid argument` (ublue-os/bazzite#4569).
  ليس قاتلاً، لكنه محاولة تحميل حقيقية داخل الطور الذي يكلّف ~2s، لوحدة لن تُحمَّل أبداً.
  أُسقط بـ`sed` منفصل عن `sed` الـ`i915 amdgpu` عمداً — ذاك مربوط بترتيب ublue بالضبط ويصمت
  إن غيّروه، والاثنان يفشلان مستقلَّين — مع بوابتين: واحدة تفشل البناء إن بقي `peermem`،
  والثانية تفشله إن أكل الـ`sed` معه `nvidia_drm`.
- [x] **مفتاح «أعد التكيّف بعد التحديث» في `moos-hardware-adapt` لم يعمل قط**: الختم مبنيّ على
  `rev` **و** الصورة المُقلعة، لكن `booted_image()` كان يقرأ
  `bootc status --json` عند `.status.booted.image.image.image` — وهذا المسار غير موجود:
  `status.booted.image` يعود **كائناً فارغاً**، فيرمي التعبير ويطبع `except` فراغاً. الختم
  ينحدر إلى `rev=1 image=` على كل جهاز، وهو يطابق نفسه للأبد. الدليل على جهاز المالك
  (2026-07-27): `/etc/moos/hardware-adapt.state` يحوي حرفياً `rev=1 image=`، والوحدة سجّلت
  «already adapted» في كل إقلاع منذ التثبيت. يقرأ الآن `container-image-reference` من
  `rpm-ostree status --json` — نفس الحقل الذي يقرأه `moai-do` أصلاً، وهو مملوء ويحمل الـdigest.
- [x] **الجهاز كان يعمل بنصف سرعته، و13 إقلاعاً متتالياً لم يلاحظ أحد**: ملف تعريف tuned
  المُفعَّل كان `powersave`، وهو يضع `boost=0` — أي **Turbo مطفأ**:
  `no_turbo=1`، `scaling_max_freq=2500000` على معالج توربو 4.7GHz، `EPP=power`. قياس
  خيط مفرد قبل/بعد التحويل إلى `balanced`: **0.594s ← 0.314s (‏1.9×)**، وتحت حمل كل الأنوية
  انتقلت من التثبيت على سقف 2.5GHz إلى ~3.95GHz. ويضيف نفس الملف `vm.laptop_mode=5` و
  `alpm=med_power_with_dipm` — تأخير كتابة وكمون رابط SATA على قرص إقلاع **حاسوب مكتبي**.
  ليس إعداداً من MoOS: زرّ «توفير الطاقة» في بلازما يُسقط عليه عبر tuned-ppd، وهذا حقّ
  المستخدم. ما تغيّر أن الافتراضي (`balanced` — لا `performance`) صار يعيد فرض نفسه بعد
  التحديث بدل أن يُختم مرة واحدة إلى الأبد (`HW_REV` 1 ← 2 مع إصلاح الختم أعلاه).

### مقيس ومرفوض — لا يُطبَّق، والسبب مكتوب

- [ ] **«‏kernel 6.098s» رقم وهمي، لا تُنفق عليه دقيقة**: عدّادات ACPI FPDT تقول إن ساعة
  النواة الرَتيبة تقف عند 6.2476s حين تطبع النواة **أول** سطر لها، لأن TSC هذه اللوحة يبدأ
  العدّ في منتصف POST. فيُحسب نفس الـ6.2s مرتين: داخل `firmware 11.301s` ومرة أخرى في
  `kernel`. تهيئة النواة المرئية الحقيقية (`Linux version` ← `Run /init`) = **‎~17ms**.
  والمجموع الحقيقي إلى `graphical.target` ‏**~26.5s** لا 32.8s، وإلى سطح مكتب صالح للاستعمال
  **~40.7s**.
- [ ] **72 ميغابايت من firmware بطاقة Ampere داخل initramfs (33% من حجمه) — مرفوض للخطر**:
  `lsinitrd --size` يعطي `gsp_ga10x.bin` ‏84.3MB خاماً / **72.5MB بعد ضغط zstd** من أصل
  227.6MB. بطاقة هذا الجهاز TU104 (Turing) وتستعمل `gsp_tu10x.bin`. إسقاطه يهبط بالـinitramfs
  إلى ~155MB، فيقرأ GRUB ‏174MB بدل 246MB عبر سائق btrfs الخاص به (~57.6MB/s مقيسة) — أي
  **~1.26s** من زمن المُقلِع. **ولم يُطبَّق:** هذه صورة تُشحن لأجهزة أخرى، وبطاقة RTX 30/40/50
  لن تجد firmware الخاص بها في الـinitrd فلا يُهيَّأ التعريف باكراً — وهذا بالضبط صنف العطل
  الذي يقول AGENTS.md إنه «شاشة سوداء». الطريق الآمن (نقل الملف جانباً قبل توليد الـinitramfs
  وإعادته بعده) يحتاج بناءً حقيقياً واختباراً على عتاد Ampere، ولا يوجد.
- [ ] **ثيم GRUB الذي تشحنه الصورة لا يُحمَّل إطلاقاً**: `/boot/grub2/grub.cfg` سطره الأول
  `# Generated by bootupd / do not edit` — ملف **ساكن** منسوخ حرفياً من coreos-assembler،
  لا يولّده `grub2-mkconfig`. لذلك `GRUB_TIMEOUT` و`GRUB_TIMEOUT_STYLE` و`GRUB_THEME` و
  `GRUB_DISTRIBUTOR` في `/etc/default/grub` **كلها خاملة**، ولا يوجد `set theme=` في أي مكان،
  ولا مجلد `themes/` تحت `/boot/grub2/`. و`grub2-mkconfig` **ممنوع** على Atomic. أي أن سطح
  الهوية الوحيد الذي يسبق Plymouth ليس ملكنا فعلياً — يحتاج مساراً آخر بالكامل، ولم يُبتّ بعد.
- [ ] **`timeout=1` يبقى كما هو**: الـ5.278s ليست المهلة — المهلة ثانية واحدة بالبناء، والباقي
  تهيئة GRUB و`blscfg` وقراءة 246.4MB. تصفيرها تشتري ثانية من 26.5s وتحذف **واجهة الرجوع
  الوحيدة** على نظام ذرّي. مقايضة خاسرة. ولطلب القائمة مرة واحدة:
  `sudo grub2-editenv /boot/grub2/grubenv set menu_show_once_timeout=10`.
- [ ] **‎10.73s من الـPOST خارج قبضة النظام**: FPDT يقول إن البرنامج الثابت يستغرق 10.73s قبل
  أن يقرأ المُقلِع أصلاً. الروافع كلها في BIOS اللوحة (Gigabyte B660 GAMING X DDR4، F34):
  إطفاء CSM، وإطفاء Network Stack/PXE OPROM، وتشغيل Fast Boot (لا Ultra — يتخطّى تهيئة USB
  فلا لوحة مفاتيح عند POST)، وإطفاء منافذ SATA غير المستعملة، وتشغيل VT-d (‏`IOMMU: Not found`
  هو سبب تقييم `HSI:0!`). و**Memory Context Restore ليس منها** — ذاك مفتاح DDR5، وهذه الذاكرة
  DDR4-2400 تتدرّب في أقل من ثانية. تقدير: 3-6s. ومعها: نزع مفتاح Ventoy الـUSB يوفّر 1-3s
  (البرنامج الثابت يفحص كل جهاز موصول، وهذا يعرض قسمَي إقلاع).

## MoPlayer — تطبيق أساسي داخل الصورة

- [x] **MoPlayer 1.1 صار مشغّلاً كاملاً لا واجهة فقيرة**: تحكم تشغيل/إيقاف، تقديم وتأخير
  10 ثوانٍ، السابق/التالي، شريط تقدّم، صوت وكتم، ملء الشاشة، احتواء/ملء/حجم أصلي، سرعات،
  ومسارات الصوت والترجمة. التعليق صار حالة مرئية مع إعادة اتصال محدودة وآمنة وزر محاولة،
  لا دوراناً صامتاً إلى الأبد.
- [x] **الهوم سينمائي وخفيف**: البطل يأخذ الخلفية والوصف من الكتالوج، والتخطيط يتكيّف مع
  الشاشة؛ أزيلت حركتان لا نهائيتان كانتا تبقيان نافذة 4K الخاملة قرب نواة CPU كاملة
  (القياس بعد الإصلاح ~1.6%).
- [x] **السيرفر يُحفظ فعلاً وبشكل مشفّر**: خدمة أسرار للجلسة + ترحيل دقيق للإعداد القديم
  الذي عطّل المحفظة، مع احترام أي إعداد مخصص. والتطبيق لا يعرض نجاحاً كاذباً إذا رفضت
  الخزنة إحدى الكتابتين، ولا يسقط كلمة المرور في ملف نصي.
- [x] **تشغيل واحد متماسك**: فتح ملف أو رابط ثانٍ يرسله إلى نافذة MoPlayer الحالية بدل
  إنشاء عملية أخرى، ومثبت حياً أثناء استمرار الفيديو.
- [x] **يُبنى من المصدر داخل الصورة** (`moplayer/` مُضمَّن، ومرحلة Flutter في `Containerfile`
  ترميه بعد البناء): الصورة تحمل الحزمة (~40MB) ولا تحمل أي أداة بناء. `libmpv` هو نفسه
  الذي يشحنه النظام — التطبيق **لا يضيف أي اعتمادية جديدة**.
- [x] **«الصق الرابط الذي أُعطيته»**: اشتراك IPTV يُباع كرابط واحد
  (`…/get.php?username=&password=&type=m3u_plus`). ذاك الرابط **يعمل** في حقل M3U — وهذا هو
  الفخّ: يُقرأ كقائمة تشغيل فيعطي القنوات فقط (لا أفلام، لا مسلسلات، لا دليل). `SourceLink`
  يقرأه كما هو — لوحة + حساب — فتُفتح واجهة Xtream كاملة. على اشتراك المالك: من نفس النص،
  **12,653 قناة + 20,187 فيلماً + 10,550 مسلسلاً** بدل القنوات وحدها.
- [x] **دليل البرامج من XMLTV**: هذه اللوحة تردّ على `get_short_epg` بـ`[]` لكل قناة، بينما
  `xmltv.php` يعطي 2,587 برنامجاً. الدليل «الفارغ» لم يكن فارغاً — كان نقطة نهاية لا تنفّذها
  اللوحة. يُحلَّل على عزلة خلفية، يُخزَّن 12 ساعة، ويُزال تكراره (اللوحة تنشر البرنامج مرتين).
- [x] **ويجت المباريات من دليل اللوحة نفسها** — لا API رياضي ولا مفتاح: 306 مباراة بأسماء
  الفرق والبطولة والتوقيت، **مربوطة بقنوات المستخدم**، فالنقر عليها يفتح القناة. مباراة على
  قناة لا يحملها الاشتراك **لا تُعرض** (بطاقة لا تُنقر خيبةٌ متنكّرة في هيئة ميزة).
- [x] **الطقس** بلا مفتاح ولا حساب (`ipwho.is` + Open-Meteo). و`ipapi.co` **مرفوض عمداً**:
  يردّ على curl صحيحاً ويردّ على User-Agent المتصفّح بتحدّي Cloudflare — واختبار API بـcurl
  لا يثبت شيئاً عن الـAPI **كما يراه التطبيق**.
- [x] **الكتالوج يُقدَّم الأحدث أولاً**: طلب كل الأفلام يعيد أولاً 32 مباراة مسجَّلة بنفس صورة
  FIFA — جدارٌ من صورة واحدة مكرَّرة يقرأه المستخدم كفشل تحميل. بالترتيب الزمني تفتح نفس
  الصفحة على مكتبة حقيقية.
- [x] **البث مثل الريسيفر**: اللوحة الثالثة تتبع القناة التي **تنظر** إليها لا التي تُشغَّل.
  الحساب يسمح باتصال واحد — فمن يضطر لتشغيل قناة ليعرف ما عليها يُسقط بثّه بنفسه.
- [x] **حارس ذاكرة GPU داخل مشغّل التطبيق نفسه** (لا في نسخة الصورة فقط — كان فرقاً صامتاً
  يُمحى بأمر `install -D` واحد). وبوابة تفرض تطابق النسختين بايتاً ببايت.
- [x] **`just sync-moplayer` يرفض شجرة غير نظيفة**: التضمين يعتمد `git ls-files`، فملف جديد
  غير مثبَّت **لا ينسخه أحد** — وتفشل الصورة بعد عشرين دقيقة داخل الحاوية على import مفقود.

## عقل Mo AI: يُختار بما تملكه الآلة، لا بثابت واحد (2026-07-27)

كان العقل الافتراضي ثابتاً واحداً — `qwen2.5:7b-instruct` — وهو **صحيح على نصف
التركيبات وخاطئ على النصف الآخر**. صار `moos-ensure-brain` يقيس ذاكرة الكرت
ويختار، وكلا الخيارين **مُقاسان في هذا المستودع** لا مأخوذَين من لوحة ترتيب:

| الآلة | العقل | لماذا |
|---|---|---|
| كرت ‏≥ ٧٫٥ غيغا | `qwen3:8b` | أفضل عربية جُرِّبت، ومعها الأوامر والشروح الصحيحة — أي ما يلزم عقل تطوير وصيانة |
| بلا كرت (السحابة) | `qwen2.5:7b-instruct` | أسرع مقاساً بلا كرت، وبلا تفكير مخفي — ونصّ خطأ Mo AI نفسه يقول ذلك للمستخدم |

**ولا نموذج «coder»، مهما بدا مغرياً لعقل تطوير:**
- `qwen2.5-coder:7b` **جُرِّب ورُفِض**: كود ممتاز وعربية مكسورة — أخرج «الملفات
  الممكدة» (ليست عبارة) واخترع خيار `lsof -m` غير الموجود. عقل لا يمسك جملة عربية
  يسقط عقد MoOS ثنائي اللغة مهما برمج.
- `qwen3-coder` **لا يصلح أصلاً على هذا الكرت**: أصغر وسم له هو بناء 30B-A3B بوزن
  ~١٩ غيغا ويحتاج ١٦ غيغا+. لا يوجد وسم تحت ١٠ غيغا. وعلى ٨ غيغا سيفيض إلى ذاكرة
  النظام — والفيض على هذه الصورة لا يعني بطئاً فقط: `moos-gpu-headroom` موجود لأن
  `kwin_wayland` مات على `NVRM: invalid mmap context` حين نفدت ذاكرة الكرت.

`num_ctx 24576` ثابت في **الملفَّين**: نفس النموذج يخدم وكيل تيليجرام (OpenClaw)
الذي يبلغ نصّ نظامه وأدواته ~١١٫٥ ألف رمز وحده. وبوابة في
`verify_user_experience.py` تحرس: وجود الملفَّين، وقاعدتهما، و`num_ctx`، وغياب
`SYSTEM` مخبوز، وألا يكون الأساس نموذج coder، وألا يعود فحص «العقل صحيح؟» إلى
عائلة واحدة مثبَّتة (كان يفعل — وعلى آلة بكرت كان سيهبط بالعقل الصحيح إلى نموذج
المعالج في **كل** تسجيل دخول).

## ترتيب الدفعات التالية

0. **المشغّل الموحّد «شغّل أي برنامج من أي نظام» (أولوية المالك)** — تقدّم كبير:
   - [x] **Windows كسطح أول**: إجراء `moai-do setup-windows` يثبّت Bottles (Wine مُدار) **ويفتحه**
     مباشرة (idempotent — يكتشفه إن كان مثبّتاً ويفتحه فقط). كتالوج التوافق في Mo AI صار يفتح
     `moos://do/setup-windows` بدل تثبيت خام يترك المستخدم أمام Bottles غير مفتوح. مُختبَر حياً:
     فتح Bottles فعلاً.
   - [x] **Android أمتن**: `do_setup_waydroid` صار idempotent (يتخطّى إعادة تنزيل صورة الغيغابايت
     إن كانت مهيّأة عبر `waydroid status`)، برسائل أوضح، ويشغّل الجلسة، ويرشد لتثبيت APK
     (`waydroid app install`). مُختبَر: الكشف يختار الفرع الصح بلا إطلاق تنزيل.
   - [x] **النقر المزدوج يشغّل الملف — «نزّل وشغّل» بنقرة**: `moos-run-foreign` (+
     `org.moos.runforeign.desktop`) يمسك ‎.exe/.msi/.lnk/.msp و‎.apk. لو الطبقة موجودة يشغّل
     الملف فيها (Bottles عبر بوابة مستندات Flatpak — مُختبَر حياً: وصل الملف كـ
     `/run/user/1000/doc/…`، أي عبر حاجز الساندبوكس صح)، ولو ناقصة يشرح ويعرض تجهيزها بنقرة
     (`moos://do/setup-windows` أو `setup-waydroid`) بدل أن يفشل صامتاً. و‎.apk يبدأ الحاوية
     ويثبّت في طرفية مرئية ثم يفتح واجهة Android.
     - ما كان مكسوراً فعلاً (من قاعدة MIME الحيّة لا من الذاكرة): ‎.exe يُصنَّف
       `application/x-msdownload` أو `vnd.microsoft.portable-executable`، و**لا أحد** كان
       يعلن الثاني — فالملف كان يُفتح في محرّر نصوص. و‎.apk كان يمسكه معالج Waydroid الخام
       الذي ينفّذ `waydroid app install` بلا أي نافذة: على جهاز لم يُهيَّأ فيه Android، النقر
       المزدوج لا يفعل شيئاً إطلاقاً. الأنواع الخمسة مثبّتة في `/etc/xdg/mimeapps.list`
       و`moos-apply-theme` يثبّتها في ملف المستخدم (الذي يتفوّق على النظام)، وبوابة تحرس
       القطع الثلاث لأن سقوط أيّها يقتل النقر بصمت.
   - [x] **طبقة قرار في Mo AI: «الحاجة ← أفضل تطبيق يعمل على هذا السطح»**. Flathub يرتّب
     بالشعبية لا بما يعمل على KDE، ولهذا تصدّر `io.github.cosmic_utils.camera` نتيجة «camera».
     `moai-control` صار يحمل جدول `KNOWN_GOOD` (camera←Snapshot، فيديو←haruna، صور←gwenview،
     موسيقى←elisa، محرر←kate، رسم←krita، مونتاج←kdenlive) يرفع الجواب الأصلي للسطح إلى القمة
     بشارة **«اختيار MoOS»** وسببٍ مكتوب، و`desktop_mismatch()` يضع تحذيراً أصفر على أي تطبيق
     مبني لسطح آخر (COSMIC) — **قبل** التنزيل لا بعده. الاستعلام بالعربية يعمل («كاميرا» ←
     الكاميرا المُوصى بها). مُختبَر على ترتيب Flathub الحقيقي: COSMIC كان الأول فصار محذَّراً.
     بوابة تحرس الطرفين — الجدول **والعرض** — لأن ترتيباً لا تُظهره الواجهة لا يصل المستخدم.
     ومنذ 2026-07-13 يمرّ **كل** إطلاق (`launch_app` و`moos://apps/run`) عبر `moos-gpu-headroom`
     أولاً: MoPlayer كان محميّاً لأن مُطلِقه يستدعي الحارس، أما أي Flatpak يُثبّته Mo AI فكان
     يُطلَق على بطاقة يحتلّها الدماغ — فيموت على `eglMakeCurrent` بدل أن يتراجع للبرمجيات.
   - كل ما سبق يمرّ عبر `moai-do` (تأكيد + Polkit) أو مسارات مستخدم بلا صلاحيات؛ لا يُنفَّذ أي
     نص عشوائي — نفس عقد الأمان القائم.

1. **الجماليات المتقدمة (MoOS UI — Liquid Glass)** — بدأ التنفيذ:
   - [x] **دوك زجاجي دائم**: `Plasma::Theme::adaptiveTransparencyEnabled()` (مؤكَّد في libplasma)
     مُطفأ الآن في plasmarc، فالدوك يبقى مفروستاً حتى تحت النوافذ المكبّرة — مثل دوك ماك وشريط
     ويندوز 11. يظهر على ترقية THEME_REV=11 (apply-theme يُسقط كاش السمة ويعيد تشغيل plasmashell).
   - [x] **الطرفية: صلبة ومجمّلة (قرار المالك — الزجاج جُرِّب ورُفِض)**. `Opacity=1` و`Blur=false`
     في NovaDark وNovaLight: لوح كحلي معتم يقرأ فيه النص بأعلى وضوح. والتجميل: حشوة داخلية
     14px، شريط تمرير مخفي، تباعد أسطر، مؤشر كتلة بلون Nova السماوي، جرس صامت، ونسخ-عند-التحديد
     مع روابط ومسارات قابلة للنقر. و**UI أصغر**: بلا شريط قوائم، وشريط تبويب يظهر عند الحاجة فقط،
     و**بلا شريطَي أدوات**.
     - فخّان اكتُشفا بالفحص الحيّ (لا يظهران في أي بوابة نصية):
       1. Konsole يقرأ مفاتيح المؤشر من `[Cursor Options]` والتحديد من `[Interaction Options]`.
          كُتِبت أولاً تحت `[Cursor]`/`[Interaction]` — الملف يُحلَّل، البوابة خضراء، والمفاتيح
          **ميتة** (طرفية حيّة أظهرت مؤشراً أبيض رغم `CustomCursorColor`). البوابة الآن تحرس
          أسماء المجموعات نفسها.
       2. شريطا الأدوات (`mainToolBar`/`sessionToolbar`) **ليسا مفتاحَي إعداد**: يستعيدهما
          KMainWindow من بصمة Qt في `~/.local/state/konsolestaterc`، وهي خارج تعاقب XDG فلا
          يصلها `/etc/xdg` إطلاقاً. `moos-apply-theme` يقلب بايت الظهور داخل البصمة (مرّة واحدة،
          بعلامة `moos-konsole-chrome.v1`، فمن أرجعه يبقى عنده). مُختبَر: حالة نظيفة (ظاهر) ←
          apply-theme ← اختفى.
   - [x] الحركة مضبوطة أصلاً: `AnimationDurationFactor=0.5` (سريع) وتأثيرات fade/scale مفعّلة.
   - [x] **زجاج لنافذة Mo AI** (2026-07-17): خلفية حيّة مدفوعة بالباليت — تدرّج يتعمّق،
     شفقان ينجرفان أبطأ من تتبّع العين، توهّجا العلامة يتنفسان، والعلامة نفسها كعلامة مائية
     بحلقة المذنّب تدور بالدقيقة. السبرايتات القانونية في `/usr/share/moos/brand/`
     (generate_login_scene.py) وبوابة تحرس أن كل مرجع مطلق يملك ملفه داخل الصورة.
   - [x] **أُغلق المتبقي في 2026-07-25**: Aurorae موحّد لكل العائلة بإطارات
     وأزرار وحالات تفاعل أصلية، وقوائم/Launcher تأخذ أسطح Plasma الزجاجية
     والـblur masks نفسها بدل طبقة Shell متعارضة.
2. **تكامل Spotlight لـ Mo AI**: دمج Mo AI كـ plasmoid أو لوحة علوية منزلقة تبحث وتنفذ أوامر النظام الذكية عبر `Cmd+Space`.
3. **ميزات Mo PC Remote المتقدمة**: أُنجزت هوية الجهاز الموثوق وحدود الاستماع؛
   المتبقي ربط الإشعارات ومزامنة الملفات عبر Tailscale.
   - [x] **التنزيل قابل للاستئناف فعلياً** (`a8689686`): التذكرة الأحادية كانت تجعل
     أول Range يقتل كل المحاولات التالية. أصبح للتنزيل lease مدته خمس دقائق، مقيداً
     بملف واحد و32 استعمالاً، داخل سقف الذاكرة المشترك 1024؛ الصوت بقي أحادي الاستعمال.
     الخادم يعيد Range مع Last-Modified وETag ثابت. إثبات HTTP حي بالتذكرة نفسها أعاد
     206 لنطاقي `5-9/37` و`10-15/37` وبنفس ETag. بناءا Linux/Windows بلا تحذيرات،
     72 اختباراً والفحص الكامل نجحوا. المزامنة الخلفية ما زالت بروتوكولاً منفصلاً
     غير منفذ، فلا يُدّعى اكتمالها.
   - [x] **الرفع قابل للاستئناف وذري** (`e484249e`): الرفع الأحادي الذي يعيد الملف
     كاملاً بعد انقطاع صار جلسة مقيدة بالمستخدم لمدة 30 دقيقة، chunks بحجم 4MiB،
     offset سلطوي، وسقفي 64 جلسة و1GiB. الملف يبقى مؤقتاً على نظام ملفات الوجهة ولا
     يظهر باسمه النهائي إلا بعد commit ذري كامل؛ الإلغاء/الانتهاء ينظفانه. الهاتف
     يبصم metadata وأول/آخر 64KiB قبل الاستئناف، يعرض التقدم، ويسأل الخادم بعد ضياع
     الرد بدلاً من تكرار البايتات. إثبات HTTP حي: offset مكرر أعاد 409 وoffset=3،
     الهدف بقي غائباً قبل commit، ثم 200 ومحتوى `abcdef`، وإعادة commit أعادت 404.
     بناءا Linux/Windows بلا تحذيرات، 84 اختباراً، TypeScript، npm audit بلا نتائج،
     حزمة v28 والفحص الكامل نجحوا. إعادة تشغيل الخدمة تُبطل جلسة الرفع المقيمة في
     الذاكرة عمداً، واختبار هاتف فعلي لنقل كبير منقطع ما زال دليلاً مفتوحاً. تدقيق
     لاحق أغلق ثغرة تنظيف: انتهاء الجلسة لم يكن يُفحص بلا طلب تالٍ، أما الآن فمؤقت
     كل خمس دقائق يمسح الجدول المحدود حتى أثناء الخمول ويُختبر بساعة اصطناعية.
   - [x] **تنبيهات هاتف خلفية خاصة واختيارية** (PWA v29): من إعدادات Remote فقط
     وبضغطة مستخدم صريحة يمكن تفعيل حدثين عامّين: انقطاع الاتصال واكتمال الرفع.
     لا يقبل الحد نصاً حراً، فلا يمكن تمرير إشعار سطح المكتب أو اسم ملف أو الحافظة
     أو الاعتماد، ويبقى صامتاً والتطبيق ظاهر. النقر يعيد التركيز إلى المتحكم، ولا
     يوجد polling أو كلفة خمول جديدة. نجحت سبع حزم PWA وTypeScript والبناء وnpm audit
     (صفر نتائج)، وبناءا Linux/Windows بلا تحذيرات والفحص الكامل. هذه ليست Web Push:
     التطبيق المغلق أو المعلّق كلياً لن يتلقى شيئاً، وإثبات iOS/Android حي عبر HTTPS
     ما زال مفتوحاً.
   - [x] **حدود موارد نقل الملفات** (`21008908`): صورة الحافظة لم تعد تُنسخ حتى
     1GiB في الذاكرة ثم تُرفض؛ Content-Length الكبير يُرفض مبكراً والقارئ streaming
     لا يحتفظ بأكثر من 25MB. قوائم المجلدات محدودة بـ500 وتعرض `truncated` في الهاتف
     بدل تجميد الوكيل أو ادعاء قائمة كاملة. اختبارات الحد الدقيق/بايت زائد/520 ملفاً
     والتنظيف عند الانقطاع تمر، وبناءا Linux/Windows بلا تحذيرات، و65 اختباراً، PWA
     v27 الحتمي والفحص الكامل نجحوا. يبقى نقل كبير منقطع على هاتف فعلي؛ لا يوجد ادعاء
     بمزامنة خلفية تلقائية بعد.
   - [x] **دورة جهاز موثوق حقيقية** (`d4364fe7`): لم يعد الاسم يعني bearer ميتاً في
     `localStorage`. الثقة اختيار صريح لمدة 30 يوماً؛ الهاتف يستلم سراً عشوائياً مرة
     واحدة والخادم يحفظ SHA-256 فقط (وداخل DPAPI على Windows)، بحد 16 جهازاً. إعادة
     تشغيل الوكيل تستبدل الاعتماد بجلسة قصيرة جديدة، والإعدادات تعرض الاسم وآخر استعمال
     وتسمح بإلغاء كل جهاز منفرداً؛ تغيير PIN أو Sign out يلغي الاعتمادات المناسبة. أُغلق
     أيضاً سباق مطالبتين متزامنتين لأول PIN داخل قفل المصادقة. إثبات HTTP حي: bearer
     القديم 401 بعد restart، ثم resume/list/revoke = 200، وإعادة استعمال الاعتماد بعد
     الإلغاء = 401. بناءا Linux وWindows بلا تحذيرات، 61 اختبار .NET، TypeScript وnpm
     audit والبناء الحتمي والفحص الكامل نجحت؛ يبقى فحص اللمس البصري على هاتف فعلي.
   - [x] **أُغلق المستمع الخام في Linux وCloud** (`efaa3c98`): كان Kestrel يستخدم
     `ListenAnyIP` بجانب بوابة Tailscale HTTPS، وكانت أداة Cloud ما تزال تنشر خدمة
     الصوت غير المصادق عليها كمسار `/audio` مستقل. الوكيل الآن loopback فقط، وDesktop
     وCloud ينشرانه عبر Tailscale Serve؛ كلا مساري إعداد Cloud يسحبان أي mount صوتي
     قديم، وDoctor يعدّه تعرضاً لا متطلباً. الصوت يبقى خلف جلسة الوكيل وتذكرة أحادية
     الاستخدام. اختبار تشغيل فعلي أعاد 200 على loopback ورفض واجهتي المضيف وtailnet؛
     بناء .NET بلا تحذيرات و48 اختباراً والفحص الكامل نجحوا. يبقى اتصال هاتف حي عبر
     Serve.
   - [x] **طلبات التحكم لا تبقى معلّقة على شبكة سوداء** (`99c66235`): تسجيل الدخول،
     إعداد الـPIN، الحالة وأوامر الجلسة/الطاقة تستخدم الآن مهلة 15 ثانية عبر
     `AbortController` مع تمرير إلغاء المستدعي وتنظيف المؤقت والمستمع دائماً. رفع
     الملفات والبث مستثنيان عمداً لأنهما عمليتان طويلتان. اختبار Node يثبت إلغاء
     `fetch` لا يستجيب، ويمر TypeScript وnpm audit والبناء الحتمي وبوابة الأصول وفحص
     المستودع الكامل. الإصدار PWA v25؛ يبقى إثبات انقطاع tailnet فعلياً من هاتف.
4. ~~**الأمن الصارم**: إكمال سياسة sigstore~~ — **منجَز**. `sigstoreSigned` مفروضة على سجل
   MoOS عند التثبيت وعند كل تحديث؛ مفتاح خاطئ يُرفض فعلياً. الباقي: تثبيت ISO كامل على عتاد.

5. ~~**ترميز الفيديو بالهاردوير لـ Mo PC Remote**~~ — **منجَز، والبند كان بائتاً**
   (صُحِّح 2026-07-26). قُرئ الكود بدل الذاكرة فتبيّن أن الطرفين مشحونان:
   - **العميل** (`controller/src/lib/decode.ts`): `H264Stream` عبر WebCodecs، مع
     `canDecodeH264()` التي تشترط `window.isSecureContext` صراحةً.
   - **الوكيل** (`agent-linux/mo-remote-portal.py`): سُلَّم مرتَّب من المُرمِّزات —
     `nvh264enc` ← `vah264enc` ← `vah264lpenc` ← `openh264enc` — مع قائمة سوداء
     زمن-تشغيل لأي مُرمِّز **موجود** ثم فشل في الفتح (NVENC يرفض فعلاً حين تمتلئ
     الذاكرة)، وسقوط إلى jpegenc عند نفاد الخيارات. وبناء الصورة يفشل إن غاب
     `openh264` — لأنه الأرضية البرمجية تحت H.264 كلّه.
   - **والقيد الذي كان يُعدّ حاجزاً — السياق الآمن — محلول أصلاً**: `mo-pc-remote`
     يشغّل `tailscale serve --bg --https=443`، فيعطي اسم MagicDNS بشهادة **صحيحة**
     (لا تحذير يُقبل مرّة، ولا MSE بزمن استجابة أعلى). أي أن الخيارين المكتوبين هنا
     (أ) و(ب) كلاهما لم يعد لازماً.
   **تصحيح جوهري (2026-07-27): لم يعمل H.264 ولا مرّة واحدة قبل `.393`.** البوابة
   أعلاه كانت تسأل «هل `openh264` مثبَّت؟» والجواب نعم — لكن المثبَّت كان `noopenh264`،
   وهو بوصف حزمته نفسها «تنفيذ زائف لمكتبة OpenH264»، تشحنه فيدورا ليجد فايرفوكس شيئاً
   يرتبط به. الإضافة تُسجَّل، و`ElementFactory.find` تنجح، ثم يموت أول إطار عند
   «Failed to create OpenH264 encoder»، فيسقط البثّ إلى JPEG لبقية عمره بتحذير واحد
   لا يقرأه أحد. أي أن **كل** جلسة على **كل** إصدار كانت JPEG. أُصلح في `86415cd`:
   تُثبَّت openh264 الحقيقية من Cisco (`--allowerasing`، فالاثنتان لا تتعايشان)، وصارت
   البوابة **تُرمِّز إطارين** بدل أن تسأل عن وجود المصنع.

   **تحقُّق حيّ على الخادم بعد إقلاع `.393` (2026-07-27):** `openh264-2.6.0` مثبَّتة
   و`noopenh264` غائبة، والسُّلَّم على هذا الخادم يهبط إلى `openh264enc` بعينه — لا
   NVENC ولا VA-API ولا x264 على VPS، أي أن المُرمِّز الذي كان وهماً هو نفسه المُرمِّز
   الوحيد المتاح هنا. وبأنبوب الوكيل نفسه حرفياً عند 1080p: بلغ `PLAYING`، وأخرج ٦٠
   إطاراً برمز بدء Annex-B (`00000001`) ومفتاح IDR واحد.

   **المتبقّي:** معدّل البِت الفعلي على جوال حقيقي عبر اسم `ts.net`. وما قِيس هنا
   (٨٫٢٦ ميجابت/ث) مقطع SMPTE مدّته ثانيتان يغلب عليه إطار الـIDR — ليس قياس سطح
   مكتب مستقرّ، ولا يُقتبس على أنه كذلك.

6. **حرق المعالج على سطح مكتب سحابي خامل — مفتوح، مقيس، والسبب غير معروف بعد** (2026-07-26).
   على خادم المالك، حسابان بنفس إعدادات الودجات والخلفية **حرفياً**، وسلوكان مختلفان
   تماماً. القياس بفرق نبضات `/proc/<pid>/task/*/stat` على مدى 30 ثانية:

   | | `moalfarras` (مخرج `bochs-drm` حقيقي) | `momo` (‏`kwin --virtual`) |
   |---|---|---|
   | `llvmpipe-0/1` | **~141%** | **0%** |
   | `QSGRenderThread` | 15% | 0% |
   | الخيط الرئيسي | 3% | **~77%** |
   | `kwin_wayland` | 19% | 19% |

   - **(أ) المصدر: خلفية `org.moos.ui2.wallpaper` — مثبَت بـ A/B/A** على الجلسة التي
     تعرض إطارات فعلاً، بعد إقلاع نظيف و17 دقيقة تشغيل، بنافذة قياس 60 ثانية:

     | الخلفية | خيوط الرسم |
     |---|---|
     | `org.moos.ui2.wallpaper` | **162%** |
     | `org.kde.image` (سادة) | **0%** |
     | `org.moos.ui2.wallpaper` مجدداً | **157%** |

     و`AmbientMotion=false` يُنزلها إلى **130%** فقط — أي أن الحركة المحيطة ~30%
     و**الباقي مصدره شيء آخر داخل الخلفية**. لاحظ أن بوابة `DashboardBento` هي
     `root.visible && longDuration > 0` ولا تتضمّن `configuration.AmbientMotion` إطلاقاً.
     ولا يوجد في الخلفية أي `Canvas` أو `ShaderEffect` أو `AnimatedImage` أو `layer.enabled`.
     الخيط الرئيسي 3% فقط، فالرسم يتكرر بلا أن يعيد QML بناء المشهد.

     **أُغلق جزئياً في `.374`:** المصدر هو **لوحة البنتو**، لا الحركة.
     `ShowDashboard=true` تعطي 153% و`false` تعطي 5% — والخلفية تُحمَّل بنجاح في
     الحالتين (تُحقَّق بغياب `Error loading the wallpaper` في السجل، وهو الفحص الذي
     أسقطَ القياس الأول). صار للّوحة مفتاح `ShowDashboard` مطفأ افتراضياً على إصدار
     السحابة وحده. القياس بعد الإقلاع على `.374`: **4%** بدل 157% — أي ~1.5 نواة.
     **ويبقى مفتوحاً:** ما الذي يطلب الإطارات أصلاً؟ إخفاء اللوحة يجعل كل إطار
     رخيصاً، لكنه لا يوقف الطلب — تخفيف لا إصلاح جذري.

     **السؤال الأصلي:** ما الذي يُوسِّخ المشهد كل إطار رغم `AnimationDurationFactor=0`؟
     ولم يُقَس بعد هل `Kirigami.Units.longDuration` يساوي صفراً فعلاً عند ADF=0 — وهي
     العقدة: لو لم يصر صفراً فكل بوابات الحركة في الخلفية بلا أثر.

   - **درس في القياس (كلّفني نتيجة خاطئة):** جلسة أُعيد تشغيل `plasmashell` فيها تظل
     تُحمّل وتُهيّئ عشرات الثواني. قياس بنافذة 25 ثانية بعد إعادة التشغيل مباشرة أعطى
     «الخلفية 44% مقابل السادة 0%» ثم لم يتكرر — واختبار A/B/A أعطى 0% في الحالات الثلاث.
     **لا يُقاس شيء قبل ~70 ثانية استقرار، وبنافذة 60 ثانية، ولا يُصدَّق قياس لا يتكرر.**
     وكل قياس حركة على جلسة `--virtual` بلا مشاهد باطل أصلاً (لا frame callbacks).
   - **(ب) إعادة بناء `ksycoca` كل ~15 ثانية** عند الحساب الافتراضي، طوال مدة التشغيل
     (‏`mtime` يتقدّم ١٥ ثانية في كل فحص)، بكلفة **~٧٩٪ من نواة** على الخيط الرئيسي.
     يبقى بعد إقلاع نظيف على `.374`، والحساب صاحب المقعد **لا يدور** أبداً.

     **المُشغِّل مُثبَت بتتبّع المكدس** (‏`eu-stack`، لا تخمين):

     ```
     QrcaHandler::update()                       ← plasma-nm (أبلت الشبكة)
       KService::serviceByDesktopName("qrca")    ← ماسح QR غير مثبَّت في MoOS
         KSycoca::ensureCacheValid()
           KSycocaPrivate::checkDatabase()
             KBuildSycoca::recreate()            ← إعادة بناء كاملة، صامتة
     ```

     الأبلت موجود عند الحسابين و`qrca` غائب عند الحسابين — ومع ذلك يدور واحد فقط.
     فالسؤال ليس «لماذا يُستدعى الفحص» بل **«لماذا لا يصير الكاش صالحاً بعد بنائه»**.

     **أربع فرضيات جُرِّبت ورُفِضت — لا تُعاد بلا دليل جديد:**
     1. *شرطات مائلة زائدة في `XDG_DATA_DIRS`*: تشغيل `kbuildsycoca6` بالصيغتين يلمس
        **نفس الملف** — KSycoca يطبّع المسارات بنفسه.
     2. *قاعدة `ksycoca6_*` ثانية قديمة تتنازع مع الحيّة*: حُذفت القاعدتان (بتوسيع الحرف
        البدل **كـroot** — توسيعه في صدفة المالك لا يرى بيتاً بصلاحية 0700 ويحذف لا شيء
        بصمت)، فبقيت واحدة نظيفة — **والحلقة استمرّت**.
     3. *ختم زمني في المستقبل على مجلد مراقَب*: لا وجود لأيّ منها.
     4. *`~/.local/share/applications` مفقود*: مفقود عند **الحسابين** بالتساوي.

     **فرضية خامسة — لها آلية تشرح كل ما رُصد، وشُحن إصلاحها (2026-07-26):**
     *مجلد في `XDG_DATA_DIRS` **غير موجود**.* الآلية: `KBuildSycoca` يسجّل كل مجلد
     مورد سُئل عنه، ثم `needsRebuild()` يعمل `stat` على كل مسار مسجَّل؛ مسارٌ غير
     موجود **لا يطابق ختمه المسجَّل أبداً**، فالجواب «أعد البناء» إلى الأبد. وهذا
     يفسّر السؤال الحقيقي أعلاه — لماذا لا يصير الكاش صالحاً بعد بنائه — لا لماذا
     يُستدعى الفحص.
     ويفسّر **اللاتماثل** أيضاً: مولّد بيئة systemd الخاص بـFlatpak يضع
     `~/.local/share/flatpak/exports/share` في `XDG_DATA_DIRS` لكل حساب، والمجلد
     لا يُنشأ إلا بعد أن يثبّت ذلك الحساب شيئاً بـflatpak فعلاً. صاحب المقعد ثبّت؛
     المطوّر الثاني المُنشأ حديثاً لم يثبّت.
     **ما شُحن:** `moos-ui-migrate` صار ينشئ `applications` و`icons`
     و`flatpak/exports/share/applications` **قبل** مخرج `--input-only` المبكر — أي
     في الاستدعاء الوحيد الذي يسبق KWin.
     **وعيب ثانٍ اكتُشف في الطريق وهو مؤكَّد لا مُفترَض:** جلسة المستخدم الثاني
     تشغّل `kwin_wayland` **يدوياً** من `moos-own-screen.service`، فلا ترث أياً من
     drop-ins الموضوعة على `plasma-kwin_wayland.service`. أي أن **كل** إصلاحات
     ما-قبل-KWin كانت تُتخطّى عند ذلك الحساب وحده (Num Lock، ترحيل لوحة المفاتيح
     القديم، ومجلدات XDG أعلاه). أُضيف `ExecStartPre=-/usr/bin/moos-ui-migrate
     --input-only`، وبوابتان في `verify_user_experience.py` تحرسان الاثنين
     (جُرِّبتا بالكسر: تفشلان فعلاً).
     **قِيس على الخادم نفسه (2026-07-27) — والفرضية الخامسة رُفضت هي الأخرى:**

     أولاً، الحلقة **حقيقية ومؤكَّدة**، لا استنتاج: ختم `ksycoca` عند momo تقدّم
     ٢٠ ثانية داخل نافذة ٢٠ ثانية، وعند moalfarras **لم يتحرّك إطلاقاً**. والمعالج:
     **٨٧٪ من نواة** عند momo مقابل **٤٪** عند moalfarras. والمكدس الحيّ
     (‏`eu-stack`، ٢٥ عيّنة) أعاد نفس السلسلة المسجَّلة أعلاه حرفياً، ومعها
     `QQuickGridView` يعيد بناء مندوبيه و`ShellCorona::checkAddPanelAction()` يمسح
     بيانات كل إضافة — وهما **نتيجة** إعادة البناء لا سببها.

     ثم جُرِّبت الفرضيات واحدةً واحدة على الجهاز الحيّ، وكلها سقطت:
     - *إنشاء المجلدات الناقصة* (`applications`, `icons`): الحلقة **استمرّت**.
       وتبيّن أن `flatpak/exports/share` **موجود عند الحسابين**، فلا فرق أصلاً.
     - *الشرطات المائلة الزائدة*: momo كان عنده `/usr/local/share/` و`/usr/share/`
       بينما moalfarras بلا شرطة. أُزيلت وأُعيد تشغيل الجلسة كاملة — **استمرّت**.
       ويوجد ملف قاعدة **واحد** لا اثنان، أي أن Qt يطبّع الشرطات فعلاً (تأكيد مستقل
       لما رُفض سابقاً).
     - *بناء نظيف للقاعدة وplasmashell متوقّف*: القاعدة **ثابتة** بلا plasmashell،
       فالمحرّك هو plasmashell لا kwin. ومع ذلك عادت الحلقة عند تشغيله.
     - *تعارض القفل*: ملف `.lock` يتناوب عليه `kwin_wayland` و`plasmashell` عند
       momo، ولا وجود له إطلاقاً عند moalfarras (٠ من ١٥ عيّنة).
     - ولا مجلد مراقَب يتغيّر ختمه، ولا إشارة `notifyDatabaseChanged` واحدة على
       ناقل أيّ من الحسابين خلال ١٢ ثانية.

     **الحالة الآن: الحلقة اختفت — ولا أدّعي سبباً.** بعد سلسلة إعادات التشغيل
     صار المعالج **٨٪** والختم **لا يتحرّك**، وبقي كذلك عبر أربع إعادات تشغيل
     للجلسة. والأهم: **أُعيد كل تغيير على حدة ولم تعد الحلقة مع أيٍّ منها** — لا
     مع حذف المجلدات، ولا مع إزالة تثبيت `XDG_DATA_DIRS`، ولا مع حذف القاعدة أثناء
     التشغيل. فالإسناد إلى تغيير واحد **غير ممكن**، والادّعاء به سيكون كذباً
     مريحاً. الأرجح أنها حالة عابرة من طريقة تركيب الجلسة الأصلية زالت بإقلاع
     نظيف كامل.

     **ما شُحن رغم ذلك، ولماذا هو صحيح على أي حال:** المجلدات تُنشأ (يجب أن توجد)،
     والجلسة الخاصة صارت تشغّل `moos-ui-migrate` مثل أي جلسة أخرى (عيب مؤكَّد
     مستقل). و`sudo moos-cloud-desktop doctor` يكشف عودة الحلقة في **٨ ثوانٍ**
     بقياس الختم — وهو الشيء الوحيد الذي كان ينقص طوال هذا التحقيق.

     ### الحلقة **عادت** بعد إقلاع نظيف على `.388` — والآلية صارت معروفة

     أول تشغيل لـ`doctor` على الصورة الجديدة كشفها فوراً: **١١–١٢ إعادة بناء كل ٢٥
     ثانية، و٧٨–٩١٪ من نواة** عند momo، و**صفر** عند moalfarras. أي أن اختفاءها
     سابقاً كان حالة عابرة كما قيل، لا إصلاحاً — والتحفّظ عن ادّعاء السبب كان في محلّه.

     **وبتشغيل `QT_LOGGING_RULES="kf.service.sycoca.debug=true"` قالت KSycoca الآلية
     بنفسها**، وهي حلقة **تُغذّي ذاتها داخل plasmashell**:

     ```
     Reusing existing ksycoca
     Recreating ksycoca file (…, version 307)
     QThread(name = "krunner_services") got a notifyDatabaseChanged signal
     Opening ksycoca from …
     Reusing existing ksycoca
     Recreating ksycoca file (…)          ← وهكذا بلا نهاية
     ```

     إعادة البناء تُطلق `notifyDatabaseChanged`، فيستيقظ خيط `krunner_services`
     (وخيط Qt الرئيسي) ويعيد الاستعلام عن الخدمات، فيستدعي `ensureCacheValid`،
     فتُعاد البناء. **الإشارة الناتجة عن البناء هي التي تطلب البناء.**

     **فرضيتان إضافيتان جُرّبتا حيّاً على الحلقة الحيّة ورُفضتا** (فالمجموع ثمانٍ):
     - *`qrca` غير موجود فتُعاد المحاولة*: زُرع `qrca.desktop` فعلاً عند momo —
       **١٢ إعادة بناء و٩١٪** كما هي. منطقي: إعادة البناء تسبق البحث لا تتبعه.
     - *أبلت plasma-nm هو المُطلِق الوحيد*: حُذف الأبلت من لوحة momo وأُعيد تشغيل
       plasmashell — **١٠ إعادات و٧٨٪**. أي أن `QrcaHandler` مجرّد **أحد** المستدعين.

     **الخلاصة الصادقة:** هذا عيب KDE أعلى المكدس يظهر في جلسة `--virtual` بلا مقعد.
     مُشخَّص بدقة، ولا إصلاح جذري في متناول هذه الصورة. والأثر محصور في **الحساب
     الثاني على إصدار السحابة وحده**. `doctor` يكشفه في ٨ ثوانٍ، وهذا هو المتاح.

     **قياس على `.393` بعد إقلاع نظيف (2026-07-27، نافذة ٦٠ ثانية):** الحلقة
     **غائبة**. ختم `ksycoca` لم يتقدّم ثانيةً واحدة عند أيٍّ من الحسابين، و`plasmashell`
     عند momo عند **٥٪** من نواة مقابل ٧٨–٩١٪ على `.388`، وعند moalfarras ٨٪.
     **ولا أدّعي إصلاحاً:** هذه الحلقة سبق أن غابت مرّة ثم عادت بعد إقلاع نظيف على
     `.388`، ولا شيء في `.393` يستهدفها. تُسجَّل كقياس لا كإغلاق، ويبقى `doctor`
     هو الكاشف إن عادت. (ولاحظ أن فحص `doctor` نافذته ٨ ثوانٍ — كافية لحلقة تعيد
     البناء كل ١٥ ثانية، ولا تُغني عن نافذة ٦٠ ثانية عند الشك.)

   **فرضيتان جُرّبتا ورُفضتا — لا تُعاد تجربتهما بلا دليل جديد:**
   - *الشرطات المائلة الزائدة في `XDG_DATA_DIRS`* (‏`/usr/share/` مقابل `/usr/share`):
     تشغيل `kbuildsycoca6` بالصيغتين **يلمس نفس الملف** — KSycoca يطبّع المسارات بنفسه.
     الإصلاح المبني على هذا أُرجع ولم يُشحن.
   - *`AnimationDurationFactor` هو السبب*: قياس سابق على `momo` أظهر «لا فرق»، وهو
     **قياس باطل** — المُصرِّف الافتراضي بلا مشاهد لا يرسل frame callbacks أصلاً، فحركاته
     تعمل منطقياً ولا تُعرض أبداً. أي قياس حركة على جلسة افتراضية بلا مشاهد لا يعني شيئاً.

   **تصحيح لادّعاء سابق:** حدّ `LP_NUM_THREADS=2` **يعيد توزيع** العمل ولا يقلّله
   (‏٢ × ~68% = نفس ٨ × ~18%). يبقى سقفاً صحيحاً على آلة بنواتين، لكنه ليس علاجاً للحرق.

7. **صوت المتصفح على إصدار السحابة — الطريق مُثبَت، والتنفيذ متبقٍّ** (2026-07-26).

   `moos-cloud-audio` شُحن معتمداً على `ffmpeg -c:a libopus`، وهذا **خطأ**: الخادم
   العامل يحمل ffmpeg 8.1.2 من RPMFusion **مع** libopus، أما **الصورة** فلا RPMFusion
   فيها — و`ffmpeg` فيها هو `ffmpeg-free` **بلا libopus إطلاقاً**. أي أن المُرمِّز الذي
   قِيس عليه ليس المُرمِّز الذي يُشحن. وكانت البوابة تُفشل البناء، فتوقّف نشر إصدار
   السحابة **أربعة كوميتات** (`moos` و`moos-nvidia` على .379 و`moos-cloud` على .375)،
   ومنها كوميتان وُجدا لإصلاح إصدار السحابة نفسه. صارت البوابة **ملاحظة** لا فشلاً،
   والخدمة تفشل ظاهرةً في `systemctl --failed` بدل تجميد الإصدار.

   **البديل مُثبَت حياً على الخادم** — GStreamer موجود في الصورة أصلاً (لا تبعية جديدة
   ولا نمو في الحجم)، وهذا الأنبوب أنتج ملفاً صالحاً (`ffprobe`: `opus,48000`):

   ```
   gst-launch-1.0 pulsesrc device=@DEFAULT_MONITOR@ \
     ! audioconvert ! audioresample \
     ! opusenc bitrate=96000 ! webmmux streamable=true ! ...
   ```

   **أُنجِز (2026-07-26).** `moos-cloud-audio` أُعيدت كتابته بالكامل على GStreamer +
   خادم HTTP بايثون صغير، وذهب ffmpeg من المسار تماماً:

   - **الوسائط تسير على fd خاص لا على stdout.** `gst-launch` يطبع
     «`Setting pipeline to PAUSED …`» على stdout، وهذه البايتات داخل ترويسة WebM
     تُنتج بثاً لا يعمل ولا يشتكي أحد. الأنبوب يكتب إلى `fdsink fd={out_fd}` —
     أنبوب موروث عبر `pass_fds` — وstdout يذهب إلى `DEVNULL`.
   - **قناتان مفروضتان بعد `audioconvert`** (`audio/x-raw,rate=48000,channels=2`):
     مراقب المصرف الصامت أحاديّ القناة، فبدون هذا يُطوى كل صوت النظام إلى مونو.
     وموضع الـcaps مهم — قبل `audioconvert` تقيّد المصدر بدل أن تطلب الرفع.
   - **تكلفة الخمول صفر حرفياً**: لا يُشغَّل مُرمِّز إلا عند طلب `/stream.webm`،
     ويُقتل عند انصراف المستمع (‏`killpg` على مجموعة عمليات خاصة).
   - **`TCP_USER_TIMEOUT=20s`**، وليس keepalive وحده: نبضات keepalive لا تُطلق إلا
     على وصلة **خاملة**، وبثٌّ يكتب ١٢ كيلوبايت في الثانية ليس خاملاً أبداً — فبدونه
     يبقى المُرمِّز يعمل لجوّالٍ خرج من التغطية حتى تستسلم إعادة الإرسال (~١٥ دقيقة).
   - **صفحة هبوط عربية RTL** على `/` بزر تشغيل (المتصفّحات تمنع التشغيل التلقائي
     للصوت، فالزر ضرورة لا زينة) وإعادة اتصال تلقائية، بلا أي أصل خارجي — التبويب
     يُفتح عبر tailnet لا طريق فيه إلى CDN.
   - **سقف ٤ مستمعين** بردّ 503: وصلة ميتة لا تحجز المقعد الوحيد.

   **والبوابة صارت تسأل الصورة لا الصدفة:** `build.sh` يتحقق من وجود
   `pulsesrc/audioconvert/audioresample/opusenc/webmmux/fdsink` **داخل الصورة** عبر
   `Gst.ElementFactory.find`، ويحرس الـfd الخاص وفرض القناتين. وملف اختبار جديد
   `tests/test_moos_cloud_audio.py` (١١ اختباراً) يشغّل الخادم فعلياً ببديل
   `gst-launch-1.0` ويثبت: البايتات تصل نظيفة، المُرمِّز يُولد عند الطلب ويُحصد عند
   القطع، والسقف يردّ 503. وأُضيف إلى `just check` وإلى CI — و**اكتُشف في الطريق أن
   `test_moai_credential_store.py` لم يكن يعمل في CI إطلاقاً** رغم وجوده في
   `just check`؛ أُضيف.

   **درسان يستحقان البقاء:** (١) لا تُقَس ميزة على حزمة الخادم العامل ثم تُشحن — الصورة
   بيئة أخرى. (٢) بوابة صحيحة على ميزة ثانوية لا يجوز أن تُجمّد إصداراً كاملاً؛ افشل
   الخدمة لا الصورة.

8. **شاشة المستخدم الثاني: من دورة مفرغة إلى شاشة تعمل — مُغلَق** (2026-07-27).

   كان البند مفتوحاً بصياغة «إدخال المستخدم الثاني محجوب بصلاحية بوابة لا يمكن
   منحها». تبيّن أن العطل **ثلاثة أعطال متتالية** لا واحد، وأن الشاشة لم تعرض شيئاً
   أصلاً — لا أنها «تعرض ولا تقبل إدخالاً» كما رُجِّح.

   **العطل الأول — الصلاحية.** بوابة RemoteDesktop تطلب نقرة، والجلسة بلا إنسان،
   والطريقة الوحيدة للنقر هي Mo PC Remote الذي يحتاج الصلاحية نفسها. الخروج من
   الدورة: المنحة العاملة عند `moalfarras` **لا تحمل أي شيء خاص بجلسة أو شاشة** —
   قراءتها من مخزن الصلاحيات تعطي `{clipboardEnabled:false, devices:3,
   screenShareEnabled:true}` فقط — فهي تُبنى وتُكتب مسبقاً. صار ذلك في
   `moos-cloud-desktop own` (‏`seed_portal_grant`).

   **تصحيح لادّعاء في هذه الوثيقة:** قيل هنا إن Mo PC Remote «لا يطلب الاستمرارية
   أصلاً» لأن `settings.json` عند `moalfarras` خالٍ من أي رمز. البحث كان في المكان
   الخطأ: `mo-remote-portal.py` يضبط `persist_mode=2` ويحفظ الرمز في
   `~/.config/MoRemote/portal-restore-token`، **والرمز نفسه هو معرّف المدخل** في مخزن
   الصلاحيات.

   ومَن يولّد الرمز عليه أن يولّده بصيغة `xdp_generate_token()` بالضبط: base64 قياسي
   مع إبدال `+` و`/` بـ`_` وحذف الحشو — لأن `xdp_is_valid_token()` تشترط أن يكون كل
   حرف صالحاً في اسم D-Bus. ‏`urlsafe_b64encode` في بايثون يستعمل `-`، فتردّ البوابة
   `Restore token is not valid` وتبدو المشكلة في المنحة لا في الرمز.

   **العطل الثاني — لا OpenGL فلا بثّ.** ‏KWin يرفض أي بثّ لجلسة لا تؤلّف بـOpenGL
   (`screencastmanager.cpp`)، وخلفية `virtual` لا تعرض OpenGL إلا إذا وجدت جهاز DRM.
   وخادم VPS يملك `bochs-drm` وحده بلا عقدة render. الحل: تحميل **vgem** — الجهاز
   الذي تستثنيه شيفرة KWin بالاسم فتفتح عقدته الأساسية — مع إجبار Mesa على المسار
   البرمجي، لأنها تجرّب `zink` وتفشل على خادم بلا Vulkan فيسقط KWin إلى QPainter بصمت.
   قاعدة udev محصورة في جهاز vgem وحده؛ ‏`/dev/dri/card0` يحتفظ بـACL مقعده فلا يضعف
   العزل بين الحسابين.

   **تصحيح للمسار (ب) — كان خطأً في مكان البحث لا في التطبيق** (2026-07-26):
   قيل هنا إن Mo PC Remote «لا يطلب الاستمرارية أصلاً» استناداً إلى غياب رمز في
   `settings.json`. الكود يقول العكس: `mo-remote-portal.py` يمرّر
   `persist_mode = 2` في `SelectDevices`، ويقرأ `restore_token` قبل الجلسة ويحفظ
   العائد بعدها — لكن في `~/.config/MoRemote/portal-restore-token` (بصلاحية 0600)،
   **لا** في `settings.json`. أي أن الاستمرارية مُنفَّذة، والسؤال الوحيد الباقي هو
   منح الموافقة **مرّة واحدة**؛ بعدها لا تظهر نافذة أبداً.

   **مسارا الحل الباقيان (بلا تفضيل قبل القياس):** (أ) بذر مدخل الصلاحية على القرص عند
   إنشاء الحساب في `moos-cloud-desktop own` — يحتاج معرفة شكل المفتاح الذي تبحث عنه
   البوابة، والمعرّف عند `moalfarras` يبدو رمزاً عشوائياً لا معرّف تطبيق ثابتاً.
   (ج) منح الصلاحية مرة واحدة من جلسة لها مقعد ثم نسخها. ولاحظ **كيف** حصل عليها
   صاحب المقعد أصلاً: جلسته على مخرج `bochs-drm` حقيقي، أي **لها مقعد**، فـ`ydotoold`
   يعمل عنده ويستطيع النقر على النافذة — وهذا بالضبط ما لا يملكه المستخدم الثاني.

   `flatpak permission-set remote-desktop …` جُرِّب ولم يُنشئ مدخلاً — الأمر لا يفشل بصوت
   مسموع، لكن `permission-show` يبقى فارغاً. (فرضية غير مُختبَرة تستحق النظر أولاً:
   `xdg-permission-store` نفسه غير عامل/غير قابل للتفعيل على ناقل momo، وعندها يصمت
   الأمر ويبقى العرض فارغاً — وهو ما يطابق ما رُصد تماماً.)

   **حُلَّ (2026-07-27) — بقراءة منحة عاملة بدل التخمين على شكلها.**

   المنحة الحقيقية عند `moalfarras`، كما تقرأها واجهة مخزن الصلاحيات نفسها:

   | الحقل | القيمة |
   |---|---|
   | table | `remote-desktop` |
   | id | `tan02ApTgIYasWH2ZZPc1w` ← **رمز الاستعادة**، عشوائي لكل منحة |
   | app-id | `""` — فارغ: Mo PC Remote ليس في صندوق رملي |
   | permission | `yes` |
   | data | `('KDE', uint32 1, <bytes>)` — حالة جلسة KDE: حافظة، أجهزة، مشاركة شاشة |

   و`id` هو **كل** الحكاية، وهو ما يفسّر لماذا بدا `flatpak permission-set` وكأنه
   لا يفعل شيئاً: هو **كتب** مدخلاً فعلاً — تحت المفتاح `moos-pc-remote` وببيانات
   **فارغة**. والبوابة لا تبحث إلا عن المفتاح الذي يسلّمه التطبيق في
   `restore_token`، فمدخلٌ باسم آخر لا يُقرأ أبداً. أي أن «`permission-show` يبقى
   فارغاً» لم يكن المشكلة إطلاقاً. (وقد وُجد مدخل `moos-pc-remote` ببيانات فارغة
   عند momo فعلاً — أثر محاولة سابقة.)

   فالمنحة **نصفان يجب أن يتطابقا**، وبذر أحدهما وحده هو ما جعل هذا يبدو مستحيلاً:
   (١) مدخل في المخزن مفتاحه الرمز وبداخله بيانات KDE، و(٢) نفس الرمز في
   `~/.config/MoRemote/portal-restore-token`.

   **`sudo moos-cloud-desktop grant-input <user> [donor]`** ينسخ الاثنين معاً من
   حساب **له مقعد** (وهو تحديداً ما مكّنه من النقر على نافذته). لا شيء يعبر بين
   المستخدمين: الكتلة تحمل أعلام أجهزة لا هوية، وكل رمز يهبط في مخزن صاحبه وبيته.
   والقراءة (`dump`) والكتابة (`apply`) خطوتان لأن ناقل المستخدم بصلاحية 0700
   و`dbus-broker` يوثّق uid الطرف — فلا عملية واحدة تصل الناقلين.
   **نصف مُختبَر حياً:** `dump` نجح على الخادم وأخرج المنحة الصحيحة. `apply` كتابةٌ
   إلى حساب آخر ولم تُنفَّذ من هنا.

   **وعُثر في الطريق على عيب أهمّ للمستخدم الثاني، وأُصلح وأُثبت:** لم يكن له
   **عنوان HTTPS إطلاقاً**. `mo-pc-remote` يعطي صاحب المقعد `tailscale serve
   --https=443`، أما سطح `own` فلا شيء — و`canDecodeH264()` تشترط
   `isSecureContext`، فكان بثّه يسقط بصمت إلى JPEG: **~٧٩ ميجابت/ث** عند 1080p بدل
   **~٤٫٣**. والفخّ: `--https=${port}` يبدو بديهياً وهو خطأ — الوكيل يحتلّ المنفذ
   أصلاً على كل الواجهات، فـserve لا يحصل عليه ويردّ العنوان **بنص صريح**
   (‏`curl` قال `wrong version number`). صار المنفذ `8443+N`، ومُثبت حياً:
   `https://…ts.net:8443/` يردّ 200 بـ`ssl_verify_result=0` وشاشة PIN تظهر.

   **وبعد كل ذلك بقيت الشاشة سوداء — العطل الثالث: مؤلّفان، والكاميرا على الفارغ
   منهما.** كان الأمر يشغّل
   `kwin_wayland --virtual … --exit-with-session=/usr/bin/startplasma-wayland`، لكن
   `startplasma-wayland` يبدأ جلسة Plasma المُدارة بـsystemd، وأول وحداتها
   `plasma-kwin_wayland.service` — **مؤلّف آخر** جاء متداخلاً داخل مؤلّفنا. فصار على
   الجهاز مؤلّف خارجي فارغ ومؤلّف داخلي يحمل سطح المكتب، والبوابة تلتقط الخارجي وتبثّ
   إطاراً **أسود** ١٩٢٠×١٠٨٠ بمعدّل كامل: الإطارات تتدفّق، والإدخال «جاهز»، والصورة
   لا شيء. لم يفشل شيء، فلم يقل شيء. اكتُشف بفكّ ترميز إطار من السلك والنظر إليه.
   الإصلاح: لا مؤلّف لنا إطلاقاً — إضافة drop-in تجعل مؤلّف Plasma نفسه افتراضياً،
   و`kwin_wayland_wrapper` يمرّر الوسائط كما هي (وبقاؤه ضروري لأنه هو ما ينشر
   `WAYLAND_DISPLAY` في بيئة systemd).

   **الحالة المقيسة بعد الإصلاح (2026-07-27):** مؤلّف واحد، `Portal ready … 1920x1080`،
   ‏`input.ready = true` عبر `KDE RemoteDesktop portal (absolute)`، وإطار حقيقي فيه
   سطح مكتب MoOS ورصيفه. تصمد عبر إعادة تشغيل الخدمة وإعادة بناء الجلسة من الصفر.
   الحارس: `tests/test_cloud_private_desktop.py`.

   **الإقلاع الكامل — تمّ وقِيس** (2026-07-27، الخادم على `.393`، إقلاع 13:22).
   الإصلاح يصمد بعد إقلاع بارد، لا بعد إعادة تشغيل خدمة فقط:
   - مؤلّف **واحد** عند momo (`kwin_wayland_wrapper --virtual --width 1920
     --height 1080`) بلا أي مؤلّف متداخل — العطل الثالث لم يعد.
   - `Compositing Type: OpenGL` عبر llvmpipe عند **الحسابين**، وإضافة `screencast`
     محمّلة. أي أن vgem ومسار Mesa البرمجي يصمدان عبر الإقلاع، وهو الشرط الذي بدونه
     يرفض KWin البثّ أصلاً.
   - المنحة المبذورة نجت **بنصفيها متطابقين**: الرمز في
     `~/.config/MoRemote/portal-restore-token` عند momo هو `2_YamnG2rZYpDY2cT2S2Eg`،
     وهو **نفسه** المفتاح الوحيد في جدول `remote-desktop` بمخزن صلاحياته — بصيغة
     `xdp_generate_token()` (شرطة سفلية لا `-`)، ولا أثر لمدخل `moos-pc-remote`
     الفارغ القديم. أي أن `seed_portal_grant` مُثبَت من طرفه إلى طرفه.
   - `https://…ts.net:8443` يردّ 200 بـ`ssl_verify_result=0`، فالسياق آمن
     و`canDecodeH264()` تمرّ — وقد صار خلفها مُرمِّز حقيقي (البند ٥).

   **ما لم يُختبر بعد:** جلسة فعلية من جهاز المستخدم الثاني بحسابه هو على Tailscale
   — وهي الوحيدة الباقية التي تحتاج إنساناً وجهازاً، ولا تُقاس من الخادم.

   **ملاحظة على `grant-input`:** بقي في مكانه ككاتب منحة من حساب متبرّع، لكن
   `moos-cloud-desktop own` صار **يبني** المنحة من الصفر (`seed_portal_grant`) —
   بايتات مطابقة لما تقرأه من منحة عاملة، ورمز مسكوك بصيغة `xdp_generate_token()`
   نفسها — فلم يعد إعداد المستخدم الثاني يحتاج حساباً متبرّعاً إطلاقاً.

9. **نظام الثيم: ما كان مركّباً فوق KDE، وبوابة الحركة التي لم تُغلق ولا مرة —
   مُغلَق** (2026-07-27).

   شكّ المالك كان في محلّه، والواقع أسوأ من الشكّ: **سطح المكتب الحيّ كان يعرض شاشة
   دخول KDE لا MoOS**. الملف `/etc/plasmalogin.conf` يحمل
   `WallpaperPluginId=org.kde.hunyango`، وهو يتفوّق على كل ما تشحنه MoOS. والأسوأ أن
   MoOS لم تكن في السباق أصلاً: وضعت رأيها في `plasmalogin.conf.d/`، وقياسٌ على ثنائيّ
   الـgreeter (‏`KConfig::addConfigSources`، والاستدعاء الأسبق يفوز) يعطي الترتيب:

   | # | الطبقة | المالك |
   |---|---|---|
   | 1 | `/etc/plasmalogin.conf` | المدير — وهنا تكتب لوحة KCM |
   | 2 | `/etc/plasmalogin.conf.d/*` | المدير |
   | 3 | `/usr/lib/plasmalogin/defaults.conf` | **الصورة — أقوى ما تملكه** |
   | 4 | `/usr/lib/plasmalogin/plasmalogin.conf.d/*` | الأضعف — وهنا كانت MoOS |

   والوثائق المثبَّتة لـplasma-login-manager (سطر 58) تسمّي شقّاً واحداً فقط:
   `defaults.conf`. وMoOS لم تكن تلمسه، فبقي فيه ما كتبته Fedora:
   `Image=file:///usr/share/wallpapers/Fedora/` — **مجلّد يحذفه هذا البناء**. مفتاح ميت
   ومسار معلّق، فوق MoOS. الصورة تملك الملف الآن.

   **وتصحيح منهجي يستحق البقاء:** استُنتج أثناء التدقيق أن كتابة MoOS فوق مكوّنات
   `org.kde.breeze` «لا تصل شاشة الدخول إطلاقاً» لأن
   `strings … | grep -i breeze` تعطي صفراً. الاستنتاج خاطئ والأداة هي السبب: Qt يخزّن
   `QStringLiteral` بترميز UTF-16 و`strings` العادي يتخطّاه. و`strings -el` يُظهر
   `org.kde.breeze.components` و`BreezeComponents.Clock/.ActionButton/.Battery/`
   `.RejectPasswordAnimation`. أي أن ملفات MoOS **تصل فعلاً**، وما لا يُطال هو التخطيط
   نفسه ومنتقي الجلسة وزرّ لوحة المفاتيح (مُصرَّفة داخل مورد Qt).

   **بوابة الحركة كانت مكسورة بفارق واحد، في كل مكان، منذ كُتبت.**

   كل بوابات MoOS تقرأ `Kirigami.Units.longDuration > 0` بناءً على اعتقاد أن Plasma
   يُنزل القيمة إلى صفر عند إطفاء الحركات. **هو يحدّها عند ١.** مقيس على Qt 6.11 /
   Kirigami 6.28 لهذا الجهاز، ثلاث نقاط:

   ```
   AnimationDurationFactor=1    ->  longDuration = 200
   AnimationDurationFactor=0.5  ->  longDuration = 100
   AnimationDurationFactor=0    ->  longDuration = 1     <- لا صفر
   ```

   فالتعبير صحيح والحركات مُطفأة تماماً، ولم يتوقّف شيء قطّ: لا خلفية الـ4K، ولا حلقات
   الـbento الثماني، ولا ساعة البطل، ولا شاشة البدء (ثمانٍ)، ولا شاشة القفل (ستّ،
   بقياس **٤٪ من نواة باستمرار** على سطح يبقى ساعات). وKDE نفسها تقول `> 1` في
   `BusyIndicator.qml` الثلاثة، و`RejectPasswordPathAnimation` يكتب `<= 1`. والأسوأ أن
   `tests/verify_user_experience.py` كان **يُلزم** الصيغة المكسورة بنصّها الحرفي — البوابة
   كانت تُمسك العطل في مكانه. الآن `tests/test_moos_motion_gate.py` يُثبت الأمر في محرّك
   QML حقيقي بدل grep، والبوابة النصّية **ترفض** `> 0` في تعريف حركة.

   **والثيم صار يحمل خلفيته.** لم تكن ولا واحدة من الـ16 `contents/defaults` تعلن
   `[Wallpaper] Image`، فاختيار ثيم MoOS من إعدادات KDE نفسها يعطي ألوان MoOS على خلفية
   شخص آخر — المظهر الكامل كان يعيش داخل 2,662 سطر bash لا تعرفها إعدادات النظام. حُذف
   المفتاح أصلاً اعتقاداً أن `LookAndFeelManager` سيفرض `org.kde.image` ويمحو المشهد؛
   و`KLookAndFeelManager::save()` في 6.7.3 **لا يقرأ تلك المجموعة إطلاقاً** (مُتحقَّق في
   المكتبة المشحونة). القيمة **اسم حزمة مجرّد** لا مسار، لأن المُحلِّل يستدعي
   `QStandardPaths::locate(GenericDataLocation, "wallpapers/<Image>")`.

   **أعطال أخرى، كلٌّ منها وُجد على الجهاز العامل:**

   - **خلفية شاشة الطاقة لم تُرسم قطّ.** الـ`OpacityAnimator` يستعمل `target: parent`،
     و`parent` داخل Animator يساوي `null` — فبقيت الطبقة على شفافية ٠ للأبد بينما
     حركتان لانهائيتان ترسمان مستطيلات ١٣٣٩×٥٧٦٠ لا يراها أحد.
   - **نقرة على خلفية اللوحة تُلغي الإطفاء.** الحاجز المفترض كان
     `acceptedButtons: Qt.NoButton` — وهي تجعل الـMouseArea ترفض كل زر فتنفذ النقرة
     من خلالها. و`auroraTint()` كانت دالّة **هوية رياضية** تُرجع دخلها، موجودة فقط
     لإرضاء اختبار.
   - **`moos-theme motion still|gentle|alive` مسرحية**: `MotionMode` تكتبه الأداة، غير
     معرَّف في أي مخطط، لا يقرأه أي QML — فـ`gentle` و`alive` سطحان متطابقان، و`still`
     لا يوقف الـbento إطلاقاً. صار المفتاح حقيقياً، بقيمة حارسة `-1` كي لا يُعيد ترقية
     جهاز أطفأ الحركة أصلاً.
   - **إعادة التشغيل ٧٨ ثانية.** drop-in تابع لـMoOS يعطي plasmashell
     `TimeoutStopSec=4min` **متداخلة داخل** نافذة ٦٠ ثانية لـ`user@.service` مع
     `TimeoutStopFailureMode=abort`: نافذة الابن لا يمكن أن تنطلق، فيُجهض systemd مدير
     المستخدم ويقتل الجلسة بينما KWin يمسك DRM ويبقى السطح المتجمّد على الشاشة. أربع
     من آخر خمس إطفاءات؛ الخامسة استغرقت ٣٫٥ ثانية. صارت ٢٠ ثانية بوضع فشل `kill`.
   - **٤٫٥ ثانية سواد** بين شاشة البدء وأول إطار للسطح (`plymouth quit` بلا
     `--retain-splash`؛ مقيس 16:52:24.269 ← 28.728)، وأرضية Plymouth كانت ما زالت
     كحليّة Nova أمام سطح غرافيتي.
   - **المشغّل يطلب `IBM Plex Mono` على صورة لا تثبّته إطلاقاً**، فيردّ `fc-match`
     بـ**Noto Sans Arabic** — خط عربي متناسب مكان خط أحادي المسافة. صار JetBrains Mono،
     وهو ما يسمّيه عقد التصميم أصلاً.
   - **الخلفيات الجديدة لم تكن تُشحن.** المولّد أُعيد توجيهه إلى أصول «flow» ولم
     يُشغَّل، فالفنّ المشحون بقي «frost» والمستودع يدّعي غير ذلك. ٤٤ صورة أُعيد توليدها.
   - **`PROJECT_STATE.md` كان يخبر الوكيل التالي** أن `moos-theme ui1-dark|ui1-light` هو
     مسار التراجع المدعوم. الأمر **غير موجود** وUI1 أُزيل من الصورة؛ وكيل يثق بالسطر
     ينفّذ تراجعاً وهمياً على جهاز المالك اليومي.

   **وبوابات أُعيدت كتابتها من ثابت إلى علاقة** — وهو الدرس الذي علّمته أربع بوابات
   بائتة في هذه الحملة وحدها: نافذة ١٤٠٠ محرف سحرية، لون كحليّ مثبَّت بنصّه، قائمة
   «أربعة من سبعة» شرائح، وثلاثة تأكيدات بنصوص حرفية عاشت أطول من ثوابتها.

   **ما لم يُنفَّذ عمداً:** شاشة القفل ما زالت تثبّت خلفيتها بدل أن تتبع الثيم. المسار
   الاحتياطي موجود، لكن إثباته يحتاج جهاز اختبار أو قفل هذه الجلسة، ووضع الفشل **شاشة
   قفل سوداء** على سطح يُرى يومياً. ولا يُطال أصلاً، وقد وُثِّق كي لا يُعاد بحثه: تخطيط
   الـgreeter (مُصرَّف داخل مورد Qt)، وثيم GRUB (‏bootupd يملك `grub.cfg` ولا يقرأه
   إطلاقاً — وثلاثة أسباب مستقلّة كلٌّ منها كافٍ)، و٢٫٤ ثانية فحص nvidia-drm المتأخّر.

## قواعد العمل المتزامن

- افحص `git status --short` و`git diff --name-only` قبل كل دفعة.
- لا تعدّل ملفاً عليه تغييرات من وكيل آخر قبل مراجعة الفرق والتنسيق.
- لا تستخدم أسماء ملفات جديدة تحت `system_files/` قبل البحث عن سطح قائم مماثل.
- كل إصلاح مرئي يجب أن يضيف اختباراً أو حارساً يمنع رجوع المشكلة.
- لا commit أو push أو تغيير صورة الجهاز المثبت إلا بطلب واضح من مالك المشروع.
