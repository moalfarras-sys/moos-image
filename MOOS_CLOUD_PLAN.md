# MoOS Cloud — نظام MoOS يعمل ٧/٢٤ على السحابة بلا جهاز حقيقي

> الهدف: نفس MoOS الحيّ الذي على المكتب — نفس الواجهة، نفس الثيم، نفس Mo AI،
> نفس قطار التحديثات الموقّعة — لكن على خادم يعمل دائماً، للتطوير وتشغيل Mo AI
> ومشاريعك الخاصة. لا ألعاب. أرخص وأفضل طريقة ممكنة.

---

## الخلاصة أولاً — التوصية

**إصدار ثالث في نفس المستودع اسمه `moos-cloud`، يُثبَّت على VPS رخيص بأمر واحد.**

| السؤال | الجواب |
|---|---|
| ريبو جديد؟ | **لا.** إصدار ثالث في `moos-image` نفسه، بنفس البوابات ومفتاح التوقيع. |
| كيف نثبّته؟ | استأجر VPS بفيدورا، شغّل `system-reinstall-bootc`، أعد الإقلاع. صار MoOS. |
| كم يكلّف؟ | **€3.79–7 شهرياً** (Hetzner) — أو **مجاناً** على Oracle بعد نقل المعمارية إلى ARM. |
| كيف أراه؟ | Tailscale (مثبّت أصلاً) + Mo PC Remote (مبني أصلاً) — نفس تجربة الآيفون الحالية. |
| Mo AI بلا كرت شاشة؟ | بوّابة Mo AI تدعم "الدماغ السحابي" أصلاً — مفتاح API بدل GPU. |
| التحديثات؟ | نفس القطار تماماً: `uupd`/`bootc upgrade` من ghcr، متحقَّق منها بـ cosign. |

---

## ١. لماذا نفس المستودع ولا ريبو جديد

المستودع الحالي يبني **إصدارين من شجرة واحدة** (`moos` و`moos-nvidia`) عبر مصفوفة
في `.github/workflows/build.yml`، والفرق بينهما وسيط بناء واحد (`AKMODS_IMAGE`).
إضافة `moos-cloud` هي **صف ثالث في نفس المصفوفة**، لا مشروع جديد.

ريبو منفصل يعني:

- ازدواج ١٦ بوابة اختبار — وكل إصلاح يُنفَّذ مرتين أو ينسى في أحدهما؛
- انقسام الهوية: `verify_identity.py` وعائلة الثيمات و`system_files` مصدرها واحد اليوم؛
- قطارا تحديث منفصلان بمفتاحَي توقيع — وهذا بالضبط ما يجعل نسخة تتخلّف بصمت.

المستودع الواحد يعني: تُصلح عطلاً في Mo AI مرة واحدة، فيصل إلى مكتبك وإلى السحابة
في نفس البناء.

---

## ٢. كيف يصل MoOS إلى خادم سحابي — ثلاثة مسارات

### المسار (أ) — التحويل من فيدورا قائمة ← **الموصى به**

معظم مزوّدي VPS لا يسمحون برفع صورة خاصة، لكن كلهم تقريباً يعرضون **Fedora**.
و`bootc` يملك أمراً يحوّل نظاماً قائماً إلى نظام صور:

```bash
# على الخادم بعد تثبيت Fedora 44 من لوحة المزوّد
sudo dnf install -y system-reinstall-bootc
sudo system-reinstall-bootc ghcr.io/moalfarras-sys/moos-cloud:latest
sudo reboot
```

`system-reinstall-bootc` يسحب الصورة، ويحافظ على مفاتيح SSH (وإلا فقدتَ الوصول)،
ويشغّل `bootc install to-existing-root`، ويضيف وحدة تنظيف لبقايا النظام القديم.
بعد الإقلاع: الخادم **هو** MoOS، ويأخذ التحديثات من نفس السجلّ الموقّع.

هذا المسار لا يحتاج أي دعم خاص من المزوّد — يعمل على Hetzner وContabo وOVH وVultr
وOracle وأي شيء يقدّم فيدورا.

### المسار (ب) — قرص qcow2 جاهز

المستودع **يبني هذا أصلاً**: `.github/workflows/build-disk.yml` ينتج qcow2 عبر
`bootc-image-builder` من الصورة المنشورة. المزوّدون الذين يسمحون بصور مخصّصة
(Hetzner عبر وضع الإنقاذ + `dd`، Vultr، OVH) يقبلونه مباشرة.

### المسار (ج) — الـISO الحيّة

نفس الملف الذي بين يديك الآن. يصلح لخادم منزلي أو VPS يدعم إقلاع ISO — لكنه أطول
مساراً من (أ) على السحابة.

---

## ٣. الأسعار — ما هو الأرخص فعلاً (تموز ٢٠٢٦)

| الخيار | المواصفات | السعر | الحكم |
|---|---|---|---|
| **Hetzner CX22** | 2 vCPU / 4 GB / 40 GB | **€3.79/شهر** | أرخص بداية جدّية. كافٍ لطرفية ومحرر وحاويات؛ سطح المكتب البعيد سيكون ضيّقاً. |
| **Hetzner CX32** | 4 vCPU / 8 GB / 80 GB | **≈€7/شهر** | **التوصية.** مريح للتطوير + بث سطح المكتب + حاويات. |
| **Hetzner CAX11** (ARM) | 2 vCPU / 4 GB | €5.99/شهر | يحتاج نقل MoOS إلى aarch64. |
| **Oracle Always Free** (ARM) | 2 OCPU / 12 GB / 200 GB | **مجاني دائماً** | مجاني فعلاً، لكن: ARM (يحتاج النقل)، وقُلِّص من 4/24 إلى 2/12 في حزيران ٢٠٢٦، وسعة المناطق متذبذبة. |
| Contabo | 4 vCPU / 8 GB | ≈€5–6/شهر | أرخص على الورق، أبطأ قرصاً وشبكة. |

**ملاحظة صادقة عن "المجاني":** Oracle هو الخيار المجاني الوحيد الجادّ للتشغيل
الدائم، وثمنه معماري لا نقدي — MoOS اليوم x86_64 فقط، لأن صورته الأساس
`ghcr.io/ublue-os/kinoite-main:44` تُنشر لـ x86_64. النقل إلى ARM يعني تبديل الأساس
إلى `quay.io/fedora-ostree-desktops/kinoite:44` (يملك aarch64) وإعادة فحص كل بوابة.
عمل حقيقي، لكنه يفتح "خادم مجاني للأبد".

**الخطة الأذكى:** ابدأ اليوم على Hetzner x86 (يعمل فوراً بالكود الحالي)، وانقل
لـ aarch64 لاحقاً لتضيف خادماً مجانياً ثانياً — لا لتستبدل الأول.

---

## ٤. كيف ترى الواجهة عبر الإنترنت

MoOS **يملك الجواب أصلاً**: `mo-remote` + Tailscale، وهو ما تستعمله من آيفونك الآن.

- الخادم السحابي ينضم إلى نفس شبكة Tailscale → يظهر كجهاز آخر عندك؛
- Mo PC Remote يبثّ سطح المكتب بنفس الواجهة والثيم؛
- **لا منفذ مفتوح على الإنترنت** — لا 5900 ولا 3389؛ كل شيء داخل الشبكة الخاصة.

الحدّ الصادق: بلا كرت شاشة، الترميز يتم على المعالج. على 2 vCPU سيكون البثّ ثقيلاً؛
على 4 vCPU مقبول لعمل المطوّر (محرر، طرفية، متصفّح). هذا سبب توصية CX32.

بديل أخفّ للعمل اليومي: SSH + VS Code Remote عبر Tailscale — بلا بثّ أصلاً، وسطح
المكتب يبقى متاحاً عند الحاجة.

---

## ٥. Mo AI على خادم بلا كرت شاشة

بوّابة Mo AI مبنية على التوجيه بين **دماغ محلي ودماغ سحابي لكل طلب**، و`moos-selfcheck`
يذكّرك اليوم بأن الدماغ السحابي غير مضبوط. على الخادم السحابي:

- **الدماغ السحابي** (مفتاح API) هو الافتراضي — لا GPU، لا انتظار؛
- نماذج صغيرة على المعالج تبقى ممكنة للمهام الخفيفة؛
- وإن أردت قوة كرتك الحقيقي: جهاز المكتب يبقى على نفس شبكة Tailscale، فيستطيع
  الخادم استدعاءه كدماغ عندما يكون شغّالاً — بلا فتح أي منفذ.

استئجار GPU سحابي (0.2–0.5 دولار/الساعة) يساوي ١٥٠–٣٦٠ دولاراً شهرياً للتشغيل
الدائم — لا يستحق مقابل مفتاح API.

---

## ٦. ما الذي يتغيّر فعلاً في إصدار `moos-cloud`

نفس `system_files` ونفس الهوية، مع فروق مقصودة:

1. **بلا NVIDIA** — `AKMODS_IMAGE=scratch` (موجود كوسيط بناء أصلاً).
2. **بلا ألعاب ولا Waydroid** — إسقاط `gamescope`/`gamemode`/`mangohud`/`waydroid`/
   `steam-devices`؛ توفير ≈١–٢ غيغابايت من صورة حجمها اليوم 5.3 غ.ب مضغوطة.
3. **مضبوط للعرض البرمجي (llvmpipe):** `BlurStrength` منخفض أو مطفأ، الحركات
   مخفَّضة، و`AmbientMotion=false` افتراضياً. بوابة الحركة التي أُضيفت اليوم تجعل
   هذا مفتاحاً واحداً لا مطاردة رسوم.
4. **دخول آمن:** لا دخول تلقائي، كلمة مرور إجبارية، SSH بمفتاح فقط، جدار ناري
   مغلق إلا Tailscale.
5. **قابلية الإنقاذ:** كونسول تسلسلي مفعّل (`console=ttyS0`) ليعمل "Rescue" عند
   المزوّد، و`cloud-init` أو ضبط أول إقلاع للمستخدم والمفاتيح.
6. **بلا مثبّت ولا ISO** — الخادم لا يحتاج Anaconda ولا شاشة ترحيب حيّة.

كل هذا وسيطُ بناء واحد وشروط في `build.sh`، لا شجرة ثانية.

---

## ٧. التطوير عليه — والتطوير به

سؤالك: "ممكن أطوّر النظام نفسه أو مشروع ثاني عليه؟" — الاثنان، وبطريقتين مختلفتين:

- **مشاريعك الخاصة:** الخادم بيئة تطوير كاملة ٧/٢٤ — حاويات، خدمات، Mo AI،
  وVS Code Remote عبر Tailscale. هذا استعماله الطبيعي.
- **تطوير MoOS نفسه:** تبني الصورة على الخادم بـ`podman build` وتختبرها بـ
  `bootc switch` + إعادة إقلاع، والرجوع دائماً `rpm-ostree rollback`. لكن **لا
  تجعله جهاز البناء الوحيد**: بناء الصورة يحتاج ≈٢٠ غيغابايت مؤقتة و٢٠ دقيقة على
  4 vCPU، وCI في GitHub يفعلها مجاناً وأسرع. الخادم للاختبار الحيّ، وCI للبناء.

---

## ٨. المخاطر والحدود — بصراحة

- **العرض البرمجي ليس كرت شاشة.** الواجهة ستعمل، والفيديو والتأثيرات الثقيلة لا.
- **2 vCPU لا يكفي للبثّ + عمل حقيقي** في نفس الوقت. ابدأ من 4.
- **Oracle المجاني يُسترجَع** إن بقي الخادم خاملاً طويلاً، وسعته متذبذبة حسب المنطقة.
- **نقل ARM ليس تبديل سطر.** أساس مختلف، ومراجعة كل بوابة، وبناء أبطأ.
- **الأمان مسؤولية جديدة:** خادم دائم على الإنترنت باسمك. قاعدة واحدة تحكم الأمر:
  لا شيء مكشوف خارج Tailscale.

---

## ٩. خطة التنفيذ — على مراحل، كل مرحلة قابلة للإثبات

| المرحلة | العمل | الإثبات |
|---|---|---|
| **١** | إضافة `moos-cloud` كصف ثالث في مصفوفة CI + شروط `build.sh` (بلا ألعاب/NVIDIA، حركة مخفَّضة، كونسول تسلسلي) | `just check` أخضر + الصورة تُبنى وتُوقَّع في CI |
| **٢** | استئجار Hetzner CX32 بفيدورا + `system-reinstall-bootc` + إعادة إقلاع | `rpm-ostree status` يقول `moos-cloud` موقّعة، و`post-update-check.sh` أخضر |
| **٣** | ضمّ الخادم إلى Tailscale + تفعيل Mo PC Remote + الدماغ السحابي لـMo AI | فتح سطح المكتب من الآيفون؛ `moos-selfcheck` أخضر على الخادم |
| **٤** | تشغيل `uupd` على نفس الجدول → إثبات أن تحديثاً واحداً يصل للمكتب وللسحابة | البصمتان متطابقتان بعد التحديث |
| **٥** (اختياري) | نقل الأساس إلى aarch64 → خادم Oracle مجاني دائم كنسخة ثانية | صورة `moos-cloud` تُبنى لـarm64 وتُقلع على Oracle |

المرحلتان ١ و٢ وحدهما تعطيانك "MoOS على السحابة ٧/٢٤" — والباقي تحسين.

---

*آخر تحديث: 2026-07-25 — كُتب بعد إغلاق بوابة الإصدار الموقّع على جهاز المكتب.*
