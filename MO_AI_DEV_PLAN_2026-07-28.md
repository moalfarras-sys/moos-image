# خطة تطوير Mo AI — 2026-07-28

> كُتبت بعد فحص حيّ للجهاز نفسه (الخدمات شغّالة، المنافذ، النماذج المثبّتة، وسجلّات
> Mo PC Remote)، لا من قراءة الكود وحده. كل رقم هنا مقيس على هذه الآلة.

---

## 1) ما هو Mo AI فعلياً اليوم — الخريطة الصحيحة

Mo AI ليس نافذة دردشة. هو **ستّ قطع** تعمل معاً:

| القطعة | الملف | الحجم | الدور | الحالة الحيّة |
|---|---|---|---|---|
| الواجهة | `usr/share/moos/apps/moai/main.qml` | 4798 سطر | الدردشة + لوحات الأجهزة/التوافق/التطبيقات/Mo PC Remote/وكلاء البرمجة | تعمل |
| البوّابة | `usr/bin/moai-gateway` (`:8080`) | 915 سطر | الباب الأمامي على localhost — توجّه كل طلب: محلي أو سحابي | **شغّالة** |
| التحكم | `usr/bin/moai-control` (`:8079`) | 1504 سطر | خلفية الإعدادات داخل التطبيق (محميّة بتوكن) | **شغّالة** |
| واجهة الوكيل | `usr/bin/moai-agent-api` (`:8077`) | 1119 سطر | إعدادات/جلسات/حالة لوحة الوكلاء | **شغّالة** |
| مستقبِل تيليجرام | `usr/bin/moai-wake` | 207 سطر | يوقظ OpenClaw عند الطلب | **شغّالة** |
| المنفّذ الآمن | `usr/bin/moai-do` | 1060 سطر | قائمة إجراءات مغلقة + تأكيد + Polkit | جاهز |

**العقول المثبّتة محلياً:** `moai-brain`, `moai-brain-fast`, `qwen2.5`, `qwen3`, `qwen3-vl`.
**العقل السحابي:** `curl :8080/health` يردّ حرفياً `{"error":{"message":"cloud brain not configured"}}` — أي **غير مُعدّ**.

---

## 2) نموذج الصلاحيات — الإجابة الدقيقة على «بدي أعطيه صلاحيات أكثر»

هذه أهم نقطة في الخطة، ولازم تكون واضحة قبل أي اقتراح.

**Mo AI عنده اليوم مستويان مختلفان تماماً من الصلاحية:**

### أ) داخل سطح المكتب — صلاحية صفر بشكل مقصود
`main.qml` هو **QML صِرف: لا يستطيع تنفيذ أمر واحد**. الدورة كاملة هي:

```
النموذج يذكر أمراً  →  الواجهة تحوّله إلى زر "شغّل"  →  moos://…  →  moos-open (case ثابت)
                                                                      →  moai-do <action>  →  تأكيد + pkexec
```

`moai-do` قائمة مغلقة من **20 إجراءً**: `update`, `install <flatpak-id>`, `fix-audio`,
`check-drivers`, `install-nvidia`, `update-firmware`, `setup-waydroid`, `setup-windows`,
`setup-gaming`, `remote-anywhere`, `install-codex`, `install-claude`, `install-opencode`,
`install-openclaw`, `setup-brain`, `optimize`, `diagnose-services`, `inspect-boot`, `hw-report`, `help`.

**النموذج لا ينفّذ شيئاً أبداً.** وهذا ليس نقصاً — هو الشيء الوحيد الذي يجعل تسجيل
`moos:` كـ URL scheme آمناً: أي صفحة ويب تزورها تستطيع تمرير `moos://…`، والناجي الوحيد
من ذلك هو أن كل مسار إجراء ثابت. `AGENTS.md` ينصّ على هذا صراحة: *«لا تضف مساراً يسمح
لنموذج، أو لصفحة ويب، بتشغيل أمر»*.

### ب) عبر تيليجرام (OpenClaw) — صلاحية كاملة
نفس المساعد، لكن يعمل **بصدفة حقيقية بصلاحيات المالك والـsandbox مطفأ**، ومعه أدوات جاهزة:
`moai-open` (يفتح تطبيقاً ويبقيه مفتوحاً)، `moai-screenshot`، `moai-camera-shot`،
`ydotool` (كتابة ونقر)، `moai-do update`.

### الخلاصة
الطريق الصحيح لتوسيع الصلاحيات ليس فتح النموذج على الصدفة — بل **توسيع قائمة
`moai-do`**، وهذا مسار مُعتمَد في `AGENTS.md`: أي إجراء جديد يُضاف في ثلاثة أماكن
(`moai-do` + `case` في `moos-open` + نصّ Mo AI) وبوّابة `verify_user_experience.py`
تتحقّق من الاتجاهين.

---

## 3) الصلاحيات المقترح إضافتها (المرحلة 1)

كل واحدة إجراء ثابت، بتأكيد، وبنفس نمط ما هو موجود:

| الإجراء الجديد | لماذا | الخطورة |
|---|---|---|
| `moai-do rollback` | الرجوع للنشر السابق موجود كتطبيق (`moos-rollback`) لكن **النموذج لا يستطيع اقتراحه**. هذه أهم عملية إنقاذ في نظام bootc ولا يعرفها المساعد. | منخفضة — عملية ذرّية وقابلة للعكس |
| `moai-do free-space` | `optimize` ينظّف، لكن لا يوجد إجراء صريح لاستعادة مساحة `/var` مع تقرير «كم استرجعنا». | منخفضة |
| `moai-do net-doctor` | لا يوجد أي تشخيص للشبكة/DNS/Tailscale — وهو أول ما يُسأل عنه لما ما يشتغل الريموت. | معدومة (قراءة فقط) |
| `moai-do gpu-report` | يقيس VRAM ويقول من يحتجزه. مرتبط مباشرة بالمشكلة في القسم 5. | معدومة (قراءة فقط) |
| `moai-do power lock\|sleep\|restart` | متاح للجوال عبر Mo PC Remote، وغير متاح للمساعد على نفس الجهاز. | متوسطة — تأكيد إجباري |
| `moai-do service <name> restart` | مقيّد بقائمة أسماء معروفة (`moai-*`, `mo-remote-personal`, `pipewire*`) لا اسم حر. | متوسطة — القائمة هي الحارس |
| `moai-do backup-home` | **لا توجد قصة نسخ احتياطي إطلاقاً في MoOS اليوم.** | منخفضة |

> ⚠️ لا تُضاف أي منها كأمر حرّ. كل واحدة `case` ثابت. القيمة الحرّة الوحيدة المسموحة اليوم
> هي `apps/install/<id>` وهي تتحقّق من شكل reverse-DNS مرتين (في `moos-open` وفي `moai-do`).

---

## 4) سهولة الاستخدام (المرحلة 2) — مرتّبة بالأثر

1. **العقل السحابي غير مُعدّ.** الشريحة بجانب صندوق الرسالة تعرض «سحابي» لكن `/health`
   يقول غير مُعدّ. أول احتكاك يقابله المستخدم. الحل: عند اختيار «سحابي» بلا مفتاح، تفتح
   الشريحة شاشة إدخال المفتاح مباشرة بدل رسالة خطأ.
2. **الصوت — القطعة الجاهزة التي لا تُستخدم.** حاوية `speaches` **شغّالة الآن** و
   `moai-transcribe` جاهز، لكنه مكتوب لـ OpenClaw (تيليجرام) فقط. زر «اضغط وتكلّم» في
   واجهة سطح المكتب = أكبر مكسب مقابل أقل عمل، لأن كل الخلفية موجودة.
3. **Mo AI لا يرى الشاشة.** `moai-screenshot` موجود ويعمل — لكن لتيليجرام. إضافة «انظر إلى
   شاشتي» (بموافقة صريحة كل مرة) تحوّل المساعد من «يجاوب على أسئلة» إلى «يساعدني فيما أفعله الآن».
   وهذا أيضاً ما يجعل `qwen3-vl` المثبّت أصلاً مفيداً بدل أن يكون نموذجاً معطّلاً.
4. **Spotlight** — مذكور في `MOOS_ROADMAP.md` سطر 599: لوحة `Cmd+Space` تبحث وتنفّذ.
5. **لوحة «خطة → معاينة → تطبيق»** بدل زر «شغّل» المفرد: يعرض ما سيتغيّر قبل التنفيذ.

---

## 5) الاكتشاف الأهم: Mo AI يسرق مُرمِّز Mo PC Remote — وهذا مُصلَح الآن

هذا ليس اقتراحاً، هذا خلل حقيقي وجدته ووصلته أثناء هذا العمل.

**الدليل من الكود نفسه** (`mo-remote-portal.py`): `nvh264enc` يفشل بـ
`Failed to open encoder` عند استهلاك **7748/8192 MiB** من ذاكرة الكرت، ويعمل عند **7625**.
و`moos-gpu-headroom` يوثّق أن العقل المحلي (`moai.service`) يحتجز **~6 GB** ولا يطلقها.

**التسلسل الذي كان يحصل:**

```
تدردش مع Mo AI  →  العقل يحمّل 6 GB على الكرت  →  تفتح الريموت من الجوال
  →  NVENC يرفض الفتح  →  المساعد يضع nvh264enc في قائمة سوداء **دائمة**
  →  الجلسة تنزل إلى JPEG:  79 Mbit/s بدل 4.3  →  «الصورة على جوالي سيئة»
```

ولا شيء في أي مكان يقول إن المساعد أخذ الكرت.

**و`moos-gpu-headroom` موجود أصلاً لحلّ هذا بالضبط** — يستدعيه MoPlayer و`moos-open`
و`moai-do` و`moos-storectl`. **Mo PC Remote كان المستهلك الوحيد للكرت الذي لا يستدعيه.**

**ما نُفّذ:**
- القائمة السوداء صارت تنتهي صلاحيتها (90 ثانية) بدل أن تكون أبدية.
- عند فشل مُرمِّز عتادي، يُستدعى `moos-gpu-headroom` على خيط جانبي ثم تُعاد بناء الأنبوب.
- النتيجة: العقل يُفرَّغ، الريموت يستعيد التشفير العتادي، و`moai-gateway` يعيد تحميل العقل
  تلقائياً عند رسالتك التالية.

---

## 6) ترتيب التنفيذ المقترح

**الآن (منفّذ في هذه الجلسة):** ربط VRAM بين Mo AI و Mo PC Remote.

**التالي — أسبوع واحد:**
1. `moai-do rollback` + `net-doctor` + `gpu-report` (الثلاثة قراءة/عكسية، أقل خطورة وأعلى قيمة).
2. زر «اضغط وتكلّم» في `main.qml` موصولاً بـ `moai-transcribe`.
3. إصلاح مسار العقل السحابي: شريحة الاختيار تفتح إدخال المفتاح.

**بعدها:**
4. «انظر إلى شاشتي» بموافقة صريحة، موصولاً بـ `qwen3-vl`.
5. `moai-do backup-home`.
6. Spotlight (`Cmd+Space`).

**لا تفعل:**
- لا تعطِ النموذج صدفة داخل سطح المكتب.
- لا تضف إجراءً يقبل أمراً حرّاً.
- لا تلتفّ على `pkexec` بـ sudo بلا كلمة سر.
- أي إجراء جديد = ثلاثة ملفات (`moai-do` + `moos-open` + نصّ Mo AI) وإلا فالزر ميّت
  والبوّابة ستكشفه.
