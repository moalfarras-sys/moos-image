[Unit]
# A LiveOS has no persistent bootloader to update.  Its /sysroot layout can
# look writable to bootupd even though the firmware target belongs to the live
# medium, so use the authoritative OSTree boot marker instead.
ConditionPathExists=/run/ostree-booted
