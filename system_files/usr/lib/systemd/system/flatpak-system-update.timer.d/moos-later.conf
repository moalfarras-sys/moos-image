# ...and it should not start two minutes into the session either. Ten minutes in, the login
# storm is over. The daily 04:00 run (Persistent=true) is untouched, so a machine that is
# only ever on for five minutes still gets updated on its next long session.
[Timer]
OnBootSec=10min
