[Unit]
# tuned-ppd is the desktop API facade for tuned. Keep both halves of the
# hardware power policy out of a disposable LiveOS session: tuned itself is
# conditioned the same way, and starting only the facade leaves a failed unit.
ConditionPathExists=/run/ostree-booted
