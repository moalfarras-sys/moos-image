[Unit]
# Existing users may have a stronger ~/.config/kcminputrc that predates the
# MoOS NumLock default. Repair it before KWin reads the startup-only value.
Wants=moos-input-migrate.service
After=moos-input-migrate.service
