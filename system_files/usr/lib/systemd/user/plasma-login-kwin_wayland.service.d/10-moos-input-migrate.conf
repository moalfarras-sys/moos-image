[Unit]
# The greeter runs its own KWin under the persistent plasmalogin account; give
# it the same startup migration ordering as a normal Wayland user session.
Wants=moos-input-migrate.service
After=moos-input-migrate.service
