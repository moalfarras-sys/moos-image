[Service]
# Flatpak's vendor unit has no recovery path when a remote static delta is
# malformed.  Keep one authority and replace only its command, not its timer.
ExecStart=
ExecStart=/usr/libexec/moos-flatpak-user-update
