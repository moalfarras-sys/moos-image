[Service]
# Flatpak's vendor unit has no recovery path when a remote static delta is
# malformed. Keep the user timer as the authority for this scope and share the
# same implementation as the system timer.
ExecStart=
ExecStart=/usr/libexec/moos-flatpak-update --user
