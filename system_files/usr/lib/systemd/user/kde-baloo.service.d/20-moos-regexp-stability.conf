[Service]
# Station .938: PCRE2 JIT SEGV while shouldFileBeIndexed() matched filenames.
# Keep the existing index and Qt's normal matcher; scope this to Baloo only.
# Recovery measured 2026-09-26: 54991 files, Idle, zero failed indexing jobs.
Environment=QT_ENABLE_REGEXP_JIT=0
