# TCP BBR, loaded at boot so the sysctl that selects it cannot lose a race.
#
# sysctl.d/90-moos-desktop.conf sets net.ipv4.tcp_congestion_control = bbr. Writing
# that sysctl does ask the kernel to autoload tcp_bbr, so on a healthy boot this file
# is belt-and-braces — but the failure mode without it is silent and permanent for
# the boot: systemd-sysctl logs one line about an invalid value, the machine stays on
# cubic, and nothing else ever says so. A congestion control algorithm that is
# supposed to be there and is not looks like "the remote is slow again today".
#
# systemd-sysctl.service is ordered After=systemd-modules-load.service, so listing it
# here means the algorithm is registered before anything tries to select it.
#
# Measured before this existed:
#     net.ipv4.tcp_available_congestion_control = reno cubic
# i.e. bbr was not even offered, despite tcp_bbr.ko.xz being present in the image.
tcp_bbr

# The fq packet scheduler, for exactly the same reason and with exactly the same
# silent failure. sysctl.d/90-moos-desktop.conf sets net.core.default_qdisc = fq,
# which BBR needs to pace correctly. sch_fq is a module, not built in, so on the
# primary systemd-sysctl pass — before anything has autoloaded it — the write is
# rejected:
#     systemd-sysctl: Couldn't write 'fq' to 'net/core/default_qdisc': No such file or directory
# and the interface silently keeps fq_codel. On this machine the qdisc only ended up
# 'fq' because a later re-exec ran sysctl a second time after sch_fq had autoloaded
# for another reason; a clean single-pass boot would keep the wrong scheduler with no
# error anyone sees. Preloading it here, After=systemd-modules-load.service, registers
# fq before the sysctl selects it — the same belt-and-braces as tcp_bbr above.
sch_fq
