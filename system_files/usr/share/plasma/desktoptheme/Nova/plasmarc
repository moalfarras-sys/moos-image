# MoOS Nova — Plasma Style per-theme config.
#
# [Settings] FallbackTheme is the VERIFIED inheritance mechanism: Plasma's
# ThemePrivate::setThemeName() (plasma/libplasma src/plasma/private/theme_p.cpp)
# reads configForTheme(<theme>) -> desktoptheme/<theme>/plasmarc, group
# [Settings] key FallbackTheme, and follows the chain, then always appends the
# built-in "default" theme (= Breeze). Nova owns its identity-bearing glyphs
# plus the first safely isolated FrameSvg states; the fallback chain supplies
# only paths that do not yet have a Nova SVG. Nova's selected `colors` file is
# retained while SVG lookup walks the chain:
#   Nova SVG + Nova colors -> breeze-dark SVG lookup -> default Breeze geometry
#   rendered with Nova colors
[Settings]
FallbackTheme=breeze-dark

# Default wallpaper hint for this Plasma Style (matches the MoOS Nova Horizon
# wallpaper shipped under /usr/share/wallpapers/NovaHorizon). This is only a
# hint; the operative wallpaper is set by the org.moos.nova Look-and-Feel.
[Wallpaper]
defaultWallpaperTheme=NovaHorizonII
defaultFileSuffix=.png
defaultWidth=1920
defaultHeight=1080

# The dock stays FROSTED GLASS at all times — like the macOS dock and the Windows 11
# acrylic taskbar — instead of turning opaque under a maximised window (the upstream
# Breeze "adaptive" behaviour). Verified real: libplasma exposes
# Plasma::Theme::adaptiveTransparencyEnabled(), which reads this key; false = never go
# opaque. The blur (Effect-blur, shipped on) frosts whatever scrolls behind it. Over the
# navy Nova wallpaper the effect is quiet; over a colourful maximised app it reads as
# real glass, which is the whole point of a floating dock.
[AdaptiveTransparency]
enabled=false
