# Compatibility identity for tooling that reads /etc/default/grub. Installed
# MoOS uses bootupd's static grub.cfg; this file does not control its timeout,
# graphics or theme and must never be used as a reason to run grub2-mkconfig.
GRUB_DISTRIBUTOR="MoOS"
