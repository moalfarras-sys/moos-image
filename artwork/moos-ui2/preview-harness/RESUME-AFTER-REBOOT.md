# Continuation — after the FOURTH reboot (2026-08-02 ~15:50Z) into .515

Booting `44.20260802.515` (`12686b7b…`) = head `71d1b466`: the ARC-FREE build
(36 TidalHorizon files deleted, splash re-centred, 6 gates now BAN the arc)
on top of the dark-Complementary correction proven live on .513
(lock-513-true-island.png = dark warm island, amber accents — already WOW-level
vs the .512 ghost).

Remaining to close the Stop-hook goal:
1. Confirm booted digest == 12686b7b; quick post-update-check.
2. FINAL capture set from the live session (env vars as before):
   - lock via kscreenlocker_greet --testing + ydotool wake → EXPECT the .513
     island WITHOUT the arc.
   - power doorway from installed /usr (make-preview.sh, sdtype 1 and 3) —
     arc-free island alone.
   - splash via `ksplashqml org.moos.ui2.study.light --test` (centred brand).
3. Commit the final captures + PROJECT_STATE closing entry (single docs push).
4. Final bilingual report: before/after table (before-restart-scholar-light.png
   + lock-current-awake.png vs the final set), what shipped, remaining items
   (Telegram token owner-side; desktop gets it via moos-update).
