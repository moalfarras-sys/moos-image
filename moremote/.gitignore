# ---- Node / frontend ----
node_modules/
controller/dist/
controller/.vite/
npm-debug.log*

# ---- .NET / agent ----
bin/
obj/
publish/
*.user

# ---- Build output served to phone ----
# NOT ignored: the shipped UI IS the built bundle in agent/wwwroot — it is tracked
# at the repo root so the image ships exactly what the controller source builds.
# (Only local dev noise below stays ignored.)

agent/wwwroot/workbox-*.js
agent/wwwroot/icons/

# ---- Generated icons (derived from Logo.png) ----
agent/app.ico
controller/public/icons/

# ---- Runtime data (created on the user's machine, never commit) ----
*.dat
config.dat
data/

# ---- OS / editor ----
Thumbs.db
.DS_Store
.vs/
.idea/
.vscode/
